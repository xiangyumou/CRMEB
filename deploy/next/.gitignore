# The configured deployment: two passwords and the digests of whatever this
# host is actually running. `deployment.env.example` is the committed template.
deployment.env
deployment.env.*
!deployment.env.example

# Dumps, upgrade manifests and settings backups.
data/
