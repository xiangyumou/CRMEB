/**
 * Type (and test-time) stand-in for `dist/admin-css.js`: the admin canvas
 * stylesheet as a string, which the editor injects into its iframe. The built
 * file replaces this one at runtime (see `package.json` `exports`).
 */
const css: string = '';
export default css;
