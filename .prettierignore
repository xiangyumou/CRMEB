# Generated or machine-owned: formatting them is either pointless or harmful.
pnpm-lock.yaml
node_modules/
dist/
.next/
.turbo/
coverage/
*.gen.ts
openapi.json
# Written by `next build`, so the Definition of Done's own order (build, then
# `prettier --check .`) goes red without this. See docs/rewrite/status/golden.md.
apps/web/next-env.d.ts

# Data, not code. The DIY fixtures are production exports and must stay byte-exact.
packages/contracts/src/diy/__fixtures__/
packages/db/seed-data/
packages/db/migrations/

# The uni-app is its own npm project with its own (HBuilderX) formatting.
apps/uni-app/
