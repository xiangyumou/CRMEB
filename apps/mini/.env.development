# Taro env file: https://docs.taro.zone/docs/next/env-mode-config
# `touristappid` is WeChat DevTools' built-in tourist id: it needs no account and cannot
# upload. Put a real AppID in `.env.development.local` / `.env.production.local`
# (gitignored), never here.
TARO_APP_ID="touristappid"
