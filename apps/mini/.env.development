# Taro env file: https://docs.taro.zone/docs/next/env-mode-config
# The shop's own mini-program AppID. It is public (every share card shows it) and the only one
# `pnpm guards` [config] allows in a committed file. A personal test AppID goes in
# `.env.development.local` / `.env.production.local` (gitignored). The AppSecret never goes
# into the mini-program: it lives only in the server's config.
TARO_APP_ID="wx4f4b772125e155ed"
