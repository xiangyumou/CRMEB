# AUDIT — security review, first and second pass

Stream K, `K-hardening.md` §5. The four surfaces where a mistake costs money or
data — **auth, payment, refund, upload** — read as an attacker, with PLAN §5
"修而不搬" as the checklist.

Every row ends in one of three places: a **test that exists** (named exactly, so
it can be run), a **CR** (a defect, with the fix proposed), or a **decision**
with the reasoning written down. Nothing ends in "looks fine".

Scope: the merged tree at `bdcf104a9` (before the E1 merge). Rows whose subject
belongs to a stream still in flight are marked `pending:<ws>` and must be
re-read in the second pass — see the bottom of `docs/rewrite/status/k.md`.

**Read the `pending:` markers against the date.** The branch was rebased onto
`rewrite/integration` at `612d0a000`, by which point D, E1, E2, F1, F2, G1-G3,
H, J and S had all merged. The guard counts below were re-run on that tree and
are current; the `pending:` rows were **not** re-read, because reading a surface
as an attacker is the work, not the label. Every one of them is on K2's list at
the bottom of `status/k.md`, and the merge that unblocks it has already
happened — so K2 starts with the audit, not with waiting.

**Second pass (K2, 2026-09-23).** Every `pending:` row and every "Re-check in
K2" line was re-read against `rewrite/ws-k2-hardening` at `2fe9947a8` (tree
`0a81148fe`, which is `rewrite/integration` at `7d0c63fed` plus nothing but K2's
own tests and docs), and so were the surfaces that merged after the first pass —
E2/E3's OA admin routes and webhook, the notification routes, E4, F4, B3, A2's
staff products and J3's `/readyz` — in the new section **Second pass — surfaces
merged after the first** below. Rows whose referenced CR has since landed or
stayed open say which, with the test that proves it. A defect found in this
pass is a `CR-<n>-k2` with its failing test committed as `it.fails` beside the
domain, so the test flips the day the fix lands. Route- and edge-level defects
(outside K2's test grant) are CRs without a test and say so.

Ids are `K-SEC-<area><n>`: **A** auth, **P** payment, **R** refund, **U**
upload, **X** cross-cutting.

## PLAN §5 — the ten "fix, don't port" items

| # | Legacy defect | State | Where | Evidence |
| --- | --- | --- | --- | --- |
| 1 | `takeOrder`/`delivery` read-then-write with no lock | fixed | `order.fulfil.*` (B2) | `order.fulfil.concurrency.int.test.ts::shipping while a refund is approved for the same line > never lets shipped + refunded exceed what was ordered`, plus the CHECK `order_items_shipped_within_quantity` |
| 2 | `applyRefund` duplicate check outside the transaction | fixed | `refund.service.ts:171` `lockOrder` (`FOR UPDATE`) + partial unique `refund_items_open_uq` | `refund.concurrency.int.test.ts::REFUND-001 — two refund requests on one order line > lets exactly one request hold the line` (6 racers, 1 winner) |
| 3 | `incStockDecSales` read-then-write | fixed | single conditional `UPDATE` (B1) | `order.concurrency.int.test.ts::two checkouts for the last unit > …` |
| 4 | storefront token not bound to the password | fixed **on the storefront**, **still broken on the admin side** (K2) | `user-session.service.ts:88` compares `passwordVersion`; the admin store compares nothing and its per-admin index is not slid | **CR-8-k**, open, now pinned: `auth/admin-session.revoke.int.test.ts::K-SEC-A1 — revoking every session of an admin > reaches a session that has been kept alive past four TTLs` (`it.fails`) |
| 5 | `scan_upload` global token | fixed | `scan-token.ts` — per-admin, single-use Lua CAS, 600 s TTL | `storage.concurrency.int.test.ts::scan tokens are single-use > lets exactly one of six phones upload through one QR code` |
| 6 | `videoDataSave` trusts the client's path | fixed — the endpoint does not exist, and no API accepts a key | `kernel/storage.ts:111-117` generates `<dir>/<YYYY>/<MM>/<uuid>.<ext>` | `s3.test.ts::createS3Storage > generates the key itself — a caller cannot choose a path` |
| 7 | `onlineUpload` SSRF | fixed as a design, **still broken as a transport** (K2) | `safe-fetch.ts` judges literals and every resolved address, re-judges every redirect hop, pins the connection to the judged IP (`safe-fetch.ts:247` still rewrites `hostname`) | `safe-fetch.test.ts` (12 its, incl. `refuses a public name that RESOLVES to a private address`, `refuses a redirect into the private network`). Transport defect: **CR-11-k**, open — and it now also breaks F4's `POST /api/v1/attachments/base64` for every `https://` own-origin URL (K-SEC-F1) |
| 8 | SMS code replay | fixed (K2) | E1: the code is consumed by an atomic Lua compare-and-delete, the key carries the scene, and wrong guesses are counted against the code | `user/storefront-auth.int.test.ts::code verification > destroys the code on use, so it cannot be replayed`, `> destroys the code after the configured number of wrong guesses`, `> will not let a code minted for one scene work in another`; `user/user.concurrency.int.test.ts::SMS codes > lets exactly one of six concurrent verifications spend a code`. Send-side budgets: K-SEC-S2 |
| 9 | code stored in the DB and `eval`'d | fixed and guarded | feature removed; `pnpm guards`' `banned` check refuses `eval`, `new Function`, `child_process` outside scripts, and asserts `no-eval`/`no-new-func` are still lint errors | `guards/src/checks/banned.ts`, run by `pnpm guards` |
| 10 | `handleTransferNotify` unverified | fixed — **there is no transfer endpoint**, and both webhooks share one verifier | `grep handleTransferNotify` → nothing; `wechat.crypto.ts:101-116` | `payment.int.test.ts::PAY-003 … > answers 401 for a signature it cannot verify, before touching the database` |

## Auth

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-A1 | A password change revokes every admin session | **defect**, still open (K2) | The per-admin Redis index expires at `ttlMs * 4` and is never slid (`admin-session.store.ts:68`), while the session key is slid on every request, so a session older than 32 h is invisible to `revokeAllForAdmin`. The `passwordVersion` "second belt" does not exist on the admin side (`AdminAuthService.resolve` is the store's `resolve`). **CR-8-k**, pinned by `auth/admin-session.revoke.int.test.ts::K-SEC-A1 — revoking every session of an admin > reaches a session that has been kept alive past four TTLs` (`it.fails`; the young-session case passes) |
| K-SEC-A2 | Login throttle | pass | Per **account**, not per IP — every request arrives through one proxy, so an IP bucket is either useless or a self-inflicted outage. Five wrong passwords park the account 15 min; a good password clears it. `auth.int.test.ts::the per-account login throttle > parks the account after the fifth wrong password`, `> is keyed by account, so one account cannot lock another out`, `> counts concurrent attempts atomically — five wrong passwords is five` |
| K-SEC-A3 | Account enumeration | pass | A wrong account and a wrong password return the same error; a disabled account is only revealed *after* the password verifies. `auth.int.test.ts::admin login > gives the same error for a wrong password and an unknown account`, `> refuses a disabled account, but only once the password is right` |
| K-SEC-A4 | The login itself is not audited | decision **plus a defect** (K2) | Not auditing the request body stays right: `handle.int.test.ts::the admin auth routes > does NOT audit the login itself — the body holds the password`. The gap is now a CR: a failed login and a lockout leave no row either, only a log line, and the Redis lockout counter expires — "who tried to get in, from where, and did they then succeed" has no answer. **CR-12-k2**, pinned by `auth/admin-login.trail.int.test.ts::K-SEC-A4 — what a password-guessing run leaves behind > leaves a readable trail of the failed attempts, without the password` (`it.fails`; `> parks the account after five misses` passes). Accepted cost, recorded: the per-account lockout lets anyone park a known account for 15 min — K-SEC-A2's reasoning (an IP bucket behind one proxy is worse) still holds |
| K-SEC-A5 | Session tokens at rest | pass | 40-byte random token, stored as `sha256Hex(token)`; a Redis dump is not replayable. `auth.int.test.ts::admin sessions > stores only the hash of the token`, storefront: `> stores sha256(token) and never the token` |
| K-SEC-A6 | Reading the payment configuration requires the write atom | decision | `writePermissionFor` only derives `:write` from a `:read` atom, so `payment` and `wechat` (both `payment:config:write`) collapse read and write. Deliberate — the group holds a merchant private key — and documented at `system.test.ts:114`. Consequence worth knowing: **there is no read-only role for payment settings** |
| K-SEC-A7 | Password storage | pass | bcrypt (cost 10, 4 in tests); legacy md5 rows verify once in constant time and are rewritten as bcrypt on that login; a password over 72 bytes is refused rather than silently truncated. `auth.int.test.ts::admin login > upgrades a legacy md5 hash to bcrypt on the first good login`; `password.ts:66-85` |
| K-SEC-A8 | CSRF on cookie-authenticated mutations | pass | `SameSite=Lax` plus a second lock that does not depend on the browser: `Sec-Fetch-Site` same-origin/same-site, or `Origin` on the allow-list; neither header present is a refusal. `handle.ts:152-178`, `handle.int.test.ts` (`AUTH_CROSS_SITE_BLOCKED`) |
| K-SEC-A9 | Every admin write is audited with a target | pass, 5 exemptions (K2) | `pnpm guards`' `route-hygiene` check: 133 admin write URLs, 5 exempt, each named and compared exactly — login (K-SEC-A4), `POST /catalog/sku-matrix` (a POST-shaped read), the two notification-inbox routes (a decision), `POST /attachments/scan-tokens` (**CR-5-k**, open; `pending(K)` as **CR-30-k2**). **CR-17-k is closed** — E4 gave all 21 notification / WeChat-OA writes a target. The **staff** surface is a different gap: its `ctx.audit` calls are no-ops (K-SEC-ST1, **CR-13-k2**) |
| K-SEC-A10 | Permission atoms are real | pass (counts re-run in K2) | `pnpm guards`' `permissions` check: 116 declared atoms, 116 used by 258 admin routes and 55 menu entries, no route or menu entry naming an atom nobody declares, no atom nobody uses |
| K-SEC-A11 | Storefront sessions | pass (K2) | E1's routes use the tested service. Password change, disable, cancellation and logout all reach a live token: `user/storefront-auth.int.test.ts::sessions > kills every session when the password changes`, `> rejects a live token the moment the account is disabled`, `> rejects a live token once a cancellation is approved`, `> logs one device out without touching the others`, `> logs every device out on request, including the caller`; `auth/auth.int.test.ts::storefront sessions > REJECTS a session whose passwordVersion is stale, and revokes it on sight`, `> revokeAllForUser logs every device out at once` |

## Payment

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-P1 | Every inbound callback is verified, before anything is written | pass | Two webhook routes exist in the whole app, both call the one verifier first, inside a try/catch that answers 401 before `ctx.withTx`. `payment.service.ts:645-651`, `refund.service.ts:870-876`. `payment.int.test.ts::PAY-003 … > answers 401 for a signature it cannot verify, before touching the database` (asserts the callbacks table is empty), `refund.concurrency.int.test.ts::REFUND-003 … > refuses a notification whose signature does not verify, before touching the database` |
| K-SEC-P2 | The verifier fails closed | pass | Header presence → 300 s timestamp skew → **known serial** → RSA-SHA256, and an unknown serial is a refusal rather than a default. `wechat.crypto.ts:101-116`. `wechat.crypto.test.ts::TLS-003 — an unfetchable platform certificate is a refusal, not a default > refuses when the key set is empty` |
| K-SEC-P3 | The signature covers the bytes that were sent | pass | The route reads `await ctx.request.text()` and verifies those bytes; nothing re-serialises an object first. `apps/web/app/api/v1/webhooks/wechat-pay/route.ts:21` |
| K-SEC-P4 | Replay | pass | A unique index, not a memory check: `payment_callbacks_notify_uq (mch_id, provider_notify_id)` with `INSERT … ON CONFLICT DO NOTHING … RETURNING`; a null return ends the handler. Second layer for two ids carrying one transaction: a conditional `UPDATE` plus the `replay` branch. `payment.concurrency.int.test.ts::PAY-007 — duplicate callback delivery > books the money once however many times WeChat delivers the same notification`, `> is the callback insert that decides, not a prior read — the repo statement alone` (8 racers, 1 winner), `> marks the order paid exactly once — the conditional update alone` |
| K-SEC-P5 | A paid amount that disagrees is never booked | pass | Compared against the attempt's frozen amount with strict inequality, so short **and** over are refused into a payment exception; unparseable, absent and zero are acknowledged and parked (a 500 would ask WeChat to redeliver the same bytes forever). `payment.service.ts:436`, `:751-753`. `payment.int.test.ts::GATEWAY-001 — an amount that disagrees is never booked > refuses a <short/over> amount, however well signed it is`, `> parks an <unparseable/absent/zero> amount on the callbacks table instead of looping` |
| K-SEC-P6 | The endpoint does not act on an event meant for the other one | **defect** (K2) | Nothing is *booked* from a misrouted event — `refund/refund.webhook.int.test.ts::K-SEC-P6 — a signed event delivered to the other webhook > books nothing when a refund notification reaches the payment endpoint`, `> books nothing when a payment notification reaches the refund endpoint` — but both handlers insert the shared `payment_callbacks` row (unique `(mch_id, provider_notify_id)`) before deciding the event is not theirs, so a misrouted copy burns the notify id and the genuine delivery is then dropped as a replay: the order stays `pending_payment` until reconciliation. **CR-3-k2**, pinned by `> lets the genuine delivery book the payment after a misrouted copy of it (CR-3-k2)` (`it.fails`) |
| K-SEC-P7 | A callback for another merchant | **defect** (K2) | The payment handler logs the `mchid` mismatch and settles anyway (`payment.service.ts:754-758`); the refund handler never reads it (`refund.service.ts:905`); an absent `mchid` defaults to ours. Holds only while no key material is shared between merchants (服务商 setups, one APIv3 key across two merchant ids, a leaked key). **CR-4-k2**, pinned by `refund/refund.webhook.int.test.ts::K-SEC-P7 — a well-signed notification naming another merchant > never marks an order paid on a transaction another merchant collected (CR-4-k2)`, `> never settles a refund on a notification from another merchant (CR-4-k2)` (both `it.fails`); `> records the merchant it was told on the callback row, so the mismatch is at least visible` passes |
| K-SEC-P8 | At most one open payment attempt per order | pass | Partial unique `payment_attempts_open_uq` on `order_id WHERE status in ('creating','submitted','closing','unknown')`. `payment.concurrency.int.test.ts::PAYC-004 … > inserts at most one open attempt — the unique index alone` |
| K-SEC-P9 | One shopper cannot see another's payment | pass | `payment.service.ts:135`, `:358` compare `userId`. `payment.int.test.ts::CLIENT-001 — what the cashier is told > never shows one shopper another shopper's payment` |
| K-SEC-P10 | TLS verification cannot be turned off from the admin | pass | No group this stream owns can grow a verification switch; the trust store is the container's. `payment.config.test.ts::TLS-001 — no TLS toggle, in any group this stream owns > …` (the cell's ids are mangled in `invariants.md` — **CR-2-k** §4) |

## Refund

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-R1 | One line, one open after-sale | pass | Partial unique `refund_items_open_uq ON refund_items (order_item_id) WHERE is_open`, taken under the order's `FOR UPDATE`, with the violation renamed to `REFUND_ALREADY_OPEN`. `refund.concurrency.int.test.ts::REFUND-001 … > lets exactly one request hold the line` |
| K-SEC-R2 | A refund can never exceed what was paid | pass, four layers | apply-time ceiling counting other open requests (`REFUND_EXCEEDS_PAID`); a settle-time conditional `UPDATE` carrying the invariant in its `WHERE`; the CHECK `orders_refunded_within_paid`; the per-refund CHECKs. `refund.concurrency.int.test.ts::REFUND-001 … > never lets two requests together ask for more than was paid`, `refund.int.test.ts::REFUND-005 … > never gives back more than the order was paid, across several requests`, `refund.rules.test.ts::remainingCeiling …` |
| K-SEC-R3 | Two operators pressing 同意 send money once | pass, three gates | a conditional `applied → approved` under `lockRefund`; one effect-ledger row keyed `(refund, id, refund.execute)`; `executeRefund` claiming `→ processing` before leaving the transaction, with `out_refund_no` frozen so WeChat deduplicates a re-send. `refund.concurrency.int.test.ts::REFUND-002 … > queues one gateway call however many times the operator presses 同意`, `> moves 'applied' exactly once — the conditional update alone`, `REFUND-006 … > sends one refund however many effect dispatchers run` |
| K-SEC-R4 | A retry cannot change the amount | pass | The frozen `requestContext` is re-checked before every send; a row edited afterwards throws `REFUND_AMOUNT_MISMATCH`. `refund.int.test.ts::REFUND-005 … > refuses a retry that asks for a different amount than the one frozen` |
| K-SEC-R5 | A stranger cannot read or act on another shopper's after-sale | pass (K2) | CR-3-k's test landed (`9c849d8b4`; the CR file's status line still reads OPEN): `refund/refund.isolation.int.test.ts::AUTH-005 — a stranger and another shopper’s after-sale > answers a stranger’s read with exactly what an unknown id answers`, `> refuses a stranger who tries to withdraw, with the same error`, `> writes nothing when a stranger tries to act on it`, `> refuses a stranger who applies for a refund against somebody else’s order`, and `AUTH-005 — the admin refund route and an unauthenticated caller > …` (7 its) |
| K-SEC-R6 | The refund notification's own amount is not read | **defect** (K2) | Settlement books the frozen amount, never the notified one — asserted: `refund/refund.webhook.int.test.ts::K-SEC-R6 — the amount inside a refund notification > can never change the amount booked: settlement uses the frozen amount`. But a gateway that refunded a *different* amount is booked silently as the asked-for one; the payment side refuses a disagreement (K-SEC-P5), the refund side does not. **CR-5-k2**, pinned by `> does not book a refund whose notified amount disagrees with the frozen one (CR-5-k2)` (`it.fails`) |
| K-SEC-R7 | The admin refund atoms | tested at both boundaries for the unauthenticated / atom-less case (K2) | `refund/refund.isolation.int.test.ts::AUTH-005 — the admin refund route and an unauthenticated caller > refuses an admin account that holds no refund atom, before the row moves`, `> completes the after-sale for the admin who does hold the atom`, plus K's `e2e/admin/specs/restricted-role.spec.ts`. The per-atom domain assertions **CR-14-k** asks for are still open |
| K-SEC-R8 | `/api/v1/staff/refunds*` | **defect**, still open (K2) | `registerStaffRefundPort` still forwards into `adminApprove` / `adminReject`, which `requirePermission` an admin atom, so every staff review answers 403. Fails closed. **CR-14-k** |
| K-SEC-R9 | The 售后设置 group's write atom | **defect**, still open (K2) | `refundConfig.permission` is still `refund:request:write` (备注售后单). **CR-10-k**, now pinned: `system/config.k2.int.test.ts::K-SEC-R9 — the 售后设置 group and the 备注 atom > does not let the remark permission rewrite the return address` (`it.fails`) |
| K-SEC-R10 | `detail()` has no ownership check | **defect**, latent (K2) | Still exported from `refund/index.ts:41`; nothing outside `refund/` imports it (the storefront calls `myDetail`, the console `adminDetail`), so it is not a live leak — but it is the function a new route would reach for. **CR-6-k2**: drop the export. The public entry points stay asserted by K-SEC-R5's tests |

## Upload

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-U1 | The accepted type is decided by the bytes | pass | `sniffFileType` reads magic numbers; the declared mime only distinguishes the two OOXML formats inside a ZIP container, and a bare ZIP is refused. SVG, XML, HTML, PHP, shebangs, ELF, PE and Mach-O are refused before the allow-list is consulted. A declared type that disagrees with the bytes is an error, not a silent correction. `file-type.test.ts::sniffFileType — the files that must never get in > refuses SVG — always, script or not`, `> refuses HTML, which would run as our own origin`, `> refuses a PHP script however it is named or declared`, `> refuses executables`; `storage.int.test.ts::/admin-api/attachments > refuses what the old uploader accepted > refuses <each of five>`; `/api/v1/uploads > refuses an executable from a shopper too` |
| K-SEC-U2 | SSRF on "import from URL" | pass as a design | Scheme and port allow-list, credentials refused, literal IPs judged before DNS, **every** resolved address judged (v4 and v6, including unwrapping `::ffff:`), loopback-by-design hostnames refused, every redirect hop re-judged with `redirect: 'manual'`, a `Content-Length` pre-check **and** a streamed hard stop, and one whole-operation timeout. Refusals collapse to two opaque codes so the endpoint cannot map the network. `safe-fetch.test.ts::safeFetch — refusals > refuses a public name that RESOLVES to a private address`, `> refuses a name that resolves to one public AND one private address`, `> refuses a redirect into the private network`, `> stops reading at maxBytes even when Content-Length lied`; route: `storage.int.test.ts::refuses to import from a private address, after resolving the name` |
| K-SEC-U3 | …but the transport is wrong | **defect**, still open (K2) | The connection is still made to the judged IP by rewriting `url.hostname` (`safe-fetch.ts:247`), so every `https://` fetch fails the TLS handshake — now including F4's base64 of our own `https://` origin (K-SEC-F1). **CR-11-k** |
| K-SEC-U4 | The storage key is never the client's | pass | `<dir>/<YYYY>/<MM>/<uuid>.<ext>`, directory hint sanitised to `[a-z0-9-]{1,32}`, extension from the sniffer; no contract has a `key` or `path` field; the local driver additionally refuses `\0`, leading `/` and `..` and re-checks containment. `s3.test.ts::createS3Storage > generates the key itself — a caller cannot choose a path` |
| K-SEC-U5 | Component-test fixtures are not checked against the contract | **defect (class)** | A stubbed `fetch` returns a hand-written object that TypeScript never compares to the contract, so a fixture can be missing a field until a component reads it — which is why `customers.test.tsx` crashed only under load and was fixed by hand in `8a03f8cb`. **CR-7-k** |
| K-SEC-U6 | The scan token | pass | Per-admin, single-use through an atomic Lua CAS, 600 s TTL, another admin's token reads as *expired* rather than as someone else's, and a rejected file releases the claim instead of burning the code. `storage.concurrency.int.test.ts::scan tokens are single-use > lets exactly one of six phones upload through one QR code`; `storage.int.test.ts::scan-to-upload > reads another admin's token as expired rather than as somebody else's`, `> does not burn the token when the file is refused` |
| K-SEC-U7 | …with two rough edges | **defect**, still open (K2) | The public scan-upload route is still unthrottled (the only `fixedWindow` in `storage.service.ts` is `userUpload`'s, `:684`) and `COMPLETE_LUA` still checks `EXISTS` (`scan-token.ts:72`). **CR-12-k** |
| K-SEC-U8 | The token at rest | decision, re-affirmed (K2) | The token is the Redis key (`storage:scan:<token>`), not hashed. Re-read: 120 bits, 600 s, single-use through the Lua CAS, bound to one admin, and what it grants is **one upload into that admin's library** — never a session, never a read. Someone with a Redis dump already holds every session's hash and the queue; a raw 10-minute upload code adds nothing they could not do more directly. Hashing it would cost a second key per token for no change in who can do what. Kept |
| K-SEC-U9 | Uploads are served from the app's own origin | partly addressed (K2) | J2's edge now sends `X-Content-Type-Options: nosniff` and `Content-Security-Policy: default-src 'none'; sandbox` on `/uploads/` (`docker/edge/nginx.conf:137-138`), refuses script extensions there, and defaults unknown types to `application/octet-stream` — **CR-13-k** items 1 and 2. Still open: item 3 (`Content-Disposition` is `inline` for everything, not `attachment` for non-media — the sandbox makes that low-risk) and item 4 (separate upload host, undecided) |
| K-SEC-U10 | A user cannot choose the served content type | pass | The stored `Content-Type` is the **sniffed** mime, not the declared one (`storage.service.ts:513-517` → `s3.ts:224-229`); no route serves object bytes and `Content-Disposition` appears nowhere |
| K-SEC-U11 | Secrets never leave through a response | pass | A secret config field travels as an "is set" boolean; an empty value on save is skipped so a partial form cannot blank a credential. Asserted from both ends: `system.int.test.ts::never returns a stored secret — only whether one is set` (the value is in the DB row and not in the JSON), `system.test.ts::marks every credential in every registered group as secret`, and `pnpm guards`' `secrets` check, which walks 8465 response-schema nodes across 397 contracts |
| K-SEC-U12 | …but they do leave through the audit log | **defect**, still open (K2) | `redactPayload` is still shallow (`auth/audit.repo.ts:35-51`). **CR-9-k**, now pinned: `auth/audit.redact.test.ts::K-SEC-U12 — what the operation log keeps of a request body > redacts the same credential one level down, as the config form sends it` (`it.fails`; the top-level case passes) |

## Cross-cutting

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-X1 | Internal errors never reach the client | pass | `handle()` catches everything non-`DomainError` and answers `INTERNAL` with no message, stack or SQL; response-validation failures include details only outside production. `handle.ts:418-428` |
| K-SEC-X2 | The response always matches its contract | pass | `VALIDATE_RESPONSES=1` in CI makes a mismatch a 500 rather than a leak. `.github/workflows/next.yml` |
| K-SEC-X3 | A retired feature cannot come back | pass | `pnpm guards`' `retired` check: 26 features as identifiers and URL tokens over 1122 source files and 397 route paths, with per-word deny-lists for the files that name them in order to refuse them. `guards/src/checks/retired.test.ts::the retired blacklist > finds no retired identifier in next/ or the uni-app API layer` |
| K-SEC-X4 | Only the contract's URLs are reachable | pass | `pnpm guards`' `contracts` check, both directions, plus `admin-client`: no hand-built `/admin-api/…` string and no raw `fetch()` outside the api seam (one documented exception, the SSE stream). `guards/src/checks/contracts.test.ts::contracts and route files > …` |
| K-SEC-X5 | The ambient clock cannot come back into core | pass | `guards/src/checks/banned.ts` imports `@shop/config/eslint` and asserts `no-restricted-properties` on `Date.now`, the zero-arg `new Date()` selector, `no-eval` and `no-new-func` are all still `error` — lint is configuration and can be turned off in one line |
| K-SEC-X6 | Domains are really installed in the process that runs them | fixed (K2) | **CR-1-k resolved**: the generator emits a bare side-effect import per domain. `pnpm guards`' `domains` check: 21 declared domains, 21 imported by name |
| K-SEC-X7 | The in-memory fakes accept only what the real adapter accepts | fixed for the case found (K2) | **CR-15-k resolved** in the adapter (`toJobId` maps `:` → `__`): `kernel/queue-bullmq.int.test.ts::createBullQueue > enqueues a job whose dedupeKey carries a colon (CR-15-k)`, on a real Redis. The class — fakes that accept more than the real adapter — is not closed by it |
| K-SEC-X8 | The admin UI refuses what the API refuses | gap, still open (K2) | Four pages now wrap themselves (F3's `stats/*` pages use `RequirePermission`); every other page still renders the shell and failed fetches. Server boundary unchanged and holding. **CR-16-k** |

## Second pass — surfaces merged after the first

Read in K2 as an attacker would: what can an anonymous caller, a shopper, a
店员 and a narrow console role each make these routes do. Ids: **S** SMS and
storefront sign-in, **O** WeChat OA, **N** notifications, **E** E4's routes,
**V** the visit beacon, **M** 小程序码, **F** F4, **B** B3, **ST** the staff
console, **J** readiness, **C** configuration. Test paths are relative to
`next/packages/core/src/` unless they start with `apps/web/`.

| id | Item | Verdict | Evidence / CR |
| --- | --- | --- | --- |
| K-SEC-S1 | SMS code replay (PLAN §5 item 8) | pass | See §5 item 8 above |
| K-SEC-S2 | SMS send budgets | pass per phone, **defect** per IP | Resend window per scene, per-phone hour and day budgets, and a provider refusal leaves no usable code: `user/storefront-auth.int.test.ts::sendSmsCode > refuses a second code inside the resend window and allows one after it`, `> keeps a separate resend window per scene`, `> enforces the per-phone hourly budget`, `> does not leave a usable code behind when the provider refuses`; `user/user.concurrency.int.test.ts::SMS codes > lets one of six sends through the resend guard`. The per-IP day budget works as coded (`> enforces the per-IP daily budget across different numbers`) but keys on the first `X-Forwarded-For` entry, which the client writes — K-SEC-X9, **CR-14-k2** |
| K-SEC-S3 | Account enumeration through sign-in | pass, one decision | `sendSmsCode` answers a registered and an unregistered number alike: `user/storefront-auth.int.test.ts::sendSmsCode > answers identically for a registered and an unregistered number`. `resetPassword` answers `USER_NOT_FOUND` — but only after the code for that number has been verified and consumed, i.e. only to someone who holds the phone. Decision: not an oracle, kept |
| K-SEC-O1 | OA callback authenticity | **defect** | The 明文 signature does not cover the body; the endpoint has no timestamp window, no single-use of the triple, and takes the mode from the request's `encrypt_type` instead of the configured `messageMode`. The baseline holds — `wechat-oa/wechat-oa.webhook-forgery.int.test.ts::K-SEC-O1 — what a valid signature triple is good for > holds in 安全模式 for a genuine encrypted delivery (the baseline)` — and three `it.fails` pin the rest: `> refuses a plaintext callback when the account is configured in 安全模式`, `> refuses a triple whose timestamp is outside a five-minute window`, `> refuses a second, different body under a triple that was already used`. **CR-7-k2** |
| K-SEC-O2 | The OA admin routes' atoms | pass | All 28 handlers in the 17 admin route files (菜单, 自动回复, 素材, 渠道二维码; the public webhook is K-SEC-O1, the storefront JS-SDK / subscribe-template pair K-SEC-O6) name a `wechat-oa:{menu,reply,media,qrcode}:{read,write}` or `wechat-oa:menu:publish` atom, enforced by `handle()` and re-checked in the services; `pnpm guards`' `permissions` check (K-SEC-A10) |
| K-SEC-O3 | …and their audit targets | pass | **CR-17-k closed** by E4; none of the five remaining `route-hygiene` exemptions is an OA route (K-SEC-A9) |
| K-SEC-O4 | Outbound calls and SSRF-ish inputs | pass, with **CR-9-k2** and one decision | Every OA call goes to the configured `apiBaseUrl` through the shared token client; no route takes a URL. The base URL itself is writable from the settings form — K-SEC-C2. Media upload sends any attachment id the admin names to WeChat's 永久素材 API; decision: an admin with `wechat-oa:media:write` choosing which of the shop's own files to publish is the feature, not a leak (the call path itself is **CR-31-k2**, filed by K2 §6) |
| K-SEC-O5 | Secrets in OA responses | pass for the AES key, **defect** for the token | `encodingAesKey` is secret: `system/system.int.test.ts::config > never returns a stored secret — only whether one is set`. The callback **token** is `type: 'text'` and returned in clear to `system:config:read` — K-SEC-C1, **CR-8-k2** |
| K-SEC-O6 | `GET /api/v1/wechat/jssdk-config` | pass | Signs only a page on our own host or a listed 授权域名, compares the host rather than a prefix, and refuses other schemes: `wechat-oa/wechat-oa.int.test.ts::the JS-SDK signature > refuses to sign somebody else’s page`, `wechat-oa/wechat-oa.storefront.test.ts::assertAllowed > compares the host, not a prefix`, `> refuses a scheme that is not http(s), without asking anybody`, `> does not turn a misconfigured or missing origin into “allow everything”` |
| K-SEC-N1 | Inbox isolation (站内信 and the bell) | pass | Another person's message answers `NOTIFICATION_MESSAGE_NOT_FOUND`, exactly as a missing id, on read, mark-read and delete, and is left untouched: `notification/notification.isolation.int.test.ts::K-SEC-N1 — another person’s message > answers a shopper’s id to another shopper as not found, on every call`, `> answers one admin’s bell message to another admin as not found`, `> never serves an admin-audience row to a shopper whose id happens to match` |
| K-SEC-N2 | Templates, send records and 重试 | pass | Four atoms (`notification:template:read/write`, `notification:log:read/handle`) on the six template/log routes; 重试 requeues only a record whose outcome is `unknown`: `notification/notification.isolation.int.test.ts::K-SEC-N2 — 重试 on a send record > will not send a delivered notification a second time`, `> requeues only a record whose outcome is unknown`, `> needs the handle atom, not the read one`. SMS codes never reach `sms_logs` (`sms.service.ts:156`, and the schema comment) |
| K-SEC-N3 | The SSE bell stream | **defect** | Bare handler: resolves the cookie once and streams until disconnect, so a password change does not end a stolen session's stream; one `redis.duplicate()` per stream with no cap. **CR-15-k2** (route-level, no test) |
| K-SEC-E1 | `POST /api/v1/auth/phone/wechat-mini` | pass | The number is the one WeChat answers for the code, never the request's; a spent code, an already-held number and a second binding are refused: `user/storefront-auth.mini.int.test.ts::bindPhoneFromMini > binds the number WeChat answered with, which the request never carried`, `> refuses a spent or forged code and leaves the account without a number`, `> refuses a number another account already holds`, `> refuses a second binding without spending the code`, `> refuses while the mini program is switched off`; `apps/web/app/api/v1/user.int.test.ts::POST /api/v1/auth/phone/wechat-mini > 401s without a token, 422s an empty code, and 409s an account that has a number` |
| K-SEC-E2 | The six `/api/v1/staff/users*` | pass | Roster-gated, phone always masked, search matches a whole number only: `apps/web/app/api/v1/user.int.test.ts::/api/v1/staff/users > 403s an ordinary shopper on every one of the six`, `> 401s without any session at all`, `> lets a 店员 through all six and never unmasks a phone`, `> 422s a uid that is not an id, before touching the database`. The default nickname (用户 + last four digits) shows nothing the mask does not. Group and label writes leave no audit row — K-SEC-ST1 |
| K-SEC-M1 | `GET /api/v1/wechat/mini-qrcodes` | **defect** | Cached pairs are free; each **new** scene costs a WeChat call, a stored PNG and a row, with no per-account budget. **CR-11-k2**, pinned by `wechat/wechat.mini-code.budget.int.test.ts::K-SEC-M1 — how many new codes one shopper can mint > stops minting new codes for one account well before a hundred in an hour` (`it.fails`) |
| K-SEC-V1 | `POST /api/v1/visits` | **defect** | Throttle keyed per path, and the path is the caller's, so a loop writes one row per request: `user/user.visit.budget.int.test.ts::K-SEC-V1 — what one anonymous address can write > writes one row per path per minute, as designed` passes, `> cannot write more than a browsing session’s worth of rows in a minute` is `it.fails`. **CR-11-k2**. 访客数 also counts the spoofable address — K-SEC-X9 |
| K-SEC-F1 | `POST /api/v1/attachments/base64` | pass, one decision, one inherited defect | Own origin, trusted hosts and the object-storage prefix only; `safeFetch` with 2 MB / 5 s; bytes sniffed, raster images only; 60 an hour per shopper: `system/attachment.int.test.ts::图片转 base64 (CR-7-h2) > refuses a foreign host without fetching anything at all`, `> refuses a host of ours that resolves to a private address`, `> refuses a public answer that comes with a private one`, `> refuses a body over the 2 MB cap`, `> refuses bytes that are not an image, whatever the URL says`, `> refuses an SVG, which is a document rather than an image`, `> refuses every scheme that is not http(s), and credentials in the URL`, `> refuses a path when nobody told this deployment its own origin`, `> takes an object-storage URL only under the configured prefix`, `> is rate limited per shopper`, `> needs a storefront session`. Decision: a redirect from one of our hosts to another *public* host is followed (every hop is still judged private-or-not, the result must still sniff as an image ≤ 2 MB) — acceptable because no route on our origins redirects to a caller-chosen URL (`grep` finds no `redirect(` in `apps/web`); revisit if one is added. Inherited: every `https://` URL fails in production until **CR-11-k** |
| K-SEC-F2 | `GET /api/v1/site/config` (public) | pass, one decision | Built field by field from three groups; every `secret: true` field in every registered group is seeded with a unique marker and none appears: `system/system.int.test.ts::站点公开配置 (CR-7-h2) > cannot leak any secret in any registered group`. Decision: `support.phone` is the 客服 number the operator typed for customers to call — public by intent, as is the ICP filing |
| K-SEC-B1 | `GET /api/v1/orders/:id/gift-coupons` | pass | `requireOrderRef` owner check: `apps/web/app/api/v1/coupons.staff.int.test.ts::GET /api/v1/orders/:id/gift-coupons > gives a stranger the same 404 as an unknown order`, `> 401s without a session` |
| K-SEC-B3 | `POST /api/v1/staff/coupon-grants` | **defect** | Roster gate holds (`apps/web/app/api/v1/coupons.staff.int.test.ts::POST /api/v1/staff/coupon-grants > 403s for a signed-in shopper who is not on the roster`), per-user limit and supply hold (`> creates exactly one row, and refuses the second grant the way the admin grant does`). But a 店员 can grant a `draft` or `disabled` template and grant to themselves: `coupon/coupon.staff-grant.int.test.ts::K-SEC-B3 — what a 店员 can grant > refuses a template marketing has not released, or has withdrawn`, `> refuses to grant to the 店员’s own account` (both `it.fails`; `> grants an active template to a customer, once` passes). **CR-10-k2** |
| K-SEC-ST1 | Staff writes are not audited | **defect** | `handle()` audits only `surface === 'admin' && actor.kind === 'admin'` (`handle.ts:414`); the 14 staff write route files' `ctx.audit` calls are no-ops, and A2 pinned the empty table as a decision (`apps/web/app/api/v1/staff/products/catalog-staff.int.test.ts:299-304`) on the grounds that the phone's trail is the order/product history — which exists for orders only. **CR-13-k2** (route-level, no test) |
| K-SEC-ST2 | A2's ten `/api/v1/staff/products*` | pass, one decision | Roster-gated on every method, writes nothing when refused: `apps/web/app/api/v1/staff/products/catalog-staff.int.test.ts::GET /api/v1/staff/products > 403s a shopper who is not on the staff list`, `POST /api/v1/staff/products > 403s a shopper who is not on the staff list, and writes nothing`, `POST /api/v1/staff/products/:id/visibility > 403s a shopper who is not on the staff list, and leaves the product up`, `/api/v1/staff/products/:id/skus > 403s a shopper who is not on the staff list, on both methods`, and the label / category / picker routes likewise; a SKU of another product is `CATALOG_SKU_NOT_FOUND` (`catalog/catalog.staff.int.test.ts::the staff SKU editor > refuses a SKU that belongs to another product`). Decision: a 店员 may reprice and restock — legacy's 移动端商品管理 did, and B2's order repricing is the same trust. The missing trail for it is K-SEC-ST1 |
| K-SEC-J1 | `/api/v1/readyz` (public) and its 503 | pass, one decision | The body names each dependency with the bare word `failed` — no host, error text or connection string: `apps/web/src/server/health.int.test.ts::GET /api/v1/readyz > is 503 naming only the worker when the heartbeat stops` (exact `toEqual` on the body), `> is 503 when the database was created but never migrated`, `> logs its 503 at info, not error (CR-1-j3)`. Decision: telling an anonymous caller *which* dependency is down, and `APP_VERSION`, is accepted for a release gate that must work without credentials; each call costs two trivial DB queries and two Redis reads |
| K-SEC-C1 | The OA callback token on the settings screen | **defect** | `configGet('wechat-oa')` returns `token` in clear to `system:config:read`; the registry heuristic misses `token`. `system/config.k2.int.test.ts::K-SEC-C1 — the OA callback token on the settings screen > never returns the webhook token to a read-only settings role` (`it.fails`). **CR-8-k2** |
| K-SEC-C2 | Schema keys the form never shows | **defect** | `configSave` accepts any schema key; `wechat.apiBaseUrl` repoints the token client, which then sends the stored AppSecret to that host. `system/config.k2.int.test.ts::K-SEC-C2 — a schema key the form never shows > refuses to repoint the WeChat client from the settings form`, `> refuses to repoint the WeChat Pay client from the settings form` (both `it.fails`). **CR-9-k2** |
| K-SEC-X9 | Which address is the client's | **defect** | The edge appends to the client's own `X-Forwarded-For` (`$proxy_add_x_forwarded_for`) and `clientIp()` takes the first entry, so the per-IP SMS budget, 访客数, `last_login_ip` and `audit_logs.ip` (the raw header) are the caller's choice unless the unversioned Traefik in front rewrites the header. `apps/web/app/api/v1/user.int.test.ts::POST /api/v1/auth/sms-codes > counts the per-IP budget against the address the proxy forwarded` asserts today's first-entry rule. **CR-14-k2** (edge + route, no core test) |

## What this pass did not cover

Updated by K2. Everything the first pass deferred has now been read; what is
left is below.

- **Load and denial of service** (`K-hardening.md` §4) — K2's `next/load/`
  sub-unit, not this audit. Two findings here feed it: the edge has **no
  `limit_req`** anywhere, and CR-11-k2 / CR-15-k2 are per-caller ceilings that
  do not exist.
- **Group-buy and presale** (D, D2) and **CMS / shipping / statistics** (F2,
  F3) — read only where they cross a surface above (the staff console,
  `site/config`). Their own storefront and admin routes have had no attacker
  read; the guards (atoms, audit targets, contracts, secrets) cover them
  mechanically.
- **The production host.** Traefik's forwarded-header trust (CR-14-k2), TLS
  termination and the container runtime are on a server this audit may not
  touch; CR-14-k2's "until then" names what to confirm there.
- **H3 and I** were in flight: the uni-app's own handling of tokens and the
  storefront e2e suite are theirs.
- **Route-level assertions** for CR-13-k2, CR-14-k2 and CR-15-k2 are asked for
  in the CRs; K2 §1 may only commit tests under `packages/core`.
