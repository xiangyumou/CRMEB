# Stream J3 — Deploy follow-up: `/readyz`, CI image + rehearsal jobs, OPS/REL invariant rows, cutover runbook draft

**Worktree** `../CRMEB-wt/ws-j3` · **Branch** `rewrite/ws-j3-deploy-followup` (from `rewrite/integration`) · **Owns** `deploy/next/**`, `next/docker/**`, `contracts/src/health/**`, `apps/web/app/api/v1/readyz/**`, `apps/web/src/server/health*.ts`, `tests/deployment/**`, `docs/rewrite/status/j3.md`. **Touches by decision of the orchestrator**: `.github/workflows/next.yml` (CR-3-j2 — the YAML is approved as proposed, adjust only what does not run), `tests/static/release-pipeline-guard.cjs` (CR-3-j2 §5), `docs/rewrite/invariants.md` **only the three J-owned sections** (CR-2-j2 — replace the three tables verbatim, touch no other line and do not let prettier reflow the rest of the file), `packages/etl/src/**` only for the rehearsal harness.

Read first: `docs/rewrite/CONVENTIONS.md`, `docs/rewrite/status/{j,j2}.md`, `docs/rewrite/cr/CR-1-j2.md`, `CR-2-j2.md`, `CR-3-j2.md`, `deploy/next/cutover.md`, `deploy/next/rehearsal/drill.sh --list`, `scripts/publish-release.sh`, `tests/static/release-pipeline-guard.cjs`, `.github/workflows/next.yml`, `apps/web/src/server/health.ts`.

## 1. CR-1-j2 — `/api/v1/readyz` (accepted as proposed)
The five files exactly as the CR lays them out: `contracts/src/health/errors.ts`, the `readinessPayload` + route in `health.contract.ts`, the readiness service (database ping, Redis ping, migrations applied = the drizzle journal's last id matches what `packages/db` ships, worker heartbeat fresh within its interval), the route handler returning 503 `HEALTH_NOT_READY` with `checks` in `details` and **no error text, host or credential**, and the `location = /readyz` proxy in `next/docker/edge/nginx.conf`. `deploy/next/lib/readiness.sh` switches to the HTTP check where it can (keep the exec-based checks as the fallback when the edge is not up yet). Int test in `apps/web`: ready → 200 with four `ok`; stop the fake worker heartbeat → 503 with `worker: failed` and nothing else in the body.

## 2. CR-3-j2 — `.github/workflows/next.yml` (accepted as proposed)
Widen the triggers, add `shell`, `images`, `rehearsal`, extend the guard (§5). One change to the CR: the release edge image must carry the **real** storefront, so the `images` job builds it first — `npm ci && npm run build:h5` in `template/uni-app` (H2 proved it builds headlessly; see `docs/rewrite/status/h.md` §build) — and the placeholder is only for a checkout where that build fails, which the job must then report loudly (`::warning::`), not silently. The `rehearsal` job runs `deploy/next/rehearsal/drill.sh` against images built in-job; cap it at the timeout the CR names. Run `tests/static/release-pipeline-guard.cjs` and `shellcheck` locally; you cannot run GitHub Actions — validate the YAML with `actionlint` if available offline (`npx --offline` may not have it; say what you could and could not verify).

## 3. CR-2-j2 — the OPS/REL rows (accepted, with the two judgement calls answered)
Replace the three tables verbatim. OPS-007's automatic rollback is accepted with its stated condition; add a static guard in `tests/static/` that fails when a drizzle migration under `packages/db` contains `DROP TABLE` / `DROP COLUMN` / `ALTER COLUMN … TYPE` without a `-- destructive: approved` marker, and cite it from the row. REL-006/007 stay `pending` until §2's guard extension lands in the same branch — then mark them `ported`. K1/K2's invariant build must see zero `unmapped` rows in the three sections.

## 4. ETL rehearsal harness (no production data)
The rehearsal against a production dump copy needs the user's go-ahead (data egress) and is not yours to run. Build the harness so it is a one-command run once a dump is present: `deploy/next/rehearsal/etl-drill.sh <mysql-dump.sql.gz>` starts MySQL 8 + PostgreSQL 17 in containers, loads the dump, runs `etl migrate` + `etl verify`, then fetches through HTTP one product, one DIY page and one attachment from a `web` container started against the result. Prove it with the synthetic fixture dump `packages/etl` already uses in its tests (extend that fixture if it lacks a DIY page or an attachment). Never add a real dump, hostname or credential to the repo.

## 5. `deploy/next/cutover.md`
Bring it to a runbook the user could follow step by step: prerequisites (images published, `deployment.env` filled, DNS/Traefik labels), the maintenance window sequence from PLAN §6 (backup MySQL + uploads → compose up postgres/redis → migrate → ETL → verify → web/worker/edge → smoke: `/readyz`, admin login, storefront order with the fake gateway → switch Traefik labels), rollback (labels back, old stack stopped-not-removed), and the retirement PR list. Every command copy-pasteable, every check with its expected output.

## Out of scope
Anything in `core/` beyond the readiness service, the uni-app, running anything against the production host (never SSH).

## Rules
Never push, never SSH, never touch `crmeb/`, `template/**` (except reading the build scripts), or another stream's worktree; the lockfile may be committed **only** if the readiness route needs a dependency (it should not). Commit after every unit with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Before the final commit, from `next/`: `pnpm turbo run gen typecheck lint test:unit build`, `pnpm --filter @shop/web test:int`, `pnpm --filter @shop/core test:int`, `pnpm exec prettier --check .`, `pnpm --filter @shop/contracts check:examples`; from the repo root `node tests/static/release-pipeline-guard.cjs`, `shellcheck --source-path=SCRIPTDIR -x deploy/next/*.sh deploy/next/lib/*.sh deploy/next/rehearsal/*.sh`, and `deploy/next/rehearsal/drill.sh` end to end if Docker is available locally (report if it is not). Write `docs/rewrite/status/j3.md`. Final report: what landed, what was verified locally vs. only lint-checked, the invariant rows' states, anything left.
