# Stream B3 — Order, cart, coupon & marketing follow-up: order ports, order config, gift coupons, staff coupons, group-buy summary, cart event

**Worktree** `../CRMEB-wt/ws-b3` · **Branch** `rewrite/ws-b3-order-followup` (from `rewrite/integration`) · **Owns** `contracts/src/{order,cart,coupon,groupbuy,presale}/**`, `core/src/{order,cart,coupon,groupbuy,presale}/**`, `apps/web/app/api/v1/{orders,cart,checkout,coupons,user-coupons,groupbuy,presale,staff/coupons*,staff/coupon-grants}/**`, `apps/web/app/admin-api/{orders,coupons,groupbuy,presale}/**`, `apps/web/app/admin/(shell)/{order,coupon,groupbuy,presale}/**` (pages only where a config screen moves). **Touches by decision of the orchestrator**: `core/src/order/ports.ts` (CR-1-d2, CR-1-f2 — frozen file, these two additions are approved verbatim), `core/src/system/trade.config.ts` (delete — CR-6-f1), `core/src/shipping/shipping.freight.port.ts` (only to drop the SKU re-read once `FreightLine` carries the mode), readers of `tradeConfig` (`grep -rn tradeConfig packages apps`), `packages/etl/src/mappers/system.ts` if a legacy key moves group, `docs/rewrite/status/b3.md`.

Read first: `docs/rewrite/CONVENTIONS.md`, `docs/rewrite/status/{b1,b2,d,d2}.md`, `docs/rewrite/cr/CR-1-d2.md`, `CR-1-f2.md`, `CR-6-f1.md`, `CR-5-h2.md`, `CR-1-h2.md`, `CR-1-f3.md` (§2), then `core/src/order/ports.ts`, `order.checkout.service.ts`, `order.pricing.ts`, `presale/presale.order.ts` (the `inTx` workaround), `groupbuy/groupbuy.order.ts`. Read-only reference: `crmeb/app/services/order/StoreOrderCreateServices.php` (gift coupons on create — `give_coupon_ids`), `crmeb/app/api/controller/v1/admin/StoreOrderController.php`, `crmeb/app/services/activity/combination/StoreCombinationServices.php` (`getCombinationIndex` 人气条), `template/uni-app/api/{order,admin,activity}.js` markers.

## 1. CR-1-d2 — `PricingDraft.adjustments` (accepted)
Add the optional `adjustments?: readonly PriceAdjustment[]` field exactly as the CR proposes; `create` passes `draft.discount.applied` to `beforeCreate`. Then rewrite presale's guard as the three honest lines the CR shows, delete `inTx` and the `{ ...ctx, db: tx }` cast, and give group buy the same check in `beforeCreate` (D's `afterCreate` guard stays; both are cheap). `presale.concurrency.int.test.ts::…holds under a crowd` must still pass with the pool at 12; add the same crowd test for group buy if D has none.

## 2. CR-1-f2 — `FreightLine` carries `freightMode` and `fixedFreightFen` (accepted)
Add both fields to `FreightLine` in `ports.ts`, set them in `freightLineOf` (`order.pricing.ts`), update `@shop/testing` fakes' `FreightLine` construction if any, and delete F2's `getSkusForSale` re-read inside `shipping.freight.port.ts` (it now reads the mode from the line). The order int tests' freight expectations (`16.00` / `126.00`) must not move. Then implement the shop-wide 满额包邮 rule inside the port (`freeShippingThreshold`, see §3): an order whose goods total reaches the threshold ships free, fixed-postage lines included; pin with a test.

## 3. CR-6-f1 — option 1: `trade` folds into `order` (accepted)
`core/src/order/order.config.ts` becomes the single 订单设置 group, sections 自动处理 / 阈值 / 售后 / 移动端订单台 with all of `trade`'s fields **except** `stockWarningThreshold`, which is deleted: `catalogConfig.stockWarningThreshold` (A, wired by N1 to `admin_low_stock`) is the one warning threshold — move its legacy key there if `trade` held it. Delete `trade.config.ts`, repoint every reader (`shipping.freight.port.ts`, B2's auto-receive/auto-review jobs, refund reasons/return address, the staff roster in `auth`), keep every legacy key routed (`etl/src/mappers/system.ts` routes by `legacyKeys`, so only the registry changes; run the ETL config tests). The settings index shows one order screen; the ConfigGroupForm page under `app/admin/(shell)/system/` for `trade` goes.

## 4. CR-5-h2 — gift coupons and the staff coupon console
- `GET /api/v1/orders/:id/gift-coupons`, `auth: 'user'`, owner-only: the coupons granted by placing this order. If B1's create path does not yet grant product-attached coupons (`give_coupon_ids` in the legacy), implement it: in `create`'s transaction, for every line whose product carries gift coupon ids, grant through the coupon domain's existing grant service (idempotent per order — `recordEffect` or a unique on `(order_id, coupon_id)`), and expose them through this route. Test: an order on a product with two gift coupons yields two `user_coupons` rows and the route lists them; a stranger gets 404.
- `GET /api/v1/staff/coupons` (`auth: 'staff'`, the grantable coupons list) and `POST /api/v1/staff/coupon-grants` `{ userId, couponId }` reusing the golden slice's grant service (same race guard). Tests: shopper 403, staff grant creates one row, double grant refused the same way the admin grant is.

## 5. CR-1-h2 — `GET /api/v1/groupbuy/summary`
Public: `{ participants, avatars }` — participants = count of distinct users in successful or in-progress teams over the shop's live activities, avatars = up to 8 most recent. 60 s Redis cache. Test with seeded teams.

## 6. CR-1-f3 §2 — the `cart` product event
In `cart`'s add path, next to the `cart_items` upsert, insert `product_events { productId, skuId, userId, kind: 'cart', quantity }` in the same transaction (copy the `view` writer in catalog). Int test: add-to-cart → F3's product page reports 加购件数 1.

## Out of scope
The uni-app (H3 flips the markers; list every route's fields in your status file), staff product routes (A2), staff user routes (E4).

## Rules
Never push, never SSH, never touch `crmeb/`, `template/**`, or another stream's worktree; lockfile never. `pnpm gen` after adding routes/config changes; contracts first with examples. Commit after every unit with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Before the final commit, from `next/`: `pnpm turbo run gen typecheck lint test:unit build`, `pnpm --filter @shop/core test:int`, `pnpm --filter @shop/web test:int`, `pnpm --filter @shop/etl test`, `pnpm --filter @shop/testing test:int`, `pnpm exec prettier --check .`, `pnpm --filter @shop/contracts check:examples`. Write `docs/rewrite/status/b3.md`. Final report: ports changed, config group shape, routes with fields, anything left.
