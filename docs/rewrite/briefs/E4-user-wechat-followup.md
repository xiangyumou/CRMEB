# Stream E4 — User & WeChat follow-up: captcha decision, mini-program phone binding, staff 用户管理, mini-program codes, visits beacon, config ownership

**Worktree** `../CRMEB-wt/ws-e4` · **Branch** `rewrite/ws-e4-user-wechat-followup` (from `rewrite/integration`) · **Owns** `contracts/src/{auth,user,wechat,wechat-oa}/**`, `core/src/{auth,user,sms,wechat,wechat-oa}/**`, `apps/web/app/api/v1/{auth,me,profile,addresses,wechat,staff/users*,staff/user-*}/**`, `apps/web/app/admin/(shell)/{user,wechat,wechat-oa}/**`, `apps/web/app/admin-api/{users,user-groups,user-labels,wechat,wechat-oa}/**`. **Touches by decision of the orchestrator**: `packages/testing/src/wechat/**` (new — CR-2-e3), `packages/db/src/schema/wechat.ts` + one migration (CR-3-e3), `core/src/system/wechat-mini.config.ts` (config ownership), `packages/etl/src/config.test.ts`, `docs/rewrite/status/e4.md`.

Read first: `docs/rewrite/CONVENTIONS.md`, `docs/rewrite/status/{e1,e2,e3}.md`, `docs/rewrite/cr/CR-2-h2.md`, `CR-6-h2.md`, `CR-2-e3.md`, `CR-3-e3.md`, `CR-1-f3.md` (§1 only), then the storefront-auth service and E1's admin user routes. Read-only reference: `crmeb/app/api/controller/v1/LoginController.php`, `crmeb/app/api/controller/v1/admin/UserController.php`, `crmeb/app/services/wechat/MiniProgramService.php` (`appCodeUnlimit`), `template/uni-app/api/{user,admin,api,store,activity}.js` markers.

## 1. First: the fake WeChat OA server moves to `@shop/testing/wechat` (CR-2-e3, accepted)
Move `core/src/wechat-oa/wechat-oa.fake-oa.ts` to `packages/testing/src/wechat/fake-oa-server.ts`, exported as `startFakeOaServer()` from `@shop/testing/wechat` (add the subpath export the way `@shop/testing` exposes its other entry points), keeping the interface as is. Then add the two endpoints this stream needs: `sns/jscode2session` and `wxa/getwxacodeunlimit` (returns PNG bytes; `behaviour` can make it answer an `errcode` body instead). Repoint the two wechat-oa int tests; nothing else changes.

## 2. CR-2-h2 §1 — decision: **no behaviour captcha**
`/api/v1/auth/captcha` and `/api/v1/auth/captcha/verifications` are not built. Confirm the SMS send path (`core/src/sms`, E1) enforces a per-phone and a per-IP limit and a resend cooldown, and that the codes are single-use (`GETDEL`); if any of those is missing, add it with an integration test. Record the exact limits in your status file — H3 deletes `pages/users/components/verify/**` and the two api calls on that basis.

## 3. CR-2-h2 §2 — `POST /api/v1/auth/phone/wechat-mini`
`auth: 'user'`, body `{ phoneCode }`, response `{ ok: true }`: exchange the code through the mini-program `getuserphonenumber` API (client in `core/src/wechat`, same transport as `code2session`; the fake server needs the endpoint too — add it in §1), then bind to the **signed-in** account with the same `AUTH_PHONE_TAKEN` / `AUTH_PHONE_ALREADY_BOUND` semantics as `authBindPhone`. Integration test through the fake server, including a code the server rejects.

## 4. CR-2-h2 §3 — six staff routes under `/api/v1/staff/`, `auth: 'staff'`
Paths exactly as the CR lists. Decision on what a 店员 sees: nickname, avatar, **masked phone** (`138****1234`), group, labels, order count, spend total, account status (read-only), registration date. **No** address book, no raw phone, no admin-only fields. Keyword search matches nickname and full phone (the search may take the full number; the response still masks). Reuse E1's group/label assignment services. Tests: shopper 403 / staff 200 per route; masked phone pinned; the assignment routes prove the write the admin route would do.

## 5. CR-6-h2 — `GET /api/v1/wechat/mini-qrcodes`
`auth: 'user'`, query `{ page, scene }` (`scene` ≤ 32 bytes, validated by zod; `page` must be one of the mini-program's pages — keep an allow-list constant in the contract with the pages the storefront actually shares: product detail, group-buy detail, presale detail, home). Response `{ url }`. Implementation: `wxa/getwxacodeunlimit` with the mini credentials, the PNG stored through the storage domain (`core/src/storage` — read how attachments are written, use its service, never write files yourself) and the row cached by `(page, scene)` in a small `wechat_mini_codes` table if none exists (add it — one migration, orchestrator grants it) so the second request returns the same URL without calling WeChat. Tests: first call hits the fake once, second call zero times; a bad scene is 422; a WeChat `errcode` is a `WECHAT_MINI_CODE_FAILED` (502-class) error.

## 6. CR-1-f3 §1 — the visits beacon
Legacy `setVisit` was removed by H because no route existed. Restore it as `POST /api/v1/visits`, `auth: 'user-optional'` (read how the conventions express an optional session), body `{ path, platform }`, response 204; the writer lives in `core/src/user/user.visit.service.ts` and inserts `user_visits` (`user_id` nullable, `session_id` from the bearer or a client-provided `X-Session-Id` header, `path`, `ip`, `province`/`city` left null until a geo source exists, `platform`, `created_at`). Rate-limit per session (one row per path per minute is enough). Stats' figures start reading true with no change on F3's side; write one int test that a beacon makes `stats` 访客数 count 1.

## 7. `routine_appId` has two claimants
`wechat` (C, `core/src/wechat/wechat.config.ts`) owns every WeChat credential (decision CR-1-j). Remove `appId` / `appSecret` / `token` / `encodingAesKey` / `messageMode` from `wechat-mini` — move `token` / `encodingAesKey` / `messageMode` into `wechat` as `miniToken` / `miniAesKey` / `miniMessageMode` with their legacy keys if anything reads them, otherwise drop them with a note. `wechat-mini` keeps only `enabled`, `name`, `contactType`, `contactPhone`. Repoint `storefront-auth.service.ts` at `wechatConfig.miniAppId`. Then change `packages/etl/src/config.test.ts` so the live-registry assertion is the opposite: **no legacy key has two claimants**, and keep the fan-out logic covered by a synthetic registry.

## 8. CR-3-e3 — partial unique index
`wechat_qrcode_categories_name_uq` becomes `.where(sql\`deleted_at is null\`)` plus the drizzle migration; keep the 409 code, shorten its message. Int test: delete, recreate with the same name succeeds.

## Out of scope
Anything under `template/uni-app` (H3 flips the markers; list every new route's fields in your status file), admin UI beyond what the config-group change forces, real WeChat endpoints (fakes only, always).

## Rules
Never push, never SSH, never touch `crmeb/`, `template/**`, or another stream's worktree; lockfile never. `pnpm gen` after adding routes/config groups; contracts first with examples. Commit after every unit with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Before the final commit, from `next/`: `pnpm turbo run gen typecheck lint test:unit build`, `pnpm --filter @shop/core test:int`, `pnpm --filter @shop/web test:int`, `pnpm --filter @shop/testing test:int`, `pnpm --filter @shop/etl test`, `pnpm exec prettier --check .`, `pnpm --filter @shop/contracts check:examples`. Write `docs/rewrite/status/e4.md`. Final report: routes with fields, SMS limits, decisions, anything left.
