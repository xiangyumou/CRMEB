# Stream G3 — DIY follow-up (CR-1/2/3-g2)

**Worktree** `../CRMEB-wt/ws-g3` · **Branch** `rewrite/ws-g3-diy-followup` · **Owns for this task** `next/apps/web/src/admin/diy/**`, `next/packages/contracts/src/diy/**` (schemas, registry, fixtures are read-only) · read `docs/rewrite/status/{g1,g2}.md` and `docs/rewrite/cr/CR-{1,2,3}-g2.md` first

The three CRs are decided; implement them.

1. **CR-1-g2 — accepted.** Promote the eight editors from `panels/_fields/` into `fields/` exactly as the CR lays out, export them from `fields/index.ts`, re-point every panel import, delete the promoted files from `_fields/`. No behaviour change: the panel suite must stay green without editing an assertion.
2. **CR-2-g2 — option 3.** No production page and no demo page uses 超级组件, and its inner designer is not scheduled. Remove `customComponent` from `CREATABLE_COMPONENT_KEYS` so the palette no longer offers it; it stays renderable and its panel stays (existing nodes remain editable, `customComponents` still round-trips untouched). Update the registry tests and the counts in both status files; say so in `docs/rewrite/invariants.md` under 页面装修.
3. **CR-3-g2 — accepted.** A freshly dragged component must be as configurable as one the old admin has opened and saved. For every component in the CR's table, extend `defaults/<key>.default.ts` with exactly the groups and values the legacy panel's `patchConfig` / `defaultConfig` merge injects (read the `.vue` — do not guess values), keeping the older scalar spacing keys in step with the four-sided pairs the way `userInfor.default.ts` does. Do **not** add the `c_common_style` debris key. Rules that must keep holding: no panel writes on open; "open every panel, change nothing, save" stays deep-equal on every fixture; each new default validates against its component schema; add a test per touched component that the default now carries every group its panel can draw (so the gap cannot reopen). Then the two smaller asks in the CR: the `labels` and `brand` picker kinds on `DiyPickerKind` (wire `labels` to the catalog's label list contract, now merged; `brand` has no backend — drop it and list the legacy rows that lose their picker), and 图片魔方 样式十一's free-draw canvas: implement it if it is under a day's work against the existing hotspot editor, otherwise leave the style selectable for existing nodes only and document it.
4. While there: the catalog is merged — replace the id-input stand-ins in the product and category pickers with the real pickers over `@shop/contracts/catalog/*` list routes (check `status/g1.md` "pickers" and `status/a.md` for the DTO; `productCardsFor` exists for previews).

Done = whole-workspace `pnpm gen typecheck lint test:unit test:int build`, prettier clean; status in `docs/rewrite/status/g3.md`; the three CR files marked applied.
