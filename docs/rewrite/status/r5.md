# R5 — wave-6 tail (what R1–R4 handed back, STAB-001 to 10/10, the CR sweep)

Branch `rewrite/ws-r5-wave6-tail`, worktree `../CRMEB-wt/ws-r5`, cut from
`rewrite/integration` at `84f1b2460`. Brief: `docs/rewrite/briefs/R5-wave6-tail.md`.

## Units

| #   | unit                                                                                          | status      | commit                  |
| --- | --------------------------------------------------------------------------------------------- | ----------- | ----------------------- |
| 1   | CR-2-r1 — groupbuy join takes the group row first; re-check under the lock                    | RESOLVED    | `b2731e6a8`             |
| 1   | STAB-001 — ten shuffled rounds                                                                | **10 / 10** | `d5666e5e1` (docs only) |
| 2   | CR-1-r1 — config reads inside a transaction go through it; `TX_POOL_OWED` empty               | RESOLVED    | `946852011`             |
| 3   | CR-1-r2 — `registerAllDomains()` calls `registerOrderDomain()` first                          | RESOLVED    | `37c63da08`             |
| 4   | CR-1-s — `ctx.etag` / `ctx.notModified`; DIY home + category routes 304                       | RESOLVED    | `6fbdfe89b`             |
| 5   | admin e2e isolation — per-checkout port + stack file, reuse opt-in                            | done        | `314cc9b07`             |
| 5   | small items — web `onConnectionError`, `CATALOG_CARD_POOL_EMPTY`, `banned.ts`, compose DB env | done        | `c42816766`             |
| 6   | CR sweep — 42 marked RESOLVED with evidence, 3 left open                                      | done        | `32b1805f4`             |

## 1. CR-2-r1

- **Change.** `core/src/groupbuy/groupbuy.order.ts` `afterCreate`: when joining
  a team, `repo.lockGroup(tx, groupId)` before the activity-stock loop, then
  the locked row is re-checked (`GROUPBUY_GROUP_NOT_FOUND` if missing or of
  another activity, `assertGroupJoinable` → `GROUPBUY_GROUP_NOT_JOINABLE`
  otherwise). Join, refund and departure now all take group → activity SKU.
- **Tests added** (`groupbuy.concurrency.int.test.ts::a join into a team that fails a moment before (CR-2-r1)`):
  `> is refused under the group lock, and reserves nothing`,
  `> queues behind a transaction holding the team, then sees what it decided`.
  Both fail on the old code (`promise resolved "2" instead of rejecting`).
- `groupbuy.concurrency` shuffled, seeds 1–20: 13/13 each.

## 1. STAB-001 — ten of ten

Each round ran from `next/packages/core` as
`timeout 900 pnpm exec vitest run --project int --sequence.seed=<n> --sequence.shuffle concurrency.int.test.ts order.sequence.int.test.ts`,
one after the other, on tree `b2731e6a8` (CR-2-r1 committed). The set is 16
files and 160 tests, none expected to fail (R2 flipped SEQ-001's two pins).
Sibling worktrees were building; no round was rerun.

| round (seed) | result   | time | load | tests                |
| ------------ | -------- | ---- | ---- | -------------------- |
| 1            | **pass** | 18 s | 4.3  | 160/160, 16/16 files |
| 2            | **pass** | 27 s | 11.6 | 160/160              |
| 3            | **pass** | 36 s | 14.0 | 160/160              |
| 4            | **pass** | 27 s | 12.3 | 160/160              |
| 5            | **pass** | 40 s | 15.2 | 160/160              |
| 6            | **pass** | 29 s | 14.1 | 160/160              |
| 7            | **pass** | 27 s | 13.7 | 160/160              |
| 8            | **pass** | 21 s | 11.9 | 160/160              |
| 9            | **pass** | 20 s | 10.4 | 160/160              |
| 10           | **pass** | 23 s | 9.7  | 160/160              |

Seeds 1 and 9, the two R1 lost to the deadlock, are clean.

### STAB-001 ledger text for the orchestrator

Replace the STAB-001 row in `invariants.md` with the row below **and delete the
`STAB-001` entry in `guards/src/lib/pending-edits.ts` in the same commit** (the
guard fails the stale entry as soon as the row says `ported`). Checked with a
temporary local edit, reverted: every one of the nine ids resolves
(`pnpm guards invariants`: the only failure is that pending-edits entry).
The ids are one per race the statement names; the proof is the soak command,
the way MUT-001 names its script.

```
| STAB-001  | The concurrency set (payment creation vs cancellation, refund agreement in two processes, coupon races, the virtual-card race, multi-item rollback, the fixed-seed sequence) passes 10 consecutive repetitions with no flake. **Owner: stream K (K2 found CR-50/51/52/53-k2); R1 fixed CR-50/51/53-k2 and filed CR-2-r1 (the groupbuy join vs refund deadlock), R5 fixed CR-2-r1. The proof is ten consecutive shuffled rounds, seeds 1–10, of `pnpm --filter @shop/core exec vitest run --project int --sequence.seed=<round> --sequence.shuffle concurrency.int.test.ts order.sequence.int.test.ts` — the 16-file set, every test, 10 of 10 clean at R5 (`docs/rewrite/status/r5.md`); nightly as the `concurrency-soak` job in `next.yml`, 50 rounds. The ids below are one per race the statement names; the soak runs every test in the set.** | `packages/core/src/payment/payment.concurrency.int.test.ts::QUEUE-003 — a callback racing an order cancel > never cancels an order whose money arrived, whichever side gets there first`, `packages/core/src/order/order.concurrency.int.test.ts::cancel racing the paid transition > lets exactly one of them through`, `… > two checkouts for the last unit > hands back every line it already took when a later line is short`, `packages/core/src/refund/refund.concurrency.int.test.ts::REFUND-002 — approval racing the buyer withdrawing > queues one gateway call however many times the operator presses 同意`, `packages/core/src/coupon/coupon.concurrency.int.test.ts::COUPON-008 — one user tapping 领取 twice > never lets simultaneous taps exceed a limit above one`, `packages/core/src/order/order.fulfil.concurrency.int.test.ts::two dispatchers replaying the same virtual delivery > gives the last card to exactly one of two orders racing for it`, `packages/core/src/groupbuy/groupbuy.concurrency.int.test.ts::leadership > passes to exactly one heir while a join is in flight`, `… > a join into a team that fails a moment before (CR-2-r1) > queues behind a transaction holding the team, then sees what it decided`, `packages/core/src/order/order.sequence.int.test.ts::SEQ-001 — a fixed-seed interleaving of real operations > holds every invariant after every step, seed <seed>` | ported |
```

## 2. CR-1-r1

`order.checkout.service.ts` `buildDraft`, `shipping.freight.port.ts` `quote`
(the `Promise.all` became two sequential reads on the same `db`) and
`order.fulfil.effects.ts` `autoDeliver` read their config groups with
`ctx.config.getIn(dbOrTx, group)`. `guards/src/checks/tx-pool.ts`
`TX_POOL_OWED` is empty (`pnpm guards tx-pool`: 0 allowed, 0 owed).
Tests (`order/order.int.test.ts::CR-1-r1 — config read through the transaction, not a second connection`):
place an order with a freight quote, and auto-deliver a virtual order, each on
a pool of one with a cold config cache; both time out acquiring a second
connection on the old code.

## 3. CR-1-r2

Option 2: the generator emits `registerOrderDomain()` first (and fails `pnpm
gen` if the order registrar disappears); imports stay alphabetical, so the
production module graph is unchanged. New case
`order.stock.hooks.test.ts::CR-1-r2 — after resetOrderPorts() + registerAllDomains() > still runs the sale commit first`
fails on the old generator (`expected 'groupbuy:take-seat' to be 'order:commit-sale'`).

## 5. Admin e2e isolation (for R6 to copy into `e2e/storefront`)

`next/e2e/admin/src/stack-file.ts` + `playwright.config.ts` + README §「多个检出」:

- `CHECKOUT_ROOT` = `path.resolve(import.meta.dirname, '../../../..')`;
  `CHECKOUT_ID` = first 8 hex of its SHA-256.
- default `PORT` = `20000 + (parseInt(CHECKOUT_ID, 16) % 5000)`; default
  `STACK_FILE` = `$TMPDIR/shop-e2e-admin-<CHECKOUT_ID>.json`.
- `SHOP_E2E_PORT` / `SHOP_E2E_STACK` still override; `reuseExistingServer` is
  `SHOP_E2E_REUSE === '1'` (was `!CI`), so a busy port is an error, not a
  silent handover to another worktree's server.
- Suggested for the storefront (R6's call): same `CHECKOUT_ID`, ports in
  25000–29999 (three consecutive: edge, web, gateway), stack file
  `shop-e2e-storefront-<CHECKOUT_ID>.json`, same `SHOP_E2E_REUSE`.

## 4. CR-1-s (+ CR-42-k2's second half)

`handle.ts`: the CR's patch plus `{ weak: true }`. 304 on
`diy/pages/home` (from the cached entry), `catalog/categories` and
`catalog/categories/version`. Left, not one-liners (listed in CR-1-s): the
other seven DIY storefront routes and `site/config` — recommendation: the diy
and system services' `tagged()` call an optional `ctx.etag`/`notModified`.

## 5. Small items

- `apps/web/src/server/container.ts`: `webDbOptions(env, logger)` gives
  `createDb` the pool max and an `onConnectionError` that warns with the
  worker's wording; `container.test.ts::webDbOptions` emits a pool `error`
  on a lazy pool and asserts the warn.
- `CATALOG_CARD_POOL_EMPTY` deleted from `contracts/src/catalog/errors.ts`:
  nothing in `next/` or `template/uni-app` names it (R1 deleted its last
  thrower, CR-23-k2); the order domain's `ORDER_VIRTUAL_CARD_EXHAUSTED` is the
  live code.
- `guards/src/checks/banned.ts`: the safe-fetch comment says
  `options.transport ?? pinnedTransport()`.
- `deploy/next/compose.yml`: `DB_POOL_ACQUIRE_TIMEOUT_MS` (5000) and
  `DB_IDLE_IN_TX_TIMEOUT_MS` (30000) passed to web and worker; documented in
  `deployment.env.example` beside `WEB_/WORKER_DB_POOL_MAX`. `docker compose
config` with the example env resolves both on both services.

## 6. CR sweep

Every CR not already RESOLVED / closed / applied / accepted / decided, by bucket. Bucket 1 files carry a `**Status (R5 sweep, 2026-09-23): RESOLVED** — …` line under the title (the old status line is kept as history). Skipped by instruction: CR-1-h4, CR-2-h4, CR-3-h4 (R6), CR-4-i, CR-5-i, CR-7-i (resolved by H4, not yet on this branch's base). Everything else was already RESOLVED / closed / applied / accepted / decided before R5; nothing was re-opened.

| CR          | bucket           | evidence                                                                                                                                                                                                                                    |
| ----------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CR-1-b2     | 1 — already done | the express-company routes live in `contracts/src/shipping/shipping.express.contract.ts` (F2, `1158b892e`) and the route files say so; B2's local adapter is gone.                                                                          |
| CR-1-d2     | 1 — already done | `PricingDraft.adjustments` is in `order/ports.ts` (B3, `41883680c`); presale contributes through it, `inTx`/`contributedAdjustment` are gone.                                                                                               |
| CR-1-e2     | 1 — already done | `site.publicOrigin` / `site.extraOrigins` are config atoms (N1, `fa65145d8`) and `notification.send` reads `publicOrigin`.                                                                                                                  |
| CR-1-e4     | 1 — already done | `MARKER_REASSIGNMENTS` in `next/guards` is empty (H3, `503fbcf35`): the markers moved to their owners.                                                                                                                                      |
| CR-1-f2     | 1 — already done | `FreightLine.freightMode` / `fixedFreightFen` and 满额包邮 are in the freight port (B3, `41883680c`).                                                                                                                                       |
| CR-1-f3     | 1 — already done | `user_visits` has a writer, `user/user.visit.repo.ts` (E4, `53ba7a17a`); the cart product event is emitted from catalog (B3, `41883680c`).                                                                                                  |
| CR-1-g1     | 1 — already done | §2 `"./*": "./src/*/index.ts"` (`6fd2f32b3`); §1 closed as recommended (content stays `jsonb`, limit documented).                                                                                                                           |
| CR-1-golden | 1 — already done | the CONVENTIONS row is reworded as asked (`6fd2f32b3`).                                                                                                                                                                                     |
| CR-1-h2     | 1 — already done | `GET /api/v1/groupbuy/summary` exists (contract + `app/api/v1/groupbuy/summary/route.ts`, B3, `41883680c`).                                                                                                                                 |
| CR-1-j2     | 1 — already done | `GET /api/v1/readyz` exists (J3, `d88065849`).                                                                                                                                                                                              |
| CR-1-p0a    | 1 — already done | applied and kept: `pnpm-workspace.yaml` `allowBuilds` (`08135d139`).                                                                                                                                                                        |
| CR-1-p0b    | 1 — already done | the conventions' `PartOf` uses `NonNullable` (`8a4a88a11`); the local workaround is gone.                                                                                                                                                   |
| CR-2-c      | 1 — already done | CONVENTIONS places config groups in the domain (`<domain>/<domain>.config.ts`) and `core/scripts/gen-config-groups.ts` aggregates them into `config-groups.gen.ts` (CR-3-f1, `b916be269`).                                                  |
| CR-2-e2     | 1 — already done | N1 (`fa65145d8`) wired `notify` at the missing call sites; every registered event now has a caller (`refund_rejected` from `refund.admin.ts`).                                                                                              |
| CR-2-e3     | 1 — already done | the fake official-account server lives in `@shop/testing/wechat` (E4, `53ba7a17a`).                                                                                                                                                         |
| CR-2-f2     | 1 — already done | the kuaidi100 client was removed (F4, `31ec0782e`).                                                                                                                                                                                         |
| CR-2-golden | 1 — already done | `@shop/core`'s `"./*": "./src/*/index.ts"` export map (`6fd2f32b3`); no `/index` imports remain in the app.                                                                                                                                 |
| CR-2-h2     | 1 — already done | §1 the captcha was dropped (E4 `53ba7a17a`, H3 `503fbcf35`); §2 `POST /api/v1/auth/phone/wechat-mini` and §3 `/api/v1/staff/users*` (`user.staff.contract.ts`) exist (E4, `53ba7a17a`).                                                     |
| CR-2-j2     | 1 — already done | every OPS/REL row in `invariants.md` is mapped (ported or retired); `pnpm guards invariants` passes.                                                                                                                                        |
| CR-2-p0a    | 1 — already done | `.prettierignore` no longer needs the workaround (`67bd50fff`).                                                                                                                                                                             |
| CR-3-b1     | 1 — already done | the schema doc comment says "Every goods-level discount" as asked (`475f59217`).                                                                                                                                                            |
| CR-3-d2     | 1 — already done | the admin kit's `ModalForm` takes a `load` (F4, `31ec0782e`); presale's local workaround is gone.                                                                                                                                           |
| CR-3-e3     | 1 — already done | migration `0001_wechat_mini_codes.sql` has the scoped unique index (E4, `53ba7a17a`).                                                                                                                                                       |
| CR-3-e4     | 1 — already done | the fix stands in `@shop/testing`'s mock server with the whole-table test "is a total order over every registered route" (B3, `41883680c`).                                                                                                 |
| CR-3-golden | 1 — already done | the `arrayContaining` pattern is kept in `define-job.test.ts` (`6fd2f32b3`).                                                                                                                                                                |
| CR-3-h2     | 1 — already done | `/api/v1/diy/pages/user-center`, `/api/v1/diy/navigation` and `/api/v1/diy/layouts/:type` exist (F4, `31ec0782e`).                                                                                                                          |
| CR-3-j2     | 1 — already done | `next.yml` has the images and rehearsal jobs and the `deploy/next/**` paths (J3, `d88065849`).                                                                                                                                              |
| CR-3-p0a    | 1 — already done | the workaround is accepted and documented in CONVENTIONS' tooling caveats (`scripts/eslint-ts6.mjs`).                                                                                                                                       |
| CR-4-b1     | 1 — already done | decided by the ledger: PRICE-001/002 retired, STOCK-004 and QUEUE-008 ported (`475f59217`).                                                                                                                                                 |
| CR-4-golden | 1 — already done | a global `next/navigation` mock in `apps/web/src/test/setup.ts` replaces the per-file lines (`6fd2f32b3`).                                                                                                                                  |
| CR-4-h2     | 1 — already done | `catalog.staff.contract.ts` has the ten staff routes (A2, `a867050b7`).                                                                                                                                                                     |
| CR-4-k      | 1 — already done | `next.yml` has the Guards step and the concurrency-soak and e2e-admin jobs (J3, `d88065849`).                                                                                                                                               |
| CR-5-b1     | 1 — already done | `mock-server.test.ts` sends the example's query (`8514dc832`); the merge gate is green.                                                                                                                                                     |
| CR-5-h2     | 1 — already done | `/api/v1/orders/:id/gift-coupons` and `/api/v1/staff/coupons` exist (`coupon.staff.contract.ts`, B3, `41883680c`).                                                                                                                          |
| CR-6-f1     | 1 — already done | option 1: `trade.config.ts` is deleted and its keys folded into `order.config` / `order.fulfil.config` (B3, `41883680c`).                                                                                                                   |
| CR-6-h2     | 1 — already done | `/api/v1/wechat/mini-qrcodes` exists (E4, `53ba7a17a`).                                                                                                                                                                                     |
| CR-6-k      | 1 — already done | `MARKER_REASSIGNMENTS` is empty (H3, `503fbcf35`).                                                                                                                                                                                          |
| CR-7-h2     | 1 — already done | `GET /api/v1/site/config` exists (F4, `31ec0782e`).                                                                                                                                                                                         |
| CR-20-k2    | 1 — already done | `.github/workflows/next.yml` runs the mutations job (`8c8d8f8fc`, K2 merge).                                                                                                                                                                |
| CR-21-k2    | 1 — already done | the TLS-001 ledger row carries the WeChat Pay TLS test (`8c8d8f8fc`), the refund client's (R2, `b02d42bb8`) and the WeChat client's.                                                                                                        |
| CR-22-k2    | 1 — already done | the PAYC-001 ledger row carries `payment.cancel-lock.int.test.ts` (`8c8d8f8fc`).                                                                                                                                                            |
| CR-52-k2    | 1 — already done | the soak step runs `pnpm --filter @shop/core exec vitest` with substring filters (`8c8d8f8fc`); R5 ran the same command ten times (`status/r5.md`).                                                                                         |
| CR-1-r1     | 2 — fixed here   | unit 2                                                                                                                                                                                                                                      |
| CR-1-r2     | 2 — fixed here   | unit 3                                                                                                                                                                                                                                      |
| CR-1-s      | 2 — fixed here   | unit 4 (+ CR-42-k2's second half)                                                                                                                                                                                                           |
| CR-2-r1     | 2 — fixed here   | unit 1                                                                                                                                                                                                                                      |
| CR-1-p0s    | 3 — open         | §1 `pg_trgm` (`67bd50fff`) and §2 the FKs are done; §3 (shared schema helpers in `_shared.ts`) is not. Owner: orchestrator (`packages/db/src/schema/**`). Recommendation: drop §3 as cosmetic and close.                                    |
| CR-2-b2     | 3 — open         | the export still ships as CSV inside JSON. Owner: orchestrator (a contract-convention call). Recommendation: option 1, leave it — the admin downloads it fine and the statistics export (CONSOLE-003) already has the BOM/formula handling. |
| CR-7-k      | 3 — open         | no `respondWith` helper and no guard; 27 `apps/web` test files call `configureApi` by hand. Owner: orchestrator (`apps/web/src/test/**`) then guards. Recommendation: build the helper first, add the guard once the files are migrated.    |

## Done gate (from `next/`, on `32b1805f4` + this status file)

| command                                                                      | result                                                                         |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `pnpm turbo run gen typecheck lint test:unit build`                          | 35/35 tasks                                                                    |
| `pnpm turbo run test:int --force --concurrency=4`                            | 12/12 tasks (core 70 files, web 22, worker, etl, testing), all green, no rerun |
| `pnpm exec prettier --check .`                                               | clean                                                                          |
| `pnpm --filter @shop/contracts check:examples`                               | 428 routes OK                                                                  |
| `pnpm guards`                                                                | 11 checks, 0 failures, 6 pending (H4 = 5, R5 = 1: STAB-001, row text above)    |
| `pnpm --filter @shop/guards mutations`                                       | 10 killed, 0 survived                                                          |
| `SHOP_E2E_PORT=3510 SHOP_E2E_STACK=/tmp/r5-admin-stack.json … e2e-admin e2e` | 45 passed (fresh `next build` from the gate)                                   |

## Merge notes

- **Ledger (orchestrator):** replace the STAB-001 row with the text in §1 and
  delete the `STAB-001` entry in `guards/src/lib/pending-edits.ts` in the same
  commit. Nothing else in `invariants.md` changes.
- **Files R6 may also touch** (R6 was told to keep out; hunks are small):
  `core/src/groupbuy/groupbuy.order.ts` (`afterCreate` only),
  `core/src/groupbuy/groupbuy.concurrency.int.test.ts` (one new `describe`),
  `core/src/order/order.checkout.service.ts` (`buildDraft`, one line),
  `core/src/order/order.int.test.ts` (imports + one appended `describe`),
  `core/src/order/order.fulfil.effects.ts` (`autoDeliver`, one line),
  `core/src/shipping/shipping.freight.port.ts` (`quote`),
  `core/src/order/order.stock.hooks.test.ts`, `core/scripts/gen-config-groups.ts`,
  `apps/web/src/server/{handle,container}.ts`.
- **Outside the brief's path list, by the brief's own units:**
  `apps/web/app/api/v1/diy/pages/home/route.ts` + new `diy/home.int.test.ts`
  (CR-42-k2's route test), the two `catalog/categories` route files and one
  test in `app/admin-api/catalog/catalog.int.test.ts` (the one-liner 304s).
- `RequestCtx` gained `etag()` / `notModified()`: anything that builds a
  `RequestCtx` by hand would need them; nothing in the tree does.
- `e2e/admin`'s default port is no longer 3210 and reuse is off by default:
  CI is unaffected (it never reused), and a developer who kept a warm stack
  now adds `SHOP_E2E_REUSE=1`.
- `docs/rewrite/cr/CR-4-i`, `CR-5-i`, `CR-7-i` are untouched here; H4's edits to
  them win.
