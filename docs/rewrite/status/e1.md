# E1 — Users and storefront login

Branch `rewrite/ws-e1-user`, worktree `../CRMEB-wt/ws-e1`.
Domains `user`, `auth/storefront`, `sms`.

## Contracts ready

`packages/contracts/src/user/` and `packages/contracts/src/auth/auth.storefront.contract.ts`
are committed and green:

```
$ pnpm --filter @shop/contracts gen
contracts: aggregated 31 contract file(s), 11 error file(s)
contracts: wrote openapi.json (279 route(s), 220 path(s))
$ pnpm --filter @shop/contracts check:examples
contracts: 279 route(s) OK, every example parses.
```

**51 new routes**, every one with at least one truthful example, so the mock
server answers them now.

| File                               | Routes | Surface                                                                                |
| ---------------------------------- | ------ | -------------------------------------------------------------------------------------- |
| `auth/auth.storefront.contract.ts` | 15     | SMS codes, password / SMS / WeChat sign-in, logout, passwords, phone binding           |
| `user/user.storefront.contract.ts` | 12     | profile, addresses, account cancellation                                               |
| `user/user.admin.contract.ts`      | 12     | customer list & detail, status, password reset, batch group/label, cancellation review |
| `user/user.taxonomy.contract.ts`   | 12     | user groups, labels, label categories                                                  |

New error registries: `user/errors.ts` (`USER_*`, 14 codes) and
`storefrontAuthErrors` in `auth/errors.ts` (`AUTH_*`, 16 codes, alongside
P0-A's admin-side `authErrors` in the same file — `pnpm gen` flattens every
export of a domain's `errors.ts`, so a second registry needed no new folder).

### What other streams need from these contracts

1. **A session is a resource.** `POST /api/v1/auth/sessions/{password,sms,
wechat-mini,wechat-oa}` creates one, `DELETE /api/v1/auth/sessions/current`
   is 退出登录, `DELETE /api/v1/auth/sessions` is 退出所有设备. There is no
   `POST /auth/logout`.
2. **The token is in the body, not a cookie.** Storefront clients send
   `Authorization: Bearer <token>`. Only its sha256 is stored, and it carries
   the `password_version` it was minted with.
3. **WeChat sign-in answers with a flat two-state result**, not a union:
   `{ status: 'signed-in' | 'phone-required', session, registered, bindToken,
bindTokenExpiresInSec }`. `phone-required` hands back a short-lived
   `bindToken` so the second call does not have to redeem the (single-use)
   WeChat `code` again.
4. **`POST /api/v1/auth/sms-codes` takes a `scene`** (`login`, `register`,
   `reset-password`, `bind-phone`, `change-phone`). The scene is part of the
   Redis key, so a code minted to confirm a phone change cannot be replayed
   against login.
5. **The admin user-detail 订单 / 优惠券 tabs are other domains' routes.**
   `GET /admin-api/orders?userId=` (B2) and `GET /admin-api/user-coupons?userId=`
   (coupon). This stream ships only `GET /admin-api/users/:id/addresses`, and
   granting a coupon from the user page is coupon's own
   `POST /admin-api/coupons/:id/grants`.
6. **`phone` is masked in the admin list** (`138****8000`) and unmasked only on
   the detail route.

## Decisions

- **Contracts for storefront auth live in `contracts/src/auth/`**, not a new
  domain folder: `pnpm gen` aggregates exactly one `errors.ts` per domain
  directory, and splitting the auth surface in two would have meant two
  registries for one `AUTH_` prefix. `storefront-schemas.ts` keeps the two
  surfaces' shapes apart inside that folder.
- **SMS code login also registers.** A phone with no account gets one, and
  `registered: true` says so. The legacy funnel's 404 → "go and register with
  the same phone and the same code" step is the most abandoned in the old flow.
- **Rebinding a phone needs a code on the new number only.** Demanding one on
  the old number too locks out exactly the customers this screen exists for
  (lost or changed handset).
- **A password change accepts either proof**: the old password, or a fresh SMS
  code to the bound number. WeChat-created accounts have no password to quote,
  and the legacy system had no way for them ever to get one.
- **Address cap of 20 per customer.** The legacy system had none, which is how
  a scripted client filled the table. Recorded here because it is a new refusal
  (`USER_ADDRESS_LIMIT_REACHED`).
- **Group membership is many-to-many.** The schema's `user_groups_map` replaces
  `eb_user.group_id`; batch assignment therefore takes a `mode`
  (`replace` / `add` / `remove`), because the legacy `save_set_label` always
  replaced and silently stripped every other label from every selected
  customer.
- **Cancellation is a real two-step review.** The legacy storefront endpoint
  flipped `is_del = 1` immediately and the admin 同意/拒绝 endpoints were empty
  stubs that returned success. Approval here anonymises and revokes sessions;
  it never deletes a row that orders reference.

## Landed

| Unit            | Where                                                                              |
| --------------- | ---------------------------------------------------------------------------------- |
| user domain     | `core/src/user/` — repo / rules / service, admin service, `permissions.ts`         |
| storefront auth | `core/src/user/storefront-auth.{service,rules}.ts`, sessions on `password_version` |
| SMS port        | `core/src/sms/` — `SmsSender` + `fakeSmsSender`, no provider call anywhere         |
| WeChat port     | `core/src/user/wechat-identity.port.ts` + fake (see **Blocked** below)             |
| tests           | 52 storefront-auth int tests, the user int/unit suites, 12 concurrency races       |
| routes          | all 51 paths under `apps/web/app/{api/v1,admin-api}/**`, plus 9 HTTP int tests     |
| admin UI        | `apps/web/app/admin/(shell)/user/{customers,groups,labels,cancellations}` + menu   |
| ETL             | `packages/etl/src/mappers/user.ts` + 26 unit tests                                 |
| invariants      | `docs/rewrite/invariants.md`: USER-001/002 ported, USER-010…015 added              |

Invariant IDs: mine are **USER-010 … USER-015**. The first draft reused
USER-002…005, which the ledger already owns with different legacy meanings; the
renumbered rows and their test names are in the "E1 additions" table.

## Decisions taken while implementing

- **`AUTH_PASSWORD_NOT_SET` never distinguishes "no such account".** Both answer
  the same way, so the sign-in form is not an account-enumeration oracle.
- **`changePassword` kills the calling session too.** Bumping
  `password_version` is what makes a stolen token useless; exempting the caller
  would mean the one session an attacker holds survives the remedy.
- **Completing an OA sign-in verifies the `login` scene**, not `bind-phone`: the
  user is signing in, and accepting a bind-phone code here would let a code
  minted inside an authenticated session mint a new one.
- **`bindPhone` / `changePhone` return `{ ok: true }` and mint no session.**
  Binding a number is not a re-authentication, and `password_version` stays put
  so the customer's other devices keep working.
- **`defaultAddress` and `currentCancellation` answer with an envelope**
  (`{ address }` / `{ request }`) so "none" is a 200 with `null`, not a 404 the
  storefront has to treat as a normal outcome.
- **A WeChat sign-in race recovers instead of erroring.** Two taps that both
  miss the identity row used to leave the loser with
  `AUTH_WECHAT_ALREADY_BOUND`; the loser now re-reads the winner's identity and
  signs in. Proved by `user.concurrency.int.test.ts` (six concurrent
  `miniLogin`s → one account, one identity, six signed-in results).
- **`hasWechat` is `'true' | 'false'` with a transform**, not
  `z.coerce.boolean()`, which turns the string `"false"` into `true` — the
  未绑定 filter would have listed the bound accounts.
- **`verify_expire_time` is not mapped.** Codes live in Redis with their own
  TTL; carrying a legacy expiry into the new store would resurrect codes.
- **`system` must not import `user`.** The config groups this stream reads
  (`sms`, `wechat-oa`, `wechat-mini`) are F1's and are reached through
  `@shop/core/system`; `storefrontAuthConfig` is declared in `user`.
- **The ETL keeps legacy MD5 hashes tagged `md5_legacy`** and upgrades on next
  login rather than forcing a shop-wide password reset, and it never hard-deletes
  a cancelled account.
- **The ETL already maps `eb_wechat_user` → `wechat_identities`** (platform
  `oa`). **E2 should not map that table again.**

## Blocked / waiting

- **Stream C's WeChat client is not merged.** This stream defines a narrow
  `WechatIdentityPort` (`miniCodeToSession`, `miniPhoneNumber`, `oaCodeToUser`)
  inside `core/src/user/` with a fake for tests. **The orchestrator wires C's
  `WechatCoreClient` to it at merge**: `miniCodeToSession` → `miniCode2Session`,
  `oaCodeToUser` → `oaCodeExchange` + `oaUserInfo`. `miniPhoneNumber` has no
  method on C's client today and must go through `client.call('mini', { method:
'POST', path: '/wxa/business/getuserphonenumber', body: { code } })`.

## New dependencies

None. Nothing was added to any `package.json`, and `pnpm-lock.yaml` is untouched.

## Open CRs

None. Everything this stream needed was inside its own paths, except the WeChat
client above, which the brief already assigns to the orchestrator at merge.
