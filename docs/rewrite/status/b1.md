# B1 — Cart and checkout

Branch `rewrite/ws-b1-checkout`, worktree `../CRMEB-wt/ws-b1`. Nothing pushed.

## Contracts ready

`packages/contracts/src/cart/` and `packages/contracts/src/order/` are complete
and committed. **14 routes, 22 examples**, `check:examples` green:

```
$ pnpm --filter @shop/contracts check:examples
contracts: 32 route(s) OK, every example parses.   # 18 existing + 14 from B1
```

| Route id                | Method | Path                             |
| ----------------------- | ------ | -------------------------------- |
| `cart.list`             | GET    | `/api/v1/cart`                   |
| `cart.count`            | GET    | `/api/v1/cart/count`             |
| `cart.addItem`          | POST   | `/api/v1/cart/items`             |
| `cart.updateItem`       | PATCH  | `/api/v1/cart/items/:id`         |
| `cart.removeItem`       | DELETE | `/api/v1/cart/items/:id`         |
| `cart.removeItems`      | POST   | `/api/v1/cart/items/removals`    |
| `cart.setSelection`     | POST   | `/api/v1/cart/selections`        |
| `cart.rebuy`            | POST   | `/api/v1/cart/rebuys`            |
| `order.checkoutPreview` | POST   | `/api/v1/checkout/preview`       |
| `order.create`          | POST   | `/api/v1/orders`                 |
| `order.list`            | GET    | `/api/v1/orders`                 |
| `order.counts`          | GET    | `/api/v1/orders/counts`          |
| `order.detail`          | GET    | `/api/v1/orders/:id`             |
| `order.cancel`          | POST   | `/api/v1/orders/:id/cancel`      |

Error files: `cart/errors.ts` (6 codes), `order/errors.ts` (15 codes).

Resource segments this stream claims: `cart/**`, `checkout/**`, and on the
storefront surface `orders` (list / counts / detail / cancel). B2 owns
`/admin-api/orders/**` and the fulfilment sub-resources of `/api/v1/orders/:id`;
C owns the payment sub-resources. No method+path collides.

## Decisions other streams must know

- **`preview` and `create` take the same body.** There is no preview token and
  no server-side draft. `create` recomputes everything and, when the client
  sends `expectedPayableAmount`, refuses with `ORDER_PRICE_CHANGED` if the
  recomputation disagrees (risk matrix §1, "SKU swap between confirm and
  create").
- **`idempotencyKey` is required on `order.create`.** 8–64 chars of
  `[A-Za-z0-9_-]`. A replay returns the first order, 201, unchanged.
- **`orders.coupon_discount` holds every goods-level discount**, not only the
  coupon: coupon + each `PricingContributor`. The per-line
  `order_items.discount_amount` shares sum back to it exactly
  (`Money.allocate`). `payable = items + freight - couponDiscount`.
  C and B2 read it that way; the `adjustments[]` array on the preview is the
  itemised breakdown for the UI. See CR-3.
- **Cancel decision table** (from `PaymentPort.ensureNoOpenAttempts`, asked
  while the order row is locked): `closed` → transition, then release the stock
  and then the coupon, all in the same transaction; `paid` → throw
  `ORDER_ALREADY_PAID` and release nothing; `unknown` → throw
  `ORDER_PAYMENT_STATE_UNKNOWN` and release nothing. A paid order is never
  cancelled at all — `ORDER_TRANSITIONS` has no such edge, and it leaves
  through C's full refund.

## What landed

**Domain** — `packages/core/src/cart/` (repo, rules, service, index) and
`packages/core/src/order/` (repo, pricing, config, checkout, query, cancel,
state machine, the catalogue seam and its stand-in adapter, index).

**Routes** — 12 files under `apps/web/app/api/v1/{cart,checkout,orders}/`,
one per contract route (two of them carry two methods):

| File | Routes |
| --- | --- |
| `cart/route.ts` | `GET /api/v1/cart` |
| `cart/count/route.ts` | `GET /api/v1/cart/count` |
| `cart/items/route.ts` | `POST /api/v1/cart/items` |
| `cart/items/[id]/route.ts` | `PATCH`, `DELETE /api/v1/cart/items/:id` |
| `cart/items/removals/route.ts` | `POST /api/v1/cart/items/removals` |
| `cart/selections/route.ts` | `POST /api/v1/cart/selections` |
| `cart/rebuys/route.ts` | `POST /api/v1/cart/rebuys` |
| `checkout/preview/route.ts` | `POST /api/v1/checkout/preview` |
| `orders/route.ts` | `GET`, `POST /api/v1/orders` |
| `orders/counts/route.ts` | `GET /api/v1/orders/counts` |
| `orders/[id]/route.ts` | `GET /api/v1/orders/:id` |
| `orders/[id]/cancel/route.ts` | `POST /api/v1/orders/:id/cancel` |

**Jobs** — `order.autoCancel` (delayed, `dedupeKey` `order-auto-cancel:<id>`,
concurrency 4) and `order.sweepExpiredOrders` (`repeat: '3/5 * * * *'`,
concurrency 1). Neither is load-bearing: the sweep is the backstop for a queue
that loses the delayed job, and both go through the same conditional
transition, so a double delivery is a no-op.

**Config** — `order` group: `payWindowMinutes` (30, legacy `order_cancel_time`),
`autoCancelSweepLimit` (200).

**Tests** — 6 files, **111 tests**:

| File | Tests |
| --- | --- |
| `packages/core/src/order/order.pricing.test.ts` (unit) | 23 |
| `packages/core/src/cart/cart.rules.test.ts` (unit) | 13 |
| `packages/core/src/order/order.int.test.ts` | 35 |
| `packages/core/src/order/order.concurrency.int.test.ts` | 10 |
| `packages/core/src/cart/cart.int.test.ts` | 18 |
| `apps/web/app/api/v1/checkout.int.test.ts` (HTTP) | 12 |

The five mandatory races are all in `order.concurrency.int.test.ts` except the
cart's own, which is in `cart.int.test.ts`:

| Race | Test |
| --- | --- |
| Last unit of stock, two checkouts | `two checkouts for the last unit > sells it exactly once` (and 12 buyers for 10 units) |
| Duplicate submit, same idempotency key | `the same idempotency key submitted several times at once > creates exactly one order and answers every caller with it` |
| Cancel racing the paid transition | `cancel racing the paid transition > lets exactly one of them through` |
| One coupon, two orders | `one coupon spent by two orders at once > is redeemed once, and the losing order does not exist` |
| Auto-cancel racing the user's cancel | `auto-cancel racing the user cancel > cancels once, returns the stock once and returns the coupon once` |
| (extra) 加入购物车 from two devices | `cart.int.test.ts::加入购物车 from several devices at once > ends at the sum, in exactly one row` |

## More decisions other streams must know

- **The cart depends on the order domain, never the reverse.** The cart needs
  live catalogue data and the order needs cart rows; pointing both arrows makes
  the module graph circular. So: the catalogue seam is re-exported from
  `order/index.ts` and the cart imports it from there; the two `cart_items`
  statements checkout needs (`selectCartLines`, `deleteCartLines`) live in
  `order.repo.ts`; `cart.rebuy` calls `order.rebuyLines`. When stream A lands a
  real `catalog` domain, both import that instead and the coupling disappears.
- **`ctx.platform` is translated at the boundary.** The contracts speak
  `'wechat-mini' | 'wechat-oa' | 'h5'`; the `orders_platform` enum spells the
  first two with underscores. `order.checkout.service.ts` holds the one
  translation map. **B2 and C store the same column** — use the same map rather
  than writing a second one.
- **An unsellable cart row is shown, never silently dropped**, with a `state`
  (`ok` / `off_shelf` / `deleted` / `out_of_stock` / `limit`) the storefront
  greys out. 全选 ticks only sellable rows; 全不选 clears everything, dead rows
  included.
- **Cart prices are read live through the catalogue port**, never stored on the
  row. Legacy's `cart_info` JSON blob is not reproduced.
- **`preview` writes nothing.** It takes no lock and reserves no stock, so two
  shoppers can both be shown the last unit; `create` is where the loser finds
  out (`ORDER_OUT_OF_STOCK`). That is deliberate — reserving at preview lets
  anyone freeze a catalogue by opening confirm pages.
- **The purchase-limit check is advisory.** `per_order` and `lifetime` limits
  are read-then-write, so a determined buyer submitting twice at once can beat
  them by one order. The limits are a merchandising rule, not an invariant;
  stock is the invariant and stock is conditional. If a limit ever has to be
  hard, it needs its own counter row and a conditional update.
- **Auto-cancel is enqueued after the creating transaction commits**, so a
  rolled-back order never leaves a job behind.

## Interfaces assumed from other streams

| Stream | What B1 assumes | What B1 does until it lands |
| --- | --- | --- |
| A (catalogue) | A `CatalogPort` with `getSkusForSale(db, skuIds)` and a `StockPort` with `reserve` / `release` / `commit`. | `order/catalog.repo.ts` implements both against `products` / `product_skus` and registers them as **fallbacks**, so A's real port wins the moment it registers one. A's `reserve` should key on the order id so `release` is idempotent per order; B1's is made safe by the caller instead (the conditional transition). |
| C (payment) | `PaymentPort.ensureNoOpenAttempts(tx, orderId)` → `'closed' \| 'paid' \| 'unknown'`, answered while B1 holds the order's row lock. | `resolvePaymentPort()` returns `null` and cancel treats that as `closed`. Sound only because no payment domain is loaded, so no money can arrive; the moment C registers a port this stops being a shortcut. |
| F2 (freight) | `FreightPort.quote(...)` for `freightMode: 'template'`. | `resolveFreightPort()` returns `null` and `fallbackFreightQuote` honours `free` and `fixed` (charged **once per product**, not per line or per unit) and quotes `template` at **zero**. Every template-freight order is under-charged until F2 lands — this is the one knowingly wrong number in the stream. |
| D / activities | `OrderKindHandler` for group-buy, seckill, presale. | An order whose `kind` has no registered handler is refused at create. Only `kind: 'normal'` works today. |
| Points / marketing | `PricingContributor` for points, member pricing, full-reduction. | The pipeline calls every registered contributor in priority order and splits its adjustments across the lines; nothing is registered, so the only discount is the coupon. |

## Change requests filed

| CR | About | Status |
| --- | --- | --- |
| CR-1-b1 | `orders` needs an `idempotency_key` column plus a partial unique index. | Local adapter in place: the claim is a `status='done'` row in the `effects` ledger, whose `UNIQUE (scope, scope_id, event_type)` is the same constraint. Three repo functions change when the column lands. |
| CR-2-b1 | *(withdrawn)* `ORDER_STATUSES` did not match the `orders_status` enum. | Fixed upstream in `b916be26` before the CR was written. |
| CR-3-b1 | `orders.coupon_discount` holds **every** goods-level discount, not only the coupon. | Implemented that way; asks for a comment fix and an ack from B2 and C. |
| CR-4-b1 | PRICE-001, PRICE-002, STOCK-004 and QUEUE-008 sit in B1's invariant sections but belong to the points and activity streams. | Needs an ownership decision before gate K. |
| CR-5-b1 | `pnpm test:unit` is red on `rewrite/integration`: the mock server never sends a route's example query, so C's `GET /api/v1/refunds/applicable-items` 422s. | Not B1's code; reproduces on `rewrite/integration` untouched. One-line fix suggested. |

## Dependencies added

**None.** No package was added to any `package.json`; `pnpm-lock.yaml` is
untouched.

## Verification

Run from `next/` at `fc2aba93` (plus the doc commit that follows it).

```
$ pnpm gen
 Tasks:    3 successful, 3 total

$ pnpm typecheck
 Tasks:    10 successful, 10 total

$ pnpm lint
@shop/web:lint: eslint: 139 files, 0 errors, 0 warnings
 Tasks:    11 successful, 11 total

$ pnpm --filter @shop/contracts check:examples
contracts: 126 route(s) OK, every example parses.

$ pnpm test:int
@shop/web:test:int:  Test Files  3 passed (3)
@shop/web:test:int:       Tests  34 passed (34)
@shop/core:test:int:  Test Files  10 passed (10)
@shop/core:test:int:       Tests  225 passed (225)
 Tasks:    10 successful, 10 total

$ pnpm build
 Tasks:    5 successful, 5 total

$ pnpm exec prettier --check .
[warn] packages/core/src/order/ports.test.ts
[warn] packages/testing/src/fakes.test.ts
[warn] Code style issues found in 2 files. Run Prettier with --write to fix.
```

Two things in that output are **not green**, and neither is B1's:

1. `pnpm test:unit` **fails**, in `@shop/testing`:

   ```
    FAIL  |unit| src/mock-server/mock-server.test.ts > serving examples > serves every registered route from its first example
   AssertionError: GET /api/v1/refunds/applicable-items: expected 422 to be 200

    Test Files  1 failed | 2 passed (3)
         Tests  1 failed | 47 passed (48)
   ```

   Everything else passes, run package by package: `@shop/core` 146,
   `@shop/web` 146, `@shop/worker` 12, and 47 of the 48 in `@shop/testing`.
   See CR-5-b1 — both files involved arrived in `b916be26` and B1
   modifies neither.

2. The two prettier warnings are `packages/core/src/order/ports.test.ts` and
   `packages/testing/src/fakes.test.ts`, also from `b916be26`. B1 formatted
   every file it owns and deliberately did not reformat those two, because they
   belong to another stream and a whitespace-only diff would collide.

