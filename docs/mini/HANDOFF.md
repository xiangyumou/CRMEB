# 小程序重写：进度交接（2026-09-24）

给接手的会话（本地或云端）看的。读完这一页，再按「先读」列表看其他文档，就能接着干。

## 1. 一句话现状

写代码的部分约完成 **80–85%**：阶段 0–3 全部合入，现在进入收尾（安全审查、体积性能、客户端复查、发布准备）。之后是切换，切换的每一步都要用户单独批准。

- 集成分支：`storefront/mini`，@ `247a8f220`。比 `master` 多 260 个提交，**只在本地，没有推送**。
- `master` 等于线上，只修线上紧急问题。开发不在 `master` 上做。

## 2. 最近一次全套检查（@ 247a8f220，全部通过）

| 检查                                             | 结果          |
| ------------------------------------------------ | ------------- |
| `prettier --check .`                             | 通过          |
| `turbo run typecheck lint build --concurrency=4` | 35/35         |
| `turbo run test:unit --concurrency=2`            | 16/16         |
| `pnpm guards`                                    | 15 项，0 失败 |
| `@shop/core test:int`                            | 1546 条通过   |
| `@shop/web test:int`                             | 331 条通过    |
| `e2e/storefront test:mini --workers=1`           | 45 条通过     |

旧 uni-app 的检查（storefront e2e 34 条、uni-app 单测 477 条）和后台 e2e（50 条），上次在 D 合并后跑过，都通过。

## 3. 已合入集成分支的内容

按合并顺序：

| 批次   | 内容                                                                                                                                             |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 阶段 0 | S1 Taro、S2 api-client、S3 Puck 试点、S4 H5 模拟小程序 e2e、S5 合规文档                                                                          |
| 批 1   | R0 路由目录、A 小程序骨架、F1 装修后端、H1/H2 后端（发货信息管理、内容安全）、I1 `mini` 守卫                                                     |
| 批 2   | B 购物、C 订单售后、F2 Puck 编辑器、G1 前 14 个块、H3 后端补充                                                                                   |
| 批 3   | D 营销（拼团、预售、优惠券、海报）、E 账户、A2/A3 打磨、G2 另外 8 个块、H4 后端补充、I2 e2e、invariants、文档                                    |
| 跟进   | J1 订单侧（修好预售价格、待评价、快递搜索）、J3 密码登录、隐私 e2e、J2 商品侧、海报开关、装修宿主、H5 后端（发货消息、装修脱离旧 diy、单测提速） |

每个流的细节在 `docs/mini/status/<流>.md`。

## 4. 正在跑、还没合并的执行者

每个执行者都有自己的 worktree，位于 `/home/xiangyu/Projects/CRMEB-mini-wt/<任务>/`，分支是 `storefront/mini-<任务>`，都从 @ `247a8f220` 拉出。任务说明的公共规则在 `docs/mini/executor-rules.md`。

| 任务             | 内容                                                                                                            |
| ---------------- | --------------------------------------------------------------------------------------------------------------- |
| K1-security      | 对 `master...storefront/mini` 的全部改动做安全审查。小问题直接修；大问题写报告；发现线上漏洞时标 **PRODUCTION** |
| H6-auth-bind     | `auth.passwordLogin` 增加可选的 `bindToken`，让密码登录也关联微信 openid；隐私被拒绝时提示中文                  |
| K2-size-perf     | 小程序包体积和运行性能；确认 demo 分包不进生产包                                                                |
| K3-client-review | 小程序客户端正确性复查：缓存失效、重复提交、页面栈、空状态、计时器                                              |
| R1-release-prep  | 商城 API 兼容守卫（先只报告不拦截）；`/` 落地页（先不接入 edge）                                                |

**如果换到云端：** 这些分支只在本机。要么等它们做完、合进 `storefront/mini` 后再迁移；要么把它们的分支一起推送（需要用户批准）。每个执行者都会把进度写进自己的 `docs/mini/status/<任务>.md`，接手时从那里继续。

## 5. 接下来要做的

1. 合并上面 5 个分支（`--no-ff`），然后按第 6 节跑**一次**全套检查。
2. 修复 K1、K3 写出来的问题，并跑执行者列出的集成测试和 e2e。
3. 切换，方案见 `docs/mini/cutover.md`：删除 uni-app、旧装修、辅助接口、店员接口，改 CI、edge、守卫和文档，然后合回 `master`。
   - 合并到 `master`、push、部署、上传小程序，**每一步都要用户单独批准**。
4. 真机检查，清单见 `docs/mini/device-check.md`，由人执行。

## 6. 规矩（用户明确要求过）

- **机器负载：** 用户的开发机（WSL，14900K，31 GB）曾经因为并行跑测试宕机。
  - 执行者最多 5 个，但只跑自己改动相关的类型检查、lint 和单元测试。
  - 全套检查、集成测试和 e2e 只由协调者来跑：一次只跑一个，限制并发（turbo `--concurrency=4`，Playwright `--workers=1`）；一批合并之后跑一次，不是每合并一个就跑。
  - 不在后台留着没跑完的测试。
- **生产：** 主机 `ubuntu@43.142.105.205` 默认只读。超出只读的操作、拉生产数据、任何 `git push`、开 PR，都要先问用户。
- **机密：** 不打印 `eb_system_config` 的任何密钥值。仓库、日志、截图里不能出现生产数据或真实凭据。
  - 小程序 AppSecret 用户在聊天里贴过，**绝不存储、复述或提交**；已建议用户重置。
  - 代码上传密钥用户还没给。以后给的话，只能是仓库外的一个路径，每次预览或上传都要单独批准。
- **外部接口：** 开发和测试只用 `@shop/testing` 的假实现，不调用真实的微信、短信、阿里云接口。
- **不要碰：** 主目录 `/home/xiangyu/Projects/CRMEB`（用户在里面修后端 bug）。
  - 里面有 6 个 G1 误写的草稿文件：`hotspot-image.ts`、`nav-grid.ts`、`notice.ts`、`rich-text.ts`、`spacer.ts`、`title-bar.ts`，等用户自己删。
- **核心业务逻辑不重写**：订单、支付、退款、库存、优惠券、拼团、预售、鉴权。
- **沟通：** 用中文。

## 7. 等用户决定的事

1. **切换方式：** 一次发布，还是 I2 提的三次发布（A：新后端不删东西，先送审；B：删除旧代码、上落地页；C：DROP 旧表）。
2. 公众号模板消息的链接是否改为打开小程序（在 B 之前）。
3. 装修模板里「所有订单均以隐私包装发出，外包装不显示商品信息」这句承诺是否属实。
4. 界面配色：默认品牌色 #E1251B，建议换成更克制的配色。截图在 `docs/mini/status/*-screens/`。
5. 谁做真机测试。
6. master 目录里那 6 个多余文件要不要删。
7. 线上 kindMeta 安全问题：用户决定不在 master 上热修，修复随切换合入；如果 ICP 通过、外网提前开放，要重新提醒用户。

**已定：**

- 原生 tabBar；v1 包含「我的评价」和「我的拼团」；关闭虚拟成团。
- 风险评价进待审核；商品海报加开关，默认开；预售 v1 只做全款。
- 精细首页装修（自由布局）暂时不做。
- 我的评价的状态文字沿用「审核后展示」和「仅自己可见」。

## 8. 上线前需要运营在后台完成的事（摘自 H2）

- 公众平台「消息推送」：URL、Token、EncodingAESKey，JSON 格式，安全模式；这些值同时填进后台的微信设置。
- 每家快递公司填写「微信快递编码」，然后点「同步」。
- 在「待审核」里审核评价。
- 域名必须是 HTTPS 且已备案。
- 先发布小程序版本，再对外发放小程序码。

## 9. 先读

- `docs/mini/README.md`：文档地图和构建方式
- `docs/mini/pages.md`：页面和页面形态改动
- `docs/mini/cutover.md`：切换方案
- `docs/mini/e2e-coverage.md`：测试覆盖
- `docs/mini/decor.md`：装修
- `docs/mini/wechat-compliance.md`、`docs/mini/auth.md`
- `docs/conventions.md`、`docs/contributing.md`、`docs/invariants.md`
- 已批准的完整方案（中文）：`docs/mini/plan.md`
- 执行者公共规则：`docs/mini/executor-rules.md`
