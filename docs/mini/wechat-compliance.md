# 微信小程序合规清单

本文列出**这家店**上线微信小程序必须满足的平台规则，以及每条规则落到代码、后台配置和验证上的具体做法。它是阶段 1–3 各个流（A、B、C、D、E、H2、I1、I2）的输入：

- 页面清单与路由目录见 [pages.md](pages.md)；
- 设计规范见 [design.md](design.md)；
- 总体方案见重写计划第 4 节。

**适用范围（本店的假设）：**

- 企业主体，单商户自营，**不是**电商平台（不入驻第三方商家）。
- 微信支付 v3，普通商户直连模式（非服务商），`payment.start` 的 `wechat_mini` 通道。
- 实物商品走快递；目录里还有 `virtual_card`、`virtual_coupon`、`virtual_manual` 三种虚拟商品类型（`packages/contracts/src/catalog/schemas.ts:26`）。
- 营销：优惠券、拼团、预售（只允许全款预售，定金预售在契约里被拒绝，见 `presale/schemas.ts`）。
- 带图评价；人工开具发票（没有电子发票服务商）。
- 旧 H5 不对外；小程序是唯一的顾客端。

**验证方式记号：**

- **[守卫]**：`mini` 守卫（I1 流，`guards/src/checks/mini.ts`）静态检查，CI 必过；
- **[测试]**：单元、集成或契约测试；
- **[E2E]**：Taro H5「模拟小程序」模式下的 Playwright 用例；
- **[人工]**：真机清单或公众平台后台核对，由人执行。

**来源说明：** 文中链接是 2026-09 查阅的微信开放文档。文档没写清楚、只在开放社区有说明的数字，都标了「社区」；这些数字在联调时要以接口的实际返回为准。

---

## C01 主体与微信认证

| 项       | 内容                                                                                                                                                                                                                                                                                                    |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 卖货必须是企业（或个体工商户）主体，并完成微信认证。以下能力都依赖认证：微信支付、手机号快速验证（只对「非个人主体且已认证」开放）、web-view（个人主体不能用）、`chooseAddress` 接口权限。                                                                                                              |
| 来源     | [服务类目（个人主体基本不能卖货）](https://developers.weixin.qq.com/miniprogram/product/material/)、[手机号快速验证](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/getPhoneNumber.html)、[web-view](https://developers.weixin.qq.com/miniprogram/dev/component/web-view.html) |
| 代码     | 无。                                                                                                                                                                                                                                                                                                    |
| 运营配置 | 注册小程序时就用企业主体（主体注册后**不能改**）。公众平台 →「设置 → 基本设置 → 微信认证」提交认证。认证有年审，费用以平台当时公示为准。                                                                                                                                                                |
| 验证     | [人工] 公众平台首页显示「已认证」，主体名称和营业执照、商户号主体一致。                                                                                                                                                                                                                                 |

## C02 服务类目与资质（含拼团、预售、虚拟商品）

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 1. 类目必须和实际卖的东西一致（拒绝情形 2.1）。单商户自营选「**商家自营**」下与商品匹配的二级类目，**不要选「电商平台」**：后者要《增值电信业务经营许可证》（在线数据处理与交易处理，即 EDI），还要平台规则和商家审核制度，本店不需要。<br>2. 特殊商品要前置资质：食品饮料要《食品经营许可证》或《食品生产许可证》或预包装食品备案；酒类、保健品、母婴食品、医疗器械、成人用品、珠宝都有单独要求。<br>3. 类目里的每项服务都必须能从首页两次点击内到达（拒绝情形 3.1.2）。<br>4. **拼团、预售**：没有单独的类目，是交易玩法。但要遵守：分享规则（见 C10）；「不得虚假宣传、虚假交易」（拒绝情形 3.2.5）；预售商品必须写明发货时间。<br>5. **虚拟商品**：卡密、券码这类商品在小程序里卖，需要有覆盖它们的类目（例如商家自营下的卡券类，以审核时类目表为准）；发货信息要用 `logistics_type=3` 录入（见 C07）。它们**不属于**虚拟支付（见 C17）。 |
| 来源     | [服务类目与资质](https://developers.weixin.qq.com/miniprogram/product/material/)、[常见拒绝情形](https://developers.weixin.qq.com/miniprogram/product/reject.html)、[运营规范](https://developers.weixin.qq.com/miniprogram/product/)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 代码     | - **后端**：「虚拟成团」已**彻底关闭**（2026-09-23 决定，H2 流）：`groupbuyConfig` 不再有 `virtualFillOnExpiry` 开关，到期未满员的团一律失败并原路退款；后台「立即成团」拒绝未满员的团；迁移 `0005_groupbuy_virtual_fill_off` 删除已存的开关值（RISK-D-006）。团详情页**不展示**虚拟成员（历史上虚拟成团的团也一样）。<br>- **客户端**：预售详情和结算页必须显示 `shipAfterDays`（「付款后 N 天内发货」）；商品详情的划线价必须标注「划线价」并给出说明入口（依据《明码标价和禁止价格欺诈规定》），样式见 design.md 的 `Price` 组件。<br>- **客户端**：首页的 DIY 默认模板要直接放出拼团、预售、领券的入口，保证「两次点击内到达」。                                                                                                                                                                                                          |
| 运营配置 | 公众平台 →「设置 → 基本设置 → 服务类目」添加类目并上传资质。最多 5 个类目，每月修改次数有限，以后台提示为准。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 验证     | [人工] 类目页显示「审核通过」。[E2E] 首页默认模板能在两次点击内进入拼团列表、预售列表、领券中心。[测试] 预售详情页组件测试断言显示发货时间。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## C03 小程序备案、ICP 与服务器域名

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 要求     | 1. **小程序备案**：2023-09-01 起，新小程序必须先完成备案才能上架。这和域名的 ICP 备案是两件事，两件都要办。<br>2. **服务器域名**（request、uploadFile、downloadFile，按类型分别配置）：只能用 `https`/`wss`；TLS 1.2 及以上；不能是 IP 或 localhost；**域名必须已经 ICP 备案**；不能配置父域名，要配具体子域名；配置了端口就只能用那个端口。<br>3. **业务域名**（web-view 用）：要在域名根目录放微信给的校验文件，见 C12。<br>4. 并发限制：request、uploadFile、downloadFile 合计最多 10 个并发，默认超时 60 秒。<br>5. 开发者工具的「不校验合法域名」只对开发版和体验版生效。 |
| 来源     | [网络 · 服务器域名配置](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/network.html)、[小程序备案说明（社区公告）](https://developers.weixin.qq.com/community/develop/article/doc/00060696d3c0a851fc30af38761013)、[备案常见问题（社区）](https://developers.weixin.qq.com/community/develop/article/doc/000ac251a9c340df3e6073ee566c13)                                                                                                                                                                                                                   |
| 代码     | - **客户端**：接口源和资源源来自构建时的环境变量（`TARO_APP_API_ORIGIN`，名字由 A 流确定），**不能写死**。所有请求都经过 `packages/api-client`，由它排队，保证并发不超过 10（留 2 个给图片下载和上传）。<br>- **后端**：`/uploads/` 和 API 用同一个已备案域名（edge 已经这样部署），这样 request、uploadFile、downloadFile 三类只需配一个域名。如果以后换成 S3 或 CDN，CDN 域名也要备案，并加到 downloadFile 合法域名（海报画布用 `getImageInfo`/`downloadFile` 加载商品图时需要）。<br>- **edge**：给业务域名校验文件留一个根路径位置（见 C12）。                             |
| 运营配置 | 1. 云厂商处给域名做 ICP 备案。<br>2. 公众平台 →「设置 → 基本设置 → 小程序备案」。<br>3. 公众平台 →「开发管理 → 开发设置 → 服务器域名」：把 request、uploadFile、downloadFile 都配成 `https://<店铺域名>`。每月修改次数有限，一次配好。                                                                                                                                                                                                                                                                                                                                         |
| 验证     | [守卫] `mini` 守卫检查：构建产物里没有 `http://` 开头的接口源；`project.config.json` 的 `urlCheck` 为 `true`。[人工] 真机清单第一项：关闭「不校验合法域名」后，体验版能登录、下单、上传评价图、生成海报。                                                                                                                                                                                                                                                                                                                                                                      |

## C04 隐私保护指引、`onNeedPrivacyAuthorization`、`requiredPrivateInfos`

**要求：**

- 小程序只能调用「用户隐私保护指引」里声明过的隐私接口。没声明的接口直接报错（errno 112）。
- 用户同意之前调用隐私接口，会触发 `wx.onNeedPrivacyAuthorization`。开发者要弹自己的隐私弹窗，用户点击同意后调用 `resolve({ event: 'agree', buttonId })`，拒绝时调用 `resolve({ event: 'disagree' })`。同意按钮必须是 `<button open-type="agreePrivacyAuthorization">`，它可以和其他 open-type 合并，例如 `getPhoneNumber|agreePrivacyAuthorization`。
- 这套机制从 2023-10-17 起默认开启，需要基础库 ≥ 2.32.3，`__usePrivacyCheck__` 已无必要。
- `wx.getPrivacySetting` 返回 `needAuthorization` 和 `privacyContractName`；`wx.openPrivacyContract` 打开平台托管的指引页。
- 指引的每次修改都要平台审核。
- `requiredPrivateInfos` 只管地理位置类接口和 `chooseAddress`。自 2022-07-14 起，没声明就不能用。

**来源：**
[隐私协议开发指南](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/PrivacyAuthorize.html)、
[隐私保护指引内容与接口对照](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/miniprogram-intro.html)、
[app.json · requiredPrivateInfos](https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html)、
[wx.chooseAddress](https://developers.weixin.qq.com/miniprogram/dev/api/open-api/address/wx.chooseAddress.html)

**本店用到的隐私接口，以及指引必须声明的条目**（这是单一事实来源。A 流在 `apps/mini/src/platform/privacy.ts` 导出同内容的常量 `PRIVACY_APIS`，守卫拿它对照）：

| 指引条目（公众平台里的名称） | 我们调用的接口/组件                                        | 用途（写进指引的文字）                 | 调用位置（页面）                        |
| ---------------------------- | ---------------------------------------------------------- | -------------------------------------- | --------------------------------------- |
| 收集你的昵称、头像           | `button open-type="chooseAvatar"`、`input type="nickname"` | 设置个人资料，在拼团成员、评价中展示   | 个人资料                                |
| 收集你的手机号               | `button open-type="getPhoneNumber"`                        | 登录、绑定账号，接收订单与售后通知     | 登录、绑定手机号、个人资料              |
| 收集你的通讯地址             | `wx.chooseAddress`                                         | 快速填写收货地址                       | 地址列表、地址编辑、确认订单            |
| 收集你的发票信息             | `wx.chooseInvoiceTitle`                                    | 快速填写发票抬头                       | 发票抬头编辑、申请开票                  |
| 收集你选中的照片或视频信息   | `wx.chooseMedia`（`mediaType: ['image']`）                 | 上传评价图片、售后凭证、头像           | 写评价、申请售后                        |
| 访问你的摄像头               | `wx.chooseMedia` 的 `sourceType: ['camera']`               | 拍摄评价图片和售后凭证                 | 写评价、申请售后                        |
| 使用你的相册（仅写入）权限   | `wx.saveImageToPhotosAlbum`                                | 保存商品和拼团海报                     | 商品详情、拼团进度（海报弹层）          |
| 使用你的剪切板               | `wx.setClipboardData`                                      | 复制订单号、运单号、退货地址、外部链接 | 订单详情、物流、售后详情、web-view 兜底 |

**本店不用：** 位置、微信运动、蓝牙、麦克风、通讯录、日历、`chooseMessageFile`、`getClipboardData`。

旧 uni-app 的 `manifest.json` 里声明了 `getLocation`、`onLocationChange`、`startLocationUpdateBackground` 和 `scope.userLocation`，这些是门店距离功能的遗留，**新小程序不能照抄**：声明了却不用，审核会问「用途」。

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | - **客户端（A 流）**：`app.config.ts` 里写 `requiredPrivateInfos: ['chooseAddress']`，此外不声明任何项。<br>- `app.tsx` 启动时注册一次 `Taro.onNeedPrivacyAuthorization`，把 `resolve` 存进 `platform/privacy.ts` 的单例，然后打开全局 `<PrivacySheet>`（在主包里）。弹层文字写「为提供 xx 服务，需要……」，提供「查看《隐私保护指引》」（`openPrivacyContract`）、「拒绝」和「同意」三个按钮，同意按钮是 `open-type="agreePrivacyAuthorization"` 并且 `id="privacy-agree"`。<br>- 登录页的手机号按钮用 `open-type="getPhoneNumber\|agreePrivacyAuthorization"`，用户点一次就同时完成隐私同意和手机号授权。<br>- 用户拒绝后，**只**让当前这个功能不可用，并给出替代方案：选地址被拒就手填，选头像被拒就用默认头像。**不能**退出小程序或阻断浏览（运营规范 15.1.3）。<br>- 页面**只**通过 `platform/` 调用上表的接口。 |
| 运营配置 | 公众平台 →「设置 → 服务内容声明 → 用户隐私保护指引」，按上表逐项填写，信息保存期限写「至用户注销账号」。「开发管理 → 开发设置 → 接口设置」里申请开通 `wx.chooseAddress`（需要已认证、类目符合）。指引修改后要等审核通过，才能发布用到新接口的版本。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 验证     | [守卫] 编译后的 `app.json` 里 `requiredPrivateInfos` 恰好等于 `['chooseAddress']`；在 `apps/mini/src` 中扫描上表接口名，出现在 `platform/` 以外就失败；在 `platform/` 中出现但不在 `PRIVACY_APIS` 里，也失败。[测试] `platform/privacy.test.ts`：先拒绝后同意、多次触发只弹一次、`resolve` 只调用一次。[人工] 真机上「设置 → 隐私 → 清除授权」后，从头走一遍：选地址、选头像、手机号登录、保存海报、复制运单号。                                                                                                                                                                                                                                                                                                                                                                                                     |

## C05 登录与用户信息

**要求：**

- 不能「一进入就要求授权手机号登录」。用户要先能浏览，自己决定何时登录；登录页必须能取消、能返回（运营规范 15.1.3，以及社区中常见的审核驳回说明）。
- 手机号快速验证：
  - 自 2023-08-28 起按次收费，每次成功调用 **0.03 元**；每个小程序有 1000 次免费体验额度；
  - 资源用完时返回 `errno 1400001`；
  - 前端拿到的 `code` 有效期 5 分钟，只能消费一次。
- 头像、昵称：
  - `getUserProfile`/`getUserInfo` 已不再返回真实头像和昵称，改用「头像昵称填写能力」：`button open-type="chooseAvatar"` 返回一个临时文件路径，`input type="nickname"`；
  - 两者从基础库 2.21.2 起支持；
  - 从 2.24.4 起，平台会对选中的头像和失焦时的昵称做安全检测，不通过就不回调或清空输入。

**来源：**
[手机号快速验证组件](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/getPhoneNumber.html)、
[头像昵称填写](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/userProfile.html)、
[运营规范 15.1.3](https://developers.weixin.qq.com/miniprogram/product/)、
[社区：只支持手机号登录被驳回](https://developers.weixin.qq.com/community/develop/doc/0002ecbd93cac85097f7683a85f400)

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | **启动时静默登录（A 流 `platform/session.ts`）：**<br>- `Taro.login()` 拿到 `code`，调用 `auth.miniLogin`；<br>- 返回 `signed-in` 就保存 session；<br>- 返回 `phone-required` 就只在内存里保存 `bindToken`（10 分钟过期），用户继续匿名浏览，**不跳转**登录页；<br>- 失败不提示。<br><br>**需要登录的动作**（加购、立即购买、收藏、领券、写评价、进入「我的订单」等）先调用 `requireLogin(redirect)`，再打开 `pages/login/index`。浏览类接口（`public`、`user-optional`）一律不要求登录。<br><br>**登录页（B/E 流）的三种方式：**<br>1. 主按钮「手机号快速登录」：`open-type="getPhoneNumber\|agreePrivacyAuthorization"`，调用 `auth.miniPhoneLogin { bindToken, phoneCode }`。如果 `bindToken` 已过期，先重新调用一次 `auth.miniLogin`。<br>2. 次按钮「短信验证码登录」：`auth.sendSmsCode`，然后调用 `auth.smsLogin`。<br>3. 「其他方式 → 密码登录」：`auth.passwordLogin`。<br><br>三种方式下方都有**默认不勾选**的「我已阅读并同意《用户协议》《隐私政策》」（链接到 `agreement` 路由）。没勾选时，点击按钮先弹出同意框，不直接报错。收到 `errno 1400001` 时，自动切换到短信方式并提示「请使用短信验证码登录」。<br><br>**后端缺口（H1 流）：**<br>- `auth.smsLogin` 和 `auth.passwordLogin` 现在都**不接收 `bindToken`**，所以用短信或密码登录的小程序用户**不会绑定 openid**，之后支付会报 `PAYMENT_OPENID_REQUIRED`，订阅消息也发不出去。需要给这两个接口加可选的 `bindToken`，或者新增 `auth.miniSmsLogin`；另外 `auth.miniLogin` 返回 `signed-in` 时，要能把 openid 绑到已登录账号。<br>- 头像：`chooseAvatar` 拿到临时路径，用 `storage.userUpload?purpose=avatar` 上传，再调用 `user.updateProfile { avatarUrl, nickname }`。<br><br>**个人资料页：** 昵称用 `type="nickname"` 输入框，头像用 `chooseAvatar` 按钮，都**不强制**填写。未设置时显示默认头像和手机号掩码（服务端已经把新用户昵称设成 `137****7000`）。 |
| 运营配置 | 公众平台 →「费用管理」（资源包入口以后台为准）：给手机号快速验证购买资源包，设置余额提醒。1000 次体验额度只够测试用。开通短信签名和模板（后台「短信设置」），保证短信登录作为兜底可用。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 验证     | [E2E] 未登录时能浏览首页、分类、商品详情、领券中心、文章，点「加入购物车」才进入登录页，登录后回到原页面（`redirect`）。[E2E] 在模拟小程序模式下，让假网关对 `getPhoneNumber` 返回 1400001，页面自动切到短信登录。[测试] H1 的集成测试：短信登录带 `bindToken` 后，`wechat_identities` 里有该用户的 mini openid。[守卫] `apps/mini/src` 中不能出现 `getUserProfile`、`getUserInfo`。[人工] 审核前自查：冷启动不弹任何授权。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## C06 支付（`wx.requestPayment`、商户号关联、退款）

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 1. `wx.requestPayment` 的参数是 `timeStamp`（字符串）、`nonceStr`、`package: 'prepay_id=…'`、`signType: 'RSA'`（v3）和 `paySign`。<br>2. **`success` 回调不代表支付成功**，必须以服务端查询结果为准；用户取消时返回 `requestPayment:fail cancel`。<br>3. 小程序 AppID 必须和商户号关联。<br>4. 退款必须原路退回，不能强制退到余额、储值卡、积分或代金券（运营规范 14.6）。<br>5. 实物交易不能引导到小程序外支付。                                                                                                                 |
| 来源     | [wx.requestPayment](https://developers.weixin.qq.com/miniprogram/dev/api/payment/wx.requestPayment.html)、[运营规范 5.13 / 14.6](https://developers.weixin.qq.com/miniprogram/product/)                                                                                                                                                                                                                                                                                                                                           |
| 代码     | - **客户端**：`platform/pay.ts` 调用 `payment.start { channel: 'wechat_mini' }`，把返回的 `jsapi` 原样传给 `Taro.requestPayment`。不管回调成功、失败还是取消，**都跳到支付结果页**，由结果页轮询 `payment.status`（1s、2s、3s…，最多约 15 秒），拿到 `paid: true` 才算成功；超时就显示「支付结果确认中」，并提供「刷新」和「查看订单」。<br>- 同一订单重复点击时，服务端会复用已打开的支付单（PAYC-003），客户端不用做额外去重，只需把按钮置灰防止连点。<br>- **后端**：已完成（v3 JSAPI/小程序下单、回调验签、对账、原路退款）。 |
| 运营配置 | 1. 商户平台 →「产品中心 → AppID 账号管理」关联小程序 AppID。<br>2. 公众平台 →「功能 → 微信支付」确认关联。<br>3. 商户平台开通「JSAPI 支付」（小程序支付用它）。<br>4. 后台「支付设置」填入商户号、APIv3 密钥、商户证书序列号和私钥、平台证书或公钥；小程序 AppID 必须等于支付配置里用的 AppID。                                                                                                                                                                                                                                   |
| 验证     | [E2E] 模拟小程序模式下，假支付网关走通「下单 → 支付 → 结果页显示成功」，并覆盖「取消后结果页显示未支付，能重新支付」。[测试] 现有的 PAYC-* 集成测试不动。[人工] 真机用 0.01 元商品完成一次真实支付和一次全额退款，确认资金原路返回。                                                                                                                                                                                                                                                                                              |

## C07 小程序发货信息管理服务（必做，影响资金结算）

**要求：**

- 被纳入「发货信息管理」的小程序，用户付款后**资金默认冻结**。只有在平台录入发货信息，并完成确认收货（手动或自动）后，资金才能结算。
- 电商小程序基本都会被纳入。可以用 `is_trade_managed` 查询是否已纳入；被纳入时平台会推送 `trade_manage_remind_access_api`。
- 已经发过货的小程序，订单超过 48 小时未发货时，平台会推送 `trade_manage_remind_shipping` 提醒。

**来源：**
[发货信息管理服务](https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/order-shipping/order-shipping.html)、
[确认收货组件接入说明](https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/order-shipping/order-shipping-half.html)、
[发货信息录入接口参数（开放平台版）](https://developers.weixin.qq.com/doc/oplatform/en/openApi/OpenApiDoc/miniprogram-management/shopping-orders/uploadShippingInfo.html)、
[社区：logistics_type 取值与入参示例](https://developers.weixin.qq.com/community/develop/doc/000ea8bc45cb7091fc500e48d61000)、
[社区：get_order 的 order_state=6](https://developers.weixin.qq.com/community/develop/doc/0006640de9cf407b3ba26777e61800)、
[社区：set_msg_jump_path](https://developers.weixin.qq.com/community/develop/doc/00000ea01f85f06cece2833fc6bc00)

**涉及的接口（`https://api.weixin.qq.com` + 路径，都带 `access_token`）：**

| 接口                                       | 作用                         | 本店怎么用                                                                                                                                                                                                                                                           |
| ------------------------------------------ | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /wxa/sec/order/upload_shipping_info` | 录入发货信息                 | 每个**支付单**（一个 `transaction_id` 或一对 `mchid`+`out_trade_no`）录入一次。字段映射见下表。                                                                                                                                                                      |
| `POST /wxa/sec/order/get_order`            | 查询单个订单的发货和结算状态 | `order_state`：1 待发货、2 已发货、3 确认收货、4 交易完成、5 已退款、6 资金待结算（社区）。确认收货组件回调成功后，**必须**再查一次（官方安全要求）。                                                                                                                |
| `POST /wxa/sec/order/set_msg_jump_path`    | 设置发货和结算消息的跳转页   | 运营在后台「系统设置 → 小程序发货信息管理」点「同步」时设置（不在保存配置时自动调用，见下文「实现」），`path` 为 `packages/order/detail/index?outTradeNo=${商品订单号}`。`${商品订单号}` 由微信替换为 `out_trade_no`。路径从路由目录的 `order` 生成（见 pages.md）。 |
| `POST /wxa/sec/order/is_trade_managed`     | 查询是否已纳入               | 同一个「同步」按钮查询并显示；收到 `trade_manage_remind_access_api` 推送时也会自动记为「已纳入」。不设每日 worker 任务。                                                                                                                                             |
| 消息推送 `trade_manage_order_settlement`   | 确认收货或结算通知           | 字段包括 `transaction_id`、`merchant_trade_no`、确认收货方式（1 手动，2 自动）、确认收货时间、结算时间。收到后，如果订单还是 `shipped`，就按「平台已确认收货」推进到 `received`（走状态机和条件更新）。                                                              |
| 消息推送 `trade_manage_remind_shipping`    | 48 小时未发货提醒            | 转成后台通知（新事件 `admin_shipping_overdue`，按 `order:shipment:write` 权限分发）。                                                                                                                                                                                |
| 消息推送 `trade_manage_remind_access_api`  | 已被纳入发货管理             | 写日志，并给后台发一条通知。                                                                                                                                                                                                                                         |

**`upload_shipping_info` 字段映射（H2 流实现，写在 `packages/core/src/wechat/wechat.shipping.ts`，经 effects ledger 调用）：**

| 微信字段                                          | 取值                                                                                                                                                                                                                                                          |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `order_key.order_number_type`                     | `2`，用微信支付单号：`transaction_id` 取 `payment_attempts` 里已支付的那条。拿不到时用 `1`，即 `mchid` + `out_trade_no`。                                                                                                                                     |
| `logistics_type`                                  | 快递 `1`（实体物流配送）；商家配送（`merchant_delivery`）`2`（同城配送）；`virtual_*` 商品 `3`（虚拟商品，卡密或券码发出后立即录入）。本店没有自提，`4` 不用。                                                                                                |
| `delivery_mode`                                   | 1 个包裹发完全部商品：`1`（UNIFIED_DELIVERY，不带 `is_all_delivered`）；拆单发货：`2`（SPLIT_DELIVERY），每个发货单录入一次，最后一个发出时 `is_all_delivered: true`。微信只允许**快递**拆单（否则 `10060006`），所以拆单里的非快递部分不单独录入，只记日志。 |
| `shipping_list[].tracking_no` / `express_company` | 运单号；快递公司编码要用微信的编码（`get_delivery_list` 返回的 `delivery_id`）。快递公司表已加 `wechat_delivery_id` 列（迁移 `0005`），后台「快递公司」页可编辑；**没填时不会乱填**，这单的录入会等待并重试，填好后自动发出。                                 |
| `shipping_list[].item_desc`                       | 商品名 × 数量，拼成字符串后截断到 120 字（社区给出的上限）。                                                                                                                                                                                                  |
| `shipping_list[].contact`                         | 快递公司是**顺丰**时必填：`receiver_contact` 用收件人手机号的掩码形式（只保留后 4 位）。                                                                                                                                                                      |
| `upload_time`                                     | 发货时间，RFC 3339 带时区。                                                                                                                                                                                                                                   |
| `payer.openid`                                    | 付款人的 mini openid（`payment_attempts.context.openid`）。                                                                                                                                                                                                   |

**特殊订单：**

- **拼团失败或预售售罄退款**的订单已经原路退款，微信侧为「已退款」，不需要录入。
- **预售**：一定是全款（契约强制），一笔支付，发货时再录入。预售期长，资金会冻结到确认收货为止，要提醒运营。
- **修改发货信息**的次数有上限（社区说法是只能改一次），所以只在包裹真正发出后才录入；后台「修改运单号」也要同步调用一次，并提示「微信侧只能修改一次」。

**实现（H2，已落地）：**

| 部分               | 位置与行为                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 端口、驱动、假服务 | `packages/core/src/wechat/wechat.shipping.ts`：`MiniShippingPort`（`uploadShippingInfo` / `getOrder` / `setMsgJumpPath` / `isTradeManaged`）和真实驱动；假服务是 `@shop/testing/wechat` 的 `startFakeOaServer()`，按微信的规则拒绝（非快递拆单 `10060006`、顺丰缺掩码手机号、只能改一次 `10060003`、相同内容 `10060023`、已退款 `10060004`）。                                                                                                                                                                                                                                                                                                                                                                               |
| 录入               | `packages/core/src/payment/payment.mini-trade.ts`。发货事务里 `onShipmentDispatched` 钩子只在「小程序支付 + 录入发货信息开关打开」时写 effect `shipment/<id>/wechat.uploadShipping`，统一/拆单在这一刻定下来写进 payload。提交后 handler 调用接口：成功或 `10060002`/`10060023` 视为已录入；`10060003`/`10060004` 记警告后结束；其他错误和网络故障交给 effects ledger 重试（8 次后停放，后台「待处理任务」可重试）。拆单按发货顺序录入：前一单还没录入成功时后一单等待。                                                                                                                                                                                                                                                     |
| 修改运单           | `onShipmentUpdated` 钩子：只有已经录入成功的发货单才写 `wechat.correctShipping`，每个发货单最多一次（effect 唯一键），与微信只能改一次的规则一致；`wechat_trade_orders.corrected_at` 记录已用掉。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 状态表             | `wechat_trade_orders`（迁移 `0005`）：每个小程序支付的订单一行，记录 `uploaded_at`、`all_delivered_at`、`corrected_at`、`confirmed_at` + `confirm_source`（manual / auto / component）、`settled_at`。所有写入都是幂等的。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 消息推送           | `GET/POST /api/v1/webhooks/wechat-mini`（`packages/core/src/wechat/wechat.mini-push.ts`）。先验签（安全模式验 `msg_signature`，包含密文），再检查时间戳 ±5 分钟、签名三元组只能用于一个请求体，再解密并核对 appid；**Token 和 EncodingAESKey 不会写进日志**。消息加解密方式以后台配置为准：安全模式下明文推送直接拒绝。只接受 **JSON** 数据格式。已知事件按解密后内容的 SHA-256 写入 effects ledger（重复推送只处理一次），然后回 `success`；未知事件回 `success` 并丢弃。                                                                                                                                                                                                                                                   |
| 推送处理           | `trade_manage_order_settlement`：有确认收货时间就把 `shipped` 订单推进到 `received`（和用户点确认收货、自动收货走同一个条件更新，谁先谁生效，只留一条收货记录），有结算时间就记 `settled_at`。`trade_manage_remind_shipping`：后台通知 `admin_shipping_overdue`（`order:shipment:write`）。`trade_manage_remind_access_api`：配置记为「已纳入」并发后台通知 `admin_mini_trade_managed`（`payment:config:write`）。                                                                                                                                                                                                                                                                                                           |
| 确认收货组件       | **偏离原计划：** 没有给 `order.detail` 加 `wechatReceipt` 字段，而是单独提供 `GET /api/v1/orders/:id/wechat-receipt`（`payment.wechatReceipt`），返回 `{ receipt: { transactionId } \| null }`。原因：订单详情属于 order 领域，而支付单号属于 payment 领域，单独的接口不需要让 order 反向依赖 payment，也不改动旧 uni-app 在用的订单详情响应。只有登录用户**自己的**订单（别人的订单返回 `ORDER_NOT_FOUND`），且订单已发货、微信侧已录入全部发货时才返回支付单号。`POST /api/v1/orders/:id/receipt` 带 `{ via: 'wechat-component' }` 时，服务端先调用 `get_order`，`order_state` 为 3、4 或 6 才推进；否则返回 `ORDER_WECHAT_RECEIPT_UNCONFIRMED`（`details.verdict` 为 `not-confirmed` 或 `unavailable`），订单保持已发货。 |
| 同步               | `GET /admin-api/wechat-mini-trade` 查看状态，`POST /admin-api/wechat-mini-trade/sync` 同步（权限 `payment:config:write`），页面在「系统设置 → 小程序发货信息管理」。**由运营手动触发，不在保存配置时调用**：需要先填好小程序 AppID/AppSecret；微信的回答（已纳入、未纳入、拒绝）需要有人看到；跳转路径只有在发版改动订单页时才会变，那时正好有人在操作。未配置时返回 `PAYMENT_MINI_NOT_CONFIGURED`，微信拒绝时返回 `PAYMENT_MINI_TRADE_SYNC_FAILED`。                                                                                                                                                                                                                                                                        |
| 配置               | 配置组 `wechat-mini-trade`：只有「录入发货信息」开关（默认开）出现在表单里；是否已纳入、跳转路径和时间是同步写入的隐藏字段。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 规则与测试         | `docs/invariants.md` 的 WXSHIP-001 … WXSHIP-007，测试在 `packages/core/src/payment/payment.mini-trade.int.test.ts`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | **后端（H2 流）：**<br>1. 发货（`order.fulfil`）在同一事务里写 effect `wechat.uploadShipping`（scope = shipment），由 handler 调用接口，失败重试，幂等键是 shipment id。<br>2. 新增小程序消息推送 webhook：`GET/POST /api/v1/webhooks/wechat-mini`（`wechat.miniWebhookVerify` / `wechat.miniWebhookEvent`），支持安全模式（AES）验签和解密，处理 `trade_manage_*` 和 `wxa_media_check`（C09）。<br>3. 契约：`GET /api/v1/orders/:id/wechat-receipt` 返回 `{ receipt: { transactionId } \| null }`（只在「已发货、小程序支付、已录入全部发货」时不为 null；见上文「实现」，这是对原计划 `order.detail.wechatReceipt` 的改动）；给 `order.confirmReceipt` 加可选 body `{ via: 'wechat-component' }`，服务端先调用 `get_order` 核实 `order_state ≥ 3`，再推进状态。<br>4. `set_msg_jump_path` 和 `is_trade_managed` 由后台「同步」按钮调用（不是 worker 启动任务）。<br>5. `@shop/testing` 的假 mini server 要补上这四个接口和推送。<br><br>**客户端（C 流）：** 订单详情和列表里的「确认收货」按钮：<br>- `GET /api/v1/orders/:id/wechat-receipt` 的 `receipt` 不为 null 时，调用 `Taro.openBusinessView({ businessType: 'weappOrderConfirm', extraData })`（基础库 ≥ 2.6.0）；<br>- 回调里 `extraData.status === 'success'` 时，调用 `order.confirmReceipt { via: 'wechat-component' }`，然后刷新订单；<br>- `cancel` 时什么都不做；`fail` 时提示并允许重试；<br>- 同时在 `App.onShow` 的 `referrerInfo.extraData` 里兜底处理同样的结果；<br>- `receipt` 为 null（例如订单不是小程序支付的）时，走原来的 `order.confirmReceipt`。<br><br>**订单详情页**要能用 `outTradeNo` 打开：先调用 `payment.status` 拿到 `orderId`，再加载订单。 |
| 运营配置 | 1. 公众平台 →「开发管理 → 开发设置 → 消息推送」：URL 填 `https://<域名>/api/v1/webhooks/wechat-mini`，填 Token 和 EncodingAESKey，选安全模式和 JSON 格式；这几项同时填进后台「微信公众号 / 小程序」设置（小程序 Token、EncodingAESKey、消息加解密方式）。<br>2. 后台「系统设置 → 小程序发货信息管理」点「同步」：查询是否已纳入，并设置消息跳转路径。公众平台 →「功能 → 发货信息管理」（入口名称以后台为准）也可查看。<br>3. 后台「快递公司」页为常用快递填好微信快递编码（例如顺丰 `SF`；其他快递的编码以微信 `get_delivery_list` 的返回为准）。没填编码的快递，录入会一直等待。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 验证     | [测试] H2 的集成测试：发货后 effect 调用假接口，参数快照包含 `logistics_type`、`delivery_mode`，顺丰时有 contact；拆单时最后一个包裹 `is_all_delivered=true`；推送 `trade_manage_order_settlement` 会把 `shipped` 推进到 `received`，重复推送保持幂等；组件确认时 `get_order` 返回 `order_state=2`，服务端拒绝推进。[E2E] 模拟小程序模式下，假运行时的 `openBusinessView` 返回 success，订单变为「已收货」。[守卫] `apps/mini/src` 中调用 `order.confirmReceipt` 的地方只有 `platform/receipt.ts`。[人工] 真机：真实发货一单，在微信「服务通知」里点发货消息，能打开订单详情；用组件确认收货；在商户平台查到资金已解冻。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## C08 订阅消息

**要求：**

- `wx.requestSubscribeMessage` **必须在点击事件中调用**，每次最多 3 个模板 ID。结果有四种：`accept`、`reject`、`ban`、`filter`；`errCode 20004` 表示用户关闭了总开关。
- 电商类目只能用一次性订阅：用户订阅一次，只能发一条。
- 不能用奖励或其他方式强制、诱导用户订阅（运营规范 5.21）。
- 服务端 `subscribeMessage.send` 的参数是 `touser`、`template_id`、`page`、`miniprogram_state`、`lang`、`data`。字段值有长度限制：`thing` ≤ 20 字，`character_string` ≤ 32，`phrase` ≤ 5，`amount` 和 `time` 有格式要求。

**来源：**
[wx.requestSubscribeMessage](https://developers.weixin.qq.com/miniprogram/dev/api/open-api/subscribe-message/wx.requestSubscribeMessage.html)、
[运营规范 5.21](https://developers.weixin.qq.com/miniprogram/product/)

**本店的订阅场景：** 每个场景最多 3 个模板，都挂在一个点击动作上。模板由运营在「公共模板库」里选，关键词按下表。场景的 key 用于 `app/config` 返回的 `subscribeScenes`，取代现在 `wechatOa.subscribeTemplates` 的四个场景。

| 场景 key           | 触发的点击             | 模板（对应通知事件 `code`）                                                                       | 订阅消息打开的页面（路由目录 key）              |
| ------------------ | ---------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `checkout`         | 确认订单页「提交订单」 | 订单发货通知 `order_shipped`、待付款提醒 `order_unpaid_reminder`、订单取消通知 `order_cancelled`  | `order`                                         |
| `groupbuyCheckout` | 拼团下单「提交订单」   | 拼团成功 `groupbuy_succeeded`、拼团失败 `groupbuy_failed`、订单发货通知 `order_shipped`           | 成功和失败打开 `groupbuyTeam`，发货打开 `order` |
| `presaleCheckout`  | 预售下单「提交订单」   | 订单发货通知 `order_shipped`、预售退款通知 `presale_sold_out`、待付款提醒 `order_unpaid_reminder` | `order`                                         |
| `refundApply`      | 申请售后页「提交申请」 | 退款申请通过 `refund_approved`、退款驳回 `refund_rejected`、退款到账 `refund_settled`             | `refund`                                        |
| `returnShipment`   | 填写退货物流「提交」   | 退款到账 `refund_settled`、退款驳回 `refund_rejected`                                             | `refund`                                        |

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | **客户端（A 流 `platform/subscribe.ts`）：** 只提供 `subscribe(sceneKey): Promise<void>`，并且**必须在 onClick 处理函数里、第一个 `await` 之前调用**。iOS 上，先等网络请求再调用，会被判定为不是用户点击。它从已缓存的 `app/config` 里取模板 ID，去掉空值后最多取 3 个；没有模板就直接 resolve。结果只写入埋点，**不影响**提交流程（用户拒绝也照常下单）。<br><br>页面写法固定为：<br>`onClick = () => { const sub = subscribe('checkout'); submit().finally(() => sub) }`<br><br>**后端（H2 流）：**<br>- `app/config` 按场景返回模板 ID。<br>- `wechatMiniChannelConfig.page`（`packages/contracts/src/notification/schemas.ts:85`）现在是运营手填的路径字符串，要改成由事件决定的路由目录 key 加参数（例如 `{ route: 'order', params: { id: '{{orderId}}' } }`），由 `storefrontRoute` 映射成小程序路径。<br>- `miniprogram_state` 继续由 `notificationConfig.miniProgramState` 决定：体验版用 `trial`，正式版用 `formal`。<br>- 字段超长时按类型截断：`thing` 取前 20 字，`character_string` 只保留允许的字符并取前 32 位。现在的 `renderFields` 没有做这一步，需要补上。 |
| 运营配置 | 公众平台 →「功能 → 订阅消息 → 公共模板库」按上表选模板，把模板 ID 和字段映射填进后台「消息管理」对应事件的「小程序订阅消息」通道。模板关键词要能用现有变量填满，例如「订单号」对应 `{{orderNo}}`，「快递公司」对应 `{{company}}`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 验证     | [守卫] 在 `apps/mini/src` 中，`requestSubscribeMessage` 只能出现在 `platform/subscribe.ts`；`subscribe(` 只能在名为 `on*` 的事件处理函数里调用，由 lint 规则或守卫的 AST 检查实现；场景 key 必须属于契约里的 `subscribeScene` 枚举。[测试] `subscribe.test.ts`：超过 3 个时截断；没有模板时不调用 API。H2 的单测：`thing` 截断到 20，`character_string` 过滤非法字符。[人工] 真机下单时弹出订阅框；拒绝后仍能下单；发货后收到服务通知，点开是订单详情。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## C09 内容安全（评价、昵称、头像、发票抬头）

**要求：**

- 用户生成的内容要做安全审核（运营规范第 10 章）。
- `msgSecCheck`（`POST /wxa/msg_sec_check`）：
  - 用 `version: 2`；`content` 最长 500 字；`scene` 取 1 资料、2 评论、3 论坛、4 社交日志；
  - 必须传 `openid`，而且该用户**2 小时内访问过小程序**；
  - 返回 `result.suggest` 为 `pass`、`review` 或 `risky`，同时带 `label`；
  - 频率上限 4000 次/分钟。
- `mediaCheckAsync`（`POST /wxa/media_check_async`）：
  - 参数 `media_url`、`media_type`（2 为图片）、`version: 2`、`scene`、`openid`（同样要 2 小时内访问过）；
  - 结果在 30 分钟内通过消息推送 `wxa_media_check` 返回，用 `trace_id` 关联；
  - 频率上限 2000 次/分钟。

**来源：**
[msgSecCheck](https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/sec-center/sec-check/msgSecCheck.html)、
[mediaCheckAsync](https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/sec-center/sec-check/mediaCheckAsync.html)、
[头像昵称填写（平台自带检测）](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/userProfile.html)

**策略**（2026-09-23 店主批准的调整，取代计划第 10 节第 10 项中「评价文字不通过就拒绝」的部分）：

本店卖情趣成人用品，`msgSecCheck` 把正常的商品评价判成 `risky` 的情况不少见。拒绝提交等于让真实顾客无法评价，所以**评价文字一律不拒绝**：可疑的评价先存为「待审核」，由人工决定。顾客只看到中性提示「评价已提交，审核后展示」，**不是**错误。同理，微信不可用时评价也进入待审核：检测调用失败，不能让没审过的内容直接上线。

| 内容                                                                       | 接口与 scene                                 | `pass`               | `review`   | `risky`                                                                     | 微信不可用              |
| -------------------------------------------------------------------------- | -------------------------------------------- | -------------------- | ---------- | --------------------------------------------------------------------------- | ----------------------- |
| 评价文字 `catalog.reviewSubmit.content`                                    | `msgSecCheck`，scene 2，同步（事务之前调用） | 按「评价需审核」开关 | **待审核** | **待审核**，不报错                                                          | **待审核**（fail-safe） |
| 昵称 `user.updateProfile.nickname`（仅在昵称改变时）                       | `msgSecCheck`，scene 1，同步                 | 保存                 | 保存       | 拒绝：`USER_NICKNAME_REJECTED`（422）                                       | 保存（fail-open）       |
| 发票抬头名称（抬头簿 `invoiceTitleCreate/Update`、`order.invoiceRequest`） | `msgSecCheck`，scene 1，同步                 | 保存                 | 保存       | 拒绝：`USER_INVOICE_TITLE_REJECTED` / `ORDER_INVOICE_TITLE_REJECTED`（422） | 保存（fail-open）       |
| 评价图片 `images[]`（去重后每张一次）                                      | `mediaCheckAsync`，scene 2，异步             | 保留                 | 保留       | 从该评价的 `images` 中移除这张图；评价本身不变                              | 保留；ledger 重试提交   |
| 头像 `avatarUrl`（仅新上传的图，不含当前头像和默认头像）                   | `mediaCheckAsync`，scene 1，异步             | 保留                 | 保留       | 仍是这张图时恢复默认头像，并发站内信「头像未通过审核」                      | 保留；ledger 重试提交   |
| 售后说明和凭证图                                                           | 不检测                                       | —                    | —          | —                                                                           | 只给商家看，不公开展示  |

昵称和发票抬头在微信不可用时放行，原因：两者都不展示给其他顾客（抬头只到商家和税务），小程序里 `type="nickname"` 的输入框已经由微信检测过一次，而且因为我们依赖的服务故障就拒绝保存资料或开票，是让顾客替我们承担故障。`risky` 仍然拒绝，这是平台的硬性要求。

图片一保存就公开，所以检测天然是事后的（推送最长 30 分钟后才到）。「不可用」的含义是「还没检测」：effect 重试，图片在有结论之前保持可见。

**只检测有小程序身份的账号。** 微信按小程序 openid 检测，且该用户 2 小时内访问过小程序。没有 mini openid 的账号（H5 或公众号用户、后台代建的评价）记为 `skipped`，只写日志、不报错；`61010`（用户近期未访问）同样记为 `skipped`，不重试。后台「内容安全」开关关闭，或小程序未配置 AppID/AppSecret 时，同样全部跳过。**文字内容从不写日志**，只记结论。

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | **后端（H2 流，已实现）：**<br>- `packages/core/src/wechat/wechat.sec-check.ts`：`ContentSecurityPort`（`msgSecCheck`、`mediaCheckAsync`）和走 `getWechatClient(ctx).call('mini', …)` 的真实实现；`checkText` 在事务外调用，返回 `pass / review / risky / unavailable / skipped`，由调用方按上表决定。<br>- 评价：`product_reviews.moderation_reason`（`sec_check_review`、`sec_check_risky`、`sec_check_unavailable`，可为空）记录被扣下的原因；`POST /api/v1/catalog/reviews` 的响应新增 `moderation: 'published' \| 'pending'`，客户端据此显示「审核后展示」。<br>- 后台审核**沿用现有「评价管理」**：状态筛选「待审核」、单条或批量「显示」即通过、「删除」即删除；列表的状态列在待审核时显示原因（例如「微信内容安全：疑似违规」）。`adminProductReview` 新增 `moderationReason`。<br>- 图片：新表 `content_security_checks`（迁移 `0007_content_security`），每张图一行；业务事务里写行和 effect `content-security / wechat.mediaCheck`，提交后调用 `media_check_async` 并存 `trace_id`；只提交 https 绝对地址（相对地址用「站点设置 → 对外地址」补全，补不出 https 则 `skipped`）。<br>- `wxa_media_check` 推送经 `/api/v1/webhooks/wechat-mini`（C07 同一个入口）按消息哈希记入 ledger；handler 按 `trace_id` 找到行，用条件更新 `submitted → 结论`，所以重复推送、并发推送只生效一次；`risky` 时调用所属领域注册的处理器（`catalog` 移除评价图片，`user` 重置头像）。<br>- 站内信事件 `user_avatar_rejected`（内置事件，只走站内信）。<br>- 后台「系统设置 → 内容安全」（`content-security` 配置组，`payment:config:write`）：总开关，默认开。<br>- 假服务器：`@shop/testing` 的 `startFakeOaServer()` 实现两个接口：含「违规测试」的文字返回 `risky`、含「待定测试」的返回 `review`；`mediaChecks` 记录图片提交；`mediaCheckPush(traceId, suggest)` 生成推送；`behaviour.failSecCheck` / `failMediaCheck` 模拟微信故障。<br><br>**客户端：** 评价提交成功后看 `moderation`：`pending` 时提示「评价已提交，审核后展示」，不要当成错误。昵称和发票抬头遇到上述 422 时，显示错误信息并让用户修改。 |
| 运营配置 | 无需申请，已认证的小程序默认可用这两个接口。**必须配置消息推送**（C07 的同一个 URL、Token、EncodingAESKey，安全模式，数据格式 JSON），否则收不到图片检测结果。「站点设置 → 对外地址」必须是 https。后台「评价管理」要有人定期处理「待审核」。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 验证     | [测试] `packages/core/src/wechat/wechat.sec-check.int.test.ts`（CONTENT-001…005）：通过即发布；`risky` / `review` / 微信返回错误 / 网络不通都存为待审核且不报错；管理员通过后公开可见、也可删除；没有 mini openid 或开关关闭时不调用；昵称和抬头 `risky` 拒绝、微信故障时保存；`risky` 图片从评价移除，重复推送和并发推送只处理一次；提交失败时图片保留并重试；`risky` 头像重置并发站内信，已更换的头像不动。假服务器单测在 `packages/testing/src/wechat/fake-oa-server.test.ts`。[E2E] 写评价时内容含「违规测试」，页面提示「审核后展示」而不是报错。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## C10 分享

**要求：**

- 页面**定义了** `onShareAppMessage`，右上角菜单才有「转发」。`path` 必须以 `/` 开头；`imageUrl` 宽高比 5:4；可以返回 promise（3 秒超时）。
- 定义了 `onShareTimeline`（基础库 ≥ 2.11.3）才能分享到朋友圈，图片 1:1。朋友圈打开时是**单页模式**（场景值 1154）：
  - 没有登录态，`wx.login` 等登录接口不可用，支付和授权也不可用；
  - 不能跳转到其他页面；
  - 不渲染 tabBar；
  - 本地存储不和普通模式共用；
  - 所以页面必须是「纯内容展示」。
- 不得强制分享、暗示分享，不得利益诱导分享到朋友圈，不得用「分享后无需互动即可获得利益」的方式诱导分享到群或好友（运营规范 5.1）。拒绝情形 3.2.1 同样禁止把分享作为使用条件。

**来源：**
[Page · onShareAppMessage / onShareTimeline](https://developers.weixin.qq.com/miniprogram/dev/reference/api/Page.html)、
[分享到朋友圈 · 单页模式](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/share-timeline.html)、
[运营规范 5.1](https://developers.weixin.qq.com/miniprogram/product/)、
[滥用分享违规案例（社区）](https://developers.weixin.qq.com/community/develop/doc/000086404ac470274a1acc2a851809)

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 代码     | **客户端：** 每个页面的分享行为见 pages.md 的「分享」列。<br>- **有** `onShareAppMessage` 的页面：首页、分类、商品详情、商品列表、精品推荐、拼团列表、拼团商品、拼团进度、预售列表、预售详情、领券中心、文章列表、文章详情、微页面。<br>- **有** `onShareTimeline` 的页面：只有首页、商品详情、预售详情、拼团商品、文章详情和微页面，它们都能匿名渲染。<br>- 其他页面（订单、售后、账户、登录、结算）**不定义**分享，菜单置灰。<br><br>`platform/share.ts` 提供 `useShare(route, { title, imageUrl })`，`path` 一律由路由目录生成，不手拼字符串。<br><br>**单页模式适配：** `platform/launch.ts` 在 scene 为 1154 时置 `isTimelineSinglePage`，页面据此隐藏加购、购买、领券和登录按钮，只显示「前往小程序」提示条（平台自带底部栏）。<br><br>**拼团「邀请好友参团」：** 是成团的必要步骤，也是用户自己的购买行为，**不附带任何奖励**；文案只能说明事实，例如「还差 1 人成团」。禁止「分享得券」「分享后才能查看」「转发助力」这类设计。优惠券**不能**以分享为领取条件。<br><br>**海报：** 在 canvas 2D 上画「商品图 + 价格 + 小程序码」，保存到相册（隐私项见 C04）。**不在**海报上印「转发给 N 个好友」之类的话。 |
| 运营配置 | 装修和活动文案里避免诱导分享的用语。DIY 里的分享文案字段（`root.props.shareTitle`）由运营填写，后台给出提示。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 验证     | [守卫] `app.config.ts` 里每个页面的分享能力要和 pages.md 路由目录中的 `share` 字段一致（路由目录导出 `share: 'none' \| 'friend' \| 'friend+timeline'`，守卫编译页面源码，检查是否导出 `onShareAppMessage`/`onShareTimeline`；在 Taro 里对应 `useShareAppMessage`/`useShareTimeline`）。[测试] 路由目录单测：`share` 为 `friend+timeline` 的路由，参数只能是公开 id，不能带 token。[人工] 真机把商品分享到朋友圈，打开单页模式，页面正常显示，没有报错。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## C11 小程序码（`getwxacodeunlimit`）

**要求：**

- `scene`：最多 32 个可见字符，只能用 `0-9a-zA-Z!#$&'()*+,/:;=?@-._~`；页面里通过 `options.scene` 读取，要先 `decodeURIComponent`。
- `page`：必须是**已发布**的页面，根路径前不加 `/`，不能带参数。
- `check_path`：默认 `true`，会校验页面是否存在于线上版本；设为 `false` 时可以给未发布的页面生码。
- `env_version`：`release`、`trial` 或 `develop`。
- 数量不限，但有频率限制。

**来源：**
[获取不限制的小程序码](https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/qrcode-link/qr-code/getUnlimitedQRCode.html)

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 代码     | **后端（H1 流）：**<br>- `wechat.miniCode` 的 query 从 `{ page, scene }` 改成 `{ route: StorefrontRouteKey, params }`。服务端做三件事：查路由目录，只允许 `miniCode: true` 的 key；用 `sceneCodec` 把参数编码成 scene，并按 UTF-8 字节数校验不超过 32（`wechat.mini-code.service.ts:31` 已经按字节检查，保留）；再按缓存键 `(page, scene)` 生码。<br>- **过渡期**两种 query 并存，切换时删除 `MINI_CODE_PAGES`（`packages/contracts/src/wechat/schemas.ts:26`）。<br>- `env_version` 改为从配置读：体验版预览装修草稿用 `trial`，线上用 `release`。<br>- 保留 `check_path: false`，因为页面有白名单保护，不用担心乱码。但**首次发版前**生成的码，扫码会显示「页面不存在」，所以发版前不要对外发码。<br><br>**客户端：** 所有可以扫码进入的页面都通过 `platform/launch.ts` 的 `readRouteParams(options)` 读取参数。有 `options.scene` 时，按当前页面对应的 key 解码；否则直接读 query。scene 格式见 pages.md 的「scene 编码」。 |
| 运营配置 | 无。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 验证     | [测试] 路由目录的单测：每个 `miniCode: true` 的 key，用 19 位最大 id 编码后都不超过 32 字节，并且解码后能还原。H1 的集成测试：非白名单 key 被拒绝。[守卫] 路由目录里每个 `miniCode: true` 的 path 都存在于 `app.config.ts`（包括分包路径）。[人工] 扫描真实生成的商品码和拼团码，能落到正确的页面和数据。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## C12 web-view

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 1. 个人主体不能用。<br>2. 除了已关联公众号的文章，其他网页的域名都必须配置为「业务域名」，并在域名根目录放校验文件；网页里的 iframe 域名也要配置。<br>3. web-view 会铺满整个页面，一个页面只能有一个。<br>4. 网页不能调用小程序的支付，需要支付时跳回小程序页面。                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 来源     | [web-view](https://developers.weixin.qq.com/miniprogram/dev/component/web-view.html)、[运营规范第 8 章](https://developers.weixin.qq.com/miniprogram/product/)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 代码     | **客户端（E 流 `packages/content/webview/index`）：**<br>- 只接受 `url` 参数，打开前检查它的主机：已关联公众号文章的主机是 `mp.weixin.qq.com`；其他主机必须在 `app/config.webviewDomains` 列表里。<br>- 不在列表里的主机，不打开 web-view，改为弹层「该链接需在浏览器中打开」，提供「复制链接」按钮（`setClipboardData`，隐私项见 C04）。<br>- 装修的 `LinkTarget.webview` 同样走这个检查。<br><br>**后端：** `app/config` 返回 `webviewDomains`，由运营在后台「小程序设置」里维护，必须和公众平台的配置一致。<br><br>**edge：** 业务域名如果是店铺域名本身，要能提供根路径 `/<校验文件名>.txt`。建议在 `docker/edge/nginx.conf` 里加一段 `location ~ ^/[A-Za-z0-9_-]+\.txt$`，从一个只读挂载目录提供（部署文档说明放置方法），不经过应用。 |
| 运营配置 | 公众平台 →「开发管理 → 开发设置 → 业务域名」，下载校验文件，交给运维放到服务器，再添加域名。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 验证     | [测试] `webview` 页的单测：主机不在白名单时显示复制弹层。[E2E] DIY 里放一个指向非白名单域名的链接，点击后出现复制弹层。[人工] 业务域名页显示校验通过；打开一篇公众号文章。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## C13 包体积与分包

**要求：**

- 主包和每个分包各自不能超过 **2 MB**。
- 所有分包加起来：普通小程序不超过 **30 MB**，第三方平台代开发的不超过 20 MB。
- tabBar 页面必须在主包；主包放启动页、tabBar 页和公共资源。
- 分包不能引用其他分包的代码和资源（分包异步化除外）。
- 分包预下载 `preloadRule`：同一分包里的页面共享 2 MB 的预下载额度。
- tabBar 图标每个不超过 40 KB，建议 81 × 81 px。

**来源：**
[分包加载](https://developers.weixin.qq.com/miniprogram/dev/framework/subpackages.html)、
[app.json · preloadRule / tabBar](https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html)

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 代码     | **本店预算（CI 卡住，比平台上限紧）：** 主包 ≤ **1.5 MB**；每个分包 ≤ **1 MB**；总包 ≤ **8 MB**。分包划分见 pages.md。<br><br>**图片：** 除了 tabBar 图标（位图）、默认头像和空状态插画（SVG 转成 base64 或放主包，合计 ≤ 60 KB），**所有图片都走服务器**（`/uploads/` 或 CDN），不打进包里。<br><br>**依赖：**<br>- NutUI 按需引入，只能通过 `apps/mini/src/ui/` 引用；<br>- 分包页面用到的重组件（日历、级联地区选择器、canvas 海报）放在对应分包里，或使用分包异步化；<br>- 产物中不能出现 zod（S2 已验证）、`eval` 或 `new Function`。<br><br>**preloadRule：**<br>- `pages/index/index` 预下载 `goods`；<br>- `pages/product/index` 和 `pages/cart/index` 预下载 `order`；<br>- `pages/me/index` 预下载 `order` 和 `account`；<br>- 网络条件都用 `all`。 |
| 运营配置 | 无。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 验证     | [守卫]（I1 流）CI 构建 `mp-weixin` 后，统计 `dist/` 中主包和各分包的体积，超过预算就失败，并打印前 10 大文件。`app.json` 的 `tabBar.list` 只能指向主包页面。tabBar 图标每个 ≤ 40 KB。分包目录之间没有 import（用 dependency-cruiser 或守卫的正则实现）。产物中不能出现 `eval(` 或 `new Function`。                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## C14 基础库版本

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                      |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 公众平台可以设置「最低可用基础库版本」，低于该版本的用户会被提示升级微信。版本分布在官方页面可以查。                                                                                                                                                                                                                                                                                      |
| 来源     | [基础库版本分布](https://developers.weixin.qq.com/miniprogram/dev/framework/client-lib/version.html)                                                                                                                                                                                                                                                                                      |
| 决定     | **最低 3.0.0**（计划第 10 节第 11 项）。我们用到的能力中，要求最高的是隐私授权（2.32.3）。其余依次是：昵称和头像的安全检测（2.24.4）、头像昵称填写（2.21.2）、分享到朋友圈（2.11.3）、订阅消息（2.8.2）、确认收货组件（2.6.0）。3.0.0 能全部覆盖，还留出余量；它发布于 2023 年，覆盖率很高。**不用 Skyline**（Taro 4 的 React 运行时以 WebView 渲染为主，Skyline 的兼容性不由我们控制）。 |
| 代码     | `project.config.json` 里 `libVersion` 固定为构建当时的最新稳定版，只用于开发者工具调试；A 流在 `app.tsx` 启动时用 `Taro.canIUse` 检查 `onNeedPrivacyAuthorization`，不支持就提示升级微信（兜底）。                                                                                                                                                                                        |
| 运营配置 | 公众平台 →「设置 → 基本设置 → 基础库最低版本设置」设为 3.0.0。设置前先在版本分布页确认 3.0.0 以下的占比可以忽略。                                                                                                                                                                                                                                                                         |
| 验证     | [人工] 后台设置截图归档。[守卫] `project.config.json` 的 `libVersion` ≥ 3.0.0。                                                                                                                                                                                                                                                                                                           |

## C15 客服

| 项       | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求     | 1. 用 `<button open-type="contact">` 打开小程序客服会话，可以传 `session-from`；配合 `show-message-card`、`send-message-title`、`send-message-path`、`send-message-img`，会把当前商品卡片带进会话。<br>2. 客服人员在网页端 `mpkf.weixin.qq.com` 或「客服小助手」小程序里回复。<br>3. 客服消息下发有 48 小时窗口：用户发消息后 48 小时内可回 5 条；用户进入会话后 1 分钟内可回 2 条。<br>4. 另一种方式是企业微信的「微信客服」（`wx.openCustomerServiceChat`），本店**不用**（计划选的是原生 contact）。                                           |
| 来源     | [小程序客服](https://developers.weixin.qq.com/miniprogram/introduction/custom.html)                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 代码     | **客户端：** `ui/ContactButton`：<br>- `app/config.support.kind === 'mini-program'` 时，渲染成 `open-type="contact"` 按钮；`session-from` 为 `route:<key>;id:<id>`（例如 `route:product;id:1024`）。<br>- 商品详情和订单详情使用 `show-message-card`，`send-message-path` 由路由目录生成。<br>- `kind === 'phone'` 时调用 `Taro.makePhoneCall`；两者都有时，弹出操作菜单让用户选。<br><br>**出现位置：** 商品详情底部栏、订单详情、售后详情、个人中心服务宫格。<br><br>**后端：** `site/config.support` 现有的三种取值保留，合并进 `app/config`。 |
| 运营配置 | 公众平台 →「功能 → 客服」绑定客服人员的微信号，客服人员要实际在线回复。                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 验证     | [测试] `ContactButton` 组件测试：三种配置分别渲染成什么。[人工] 真机从商品详情进入客服会话，能看到商品卡片，客服端能收到并回复。                                                                                                                                                                                                                                                                                                                                                                                                                  |

## C16 审核常见驳回原因（电商）与本店对策

来源：[常见拒绝情形](https://developers.weixin.qq.com/miniprogram/product/reject.html)、[运营规范](https://developers.weixin.qq.com/miniprogram/product/)

| 驳回原因                                           | 本店对策                                                                                                                                                                           | 负责      |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 2.1 类目与实际服务不符                             | 上架商品只在已申请类目范围内；审核前清掉测试商品（C02）。                                                                                                                          | 运营      |
| 3.1.2 类目功能在首页两次点击内到达不了             | 预置的首页模板直接提供分类、拼团、预售、领券入口。                                                                                                                                 | F2、G     |
| 3.1.4 功能不完整、有测试数据、有空白页             | 每个页面都有空状态组件（design.md）；审核前清理「测试」字样的商品、文章、活动；DIY 里没有数据的块自动隐藏。审核备注里提供测试账号（手机号 + 短信验证码由运营临时发，或密码账号）。 | 运营、B–E |
| 3.2.1 诱导分享、关注、下载                         | 见 C10。「关注公众号」块只能展示，不能作为使用条件。                                                                                                                               | D、G      |
| 3.2.5 虚假宣传、虚假交易                           | 划线价要真实并附说明；「虚拟成团」已彻底关闭（C02）；目录里没有虚拟销量字段，展示的销量都是真实成交。                                                                              | 运营、H2  |
| 3.2.8.9 抽奖、有奖竞猜                             | 抽奖功能已退役，没有入口（`retired` 守卫）。                                                                                                                                       | —         |
| 3.4.1 未经同意收集个人信息；一进入就要求授权手机号 | 见 C04、C05。冷启动不弹任何授权；协议勾选默认不勾选。                                                                                                                              | A、B      |
| 3.4.5 未经授权展示用户信息                         | 拼团成员、评价只展示昵称和头像，并对昵称做掩码处理（例如「小*」，由服务端决定）；不展示手机号（运营规范 15.2.1）。                                                                 | C、D      |
| 3.5.3 下载可执行代码                               | 装修数据只有 JSON 块，没有脚本；守卫扫描产物中不能有 `eval` 或 `new Function`。                                                                                                    | I1        |
| 缺少用户协议、隐私政策、注销入口                   | 设置页有《用户协议》《隐私政策》和「注销账号」（`account-cancellations`）。                                                                                                        | E         |
| 退款不原路返回                                     | 退款只退到原微信支付（C06）。                                                                                                                                                      | —         |
| 页面白屏、接口报错（审核员网络环境不同）           | 请求失败时显示可重试的错误态，不白屏；审核前关闭「不校验合法域名」再自测（C03）。                                                                                                  | A、人工   |
| 右上角胶囊区域被遮挡或有交互冲突                   | 首页和个人中心的自定义导航栏按 `getMenuButtonBoundingClientRect` 留出胶囊位置（design.md）。                                                                                       | A         |

## C17 虚拟支付：不适用

| 项   | 内容                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 要求 | 运营规范 5.13 规定，在小程序里消费的**虚拟内容或服务**（会员、课程、短剧、道具等）必须接入虚拟支付，不得引导到外部支付。                                                                                                                                                                                                                                                                                                  |
| 结论 | **本店不适用。** 本店卖的是实物商品；目录里的 `virtual_card`、`virtual_coupon`、`virtual_manual` 是可以在线下或第三方兑换的卡密和券码，属于「虚拟商品」的发货方式（C07 的 `logistics_type=3`），不是在小程序内消费的虚拟内容。付费会员、余额充值、积分这些可能触发虚拟支付的功能**不在产品范围内**（`docs/conventions.md` 的 Out of scope 一节，由 `retired` 守卫保护）。如果以后要卖在小程序内消费的内容，必须重新评估。 |
| 来源 | [运营规范 5.13](https://developers.weixin.qq.com/miniprogram/product/)                                                                                                                                                                                                                                                                                                                                                    |
| 验证 | [守卫] `retired` 守卫不变。[人工] 上架前确认目录里是否真的有虚拟商品；有的话按 C02 补类目（见文末待定问题）。                                                                                                                                                                                                                                                                                                             |

## C18 其他必须有的东西

| 项                     | 要求与做法                                                                                                                                                                | 来源                                                                                 | 验证   |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------ |
| 用户协议和隐私政策页面 | `agreement` 路由（主包），读取 `system.agreementGet` 的 `user`、`privacy`、`cancellation`。隐私政策的内容要和公众平台的「用户隐私保护指引」一致（C04 的表），由运营维护。 | [运营规范第 15 章](https://developers.weixin.qq.com/miniprogram/product/)            | [人工] |
| 账号注销               | 设置 → 注销账号：先展示注销协议，再调用 `user.requestCancellation`，可以撤回（`user.withdrawCancellation`）。                                                             | 个人信息保护法第 47 条，以及平台常见审核要求                                         | [E2E]  |
| 未成年人、定位、广告   | 不涉及。                                                                                                                                                                  | —                                                                                    | —      |
| 微信支付消费者投诉     | 商户平台「消费者投诉」要按平台时限处理（不属于小程序的技术要求，但会影响商户号）。                                                                                        | 微信支付商户平台                                                                     | [人工] |
| 小程序名称、简介、头像 | 名称不得侵权，不得使用未授权的商标（拒绝情形 1.1），这里指商标，与 CRMEB 源码无关。                                                                                       | [常见拒绝情形 1.1](https://developers.weixin.qq.com/miniprogram/product/reject.html) | [人工] |

---

## 守卫与测试汇总（给 I1、I2 流）

| 检查                                                                                                                     | 放在哪里                           | 对应条目 |
| ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | -------- |
| `requiredPrivateInfos` 恰好是 `['chooseAddress']`；隐私接口只在 `platform/` 中出现，并且都列在 `PRIVACY_APIS` 里         | `guards/src/checks/mini.ts`        | C04      |
| 不出现 `getUserProfile`、`getUserInfo`、`requestSubscribeMessage`（`platform/` 除外）、`Taro.request`（api-client 除外） | `mini.ts`                          | C05、C08 |
| `subscribe(` 只能在事件处理函数里调用；场景 key 属于契约枚举                                                             | `mini.ts`（AST）                   | C08      |
| 页面的分享导出和路由目录的 `share` 一致                                                                                  | `mini.ts`                          | C10      |
| 路由目录中的每个 path 都在 `app.config.ts` 里；`miniCode: true` 的 path 在主包或分包中存在；scene 最长编码不超过 32 字节 | `mini.ts` 加路由目录单测           | C11      |
| 包体积预算；tabBar 只指向主包；tabBar 图标 ≤ 40 KB；没有跨分包 import；没有 `eval` 和 `new Function`                     | CI 的 `mini:size` 任务加 `mini.ts` | C13      |
| `urlCheck: true`，`libVersion ≥ 3.0.0`，接口源不是 `http://`                                                             | `mini.ts`                          | C03、C14 |
| `order.confirmReceipt` 只在 `platform/receipt.ts` 中调用                                                                 | `mini.ts`                          | C07      |
| 发货录入、推送处理、内容安全、订阅字段截断                                                                               | `packages/core` 集成测试（H2）     | C07–C09  |
| 未登录浏览、登录后回跳、1400001 回退到短信、确认收货组件、订阅被拒仍能下单、web-view 兜底、内容安全拒绝                  | `e2e/storefront`（I2）             | C05–C12  |

---

## 需要运营或老板办理的事项

按依赖顺序排列。「周期」是常见经验值，以各平台的实际审核为准；**外部资质是上线的关键路径**（计划第 7 节），建议立即开始。

| #   | 事项                                                                                                                                       | 依赖                     | 典型周期                             | 在哪里办                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------ | ------------------------------------ | -------------------------------------- |
| 1   | 准备营业执照、法人身份证、对公账户；确认要卖的商品需要哪些**前置资质**（如食品经营许可证）。                                               | —                        | 已有则 0；新办许可证需数周           | 线下                                   |
| 2   | 域名 **ICP 备案**（计划第 0 节提到尚未通过）。                                                                                             | 1                        | 1–3 周（管局审核最长约 20 个工作日） | 云厂商备案系统                         |
| 3   | 用企业主体注册小程序（专用邮箱），拿到 AppID 和 AppSecret，交给技术填入后台「小程序设置」。                                                | 1                        | 1 天                                 | mp.weixin.qq.com                       |
| 4   | **微信认证**（C01）。                                                                                                                      | 3                        | 1–5 个工作日（认证费以平台公示为准） | 公众平台 → 设置                        |
| 5   | **服务类目**：商家自营 + 与商品对应的二级类目，上传资质（C02）。                                                                           | 4、1                     | 1–7 天                               | 公众平台 → 设置 → 服务类目             |
| 6   | **小程序备案**（C03）。                                                                                                                    | 4、5（备案要填服务内容） | 平台初审 1–2 天，加管局审核 1–3 周   | 公众平台 → 设置 → 小程序备案           |
| 7   | **微信支付商户号**：申请或复用（主体一致）→ 开通 JSAPI 支付 → 关联小程序 AppID（两边都要确认）→ 生成 APIv3 密钥和证书，交给技术（C06）。   | 1、3（关联需要 AppID）   | 申请 1–5 个工作日；关联当天          | pay.weixin.qq.com、公众平台 → 微信支付 |
| 8   | **用户隐私保护指引**：按 C04 的表逐项填写。                                                                                                | 3                        | 审核约 1–3 天                        | 公众平台 → 设置 → 服务内容声明         |
| 9   | 申请 `wx.chooseAddress` 接口权限（C04）。                                                                                                  | 4、5                     | 1–3 天                               | 开发管理 → 开发设置 → 接口设置         |
| 10  | **服务器域名**和**业务域名**，放置校验文件（C03、C12）。                                                                                   | 2、生产环境已部署        | 当天（每月修改次数有限）             | 开发管理 → 开发设置                    |
| 11  | **消息推送**：填 URL、Token 和 EncodingAESKey（C07、C09）。                                                                                | 10，H2 已部署            | 当天                                 | 开发管理 → 开发设置 → 消息推送         |
| 12  | **订阅消息模板**：按 C08 的场景表从公共模板库选模板，把模板 ID 交给技术或填入后台。                                                        | 5（模板库随类目变化）    | 选用即时；申请新模板需数天           | 公众平台 → 功能 → 订阅消息             |
| 13  | **手机号快速验证**资源包，设置余额提醒（C05）。                                                                                            | 4                        | 即时（预付费）                       | 公众平台 → 费用相关入口                |
| 14  | **客服**：绑定客服人员（C15）。                                                                                                            | 3                        | 当天                                 | 公众平台 → 功能 → 客服                 |
| 15  | 短信签名和模板（短信登录兜底，C05）。                                                                                                      | 1                        | 签名和模板审核约 1–2 天              | 阿里云短信控制台                       |
| 16  | **代码上传密钥**（`miniprogram-ci`）和 IP 白名单；把真机测试人员加为体验成员。                                                             | 3                        | 当天                                 | 开发管理 → 开发设置；成员管理          |
| 17  | 基础库最低版本设为 3.0.0（C14）。                                                                                                          | 3                        | 当天                                 | 设置 → 基本设置                        |
| 18  | 快递公司的微信编码对照，在后台填写（C07）。                                                                                                | H2 迁移已上线            | 半天                                 | 本店后台 → 快递公司                    |
| 19  | 审核准备：测试账号、清理测试数据、准备审核备注（C16）。                                                                                    | 2–18                     | 半天                                 | 本店后台                               |
| 20  | **提交审核并发布**；首次发布后确认已纳入发货管理（`is_trade_managed`），用 0.01 元商品走一次真实的支付、发货、确认收货和结算（C06、C07）。 | 全部                     | 审核通常 1–3 天（高峰更久）          | 公众平台 → 版本管理                    |

**关键路径：** 1 → 2 ‖ (3 → 4 → 5 → 6) → 10 → 20。第 2 项和第 6 项都走管局，是最长、最不可控的两段。第 7 项可以和第 5、6 项并行。
