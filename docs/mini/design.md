# 小程序设计规范

`apps/mini` 的视觉与交互规范：设计 token、主题、`ui/` 组件清单、页面版式、无障碍、深色模式。

- 页面见 [pages.md](pages.md)；平台硬性要求见 [wechat-compliance.md](wechat-compliance.md)。
- 本文所有尺寸都是 **750 设计宽度下的 px**。Taro 的 `designWidth: 750` 把 Sass 中的 `px` 按 1:1 转为 `rpx`，所以 `28px` 在 375 pt 宽的屏幕上显示为 14 pt。
- 行内样式里的尺寸必须经 `Taro.pxTransform`，由守卫检查（第 9 节）。

---

## 1. 原则

### 1.1 微信设计指南

依据：https://developers.weixin.qq.com/miniprogram/design/

指南的四条原则是「友好礼貌、清晰明确、便捷优雅、统一稳定」。落到本项目：

| 原则     | 在本项目中的具体要求                                                                                                                                                                                                                                                               |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 友好礼貌 | **不打断用户：**<br>- 开屏浮层每天最多一次，可跳过；<br>- 不在进入页面时弹登录，登录只在动作触发时出现（C05）；<br>- 订阅消息只在用户点击「提交」时请求（C08）；<br>- 首页不叠放多个弹窗，新人券弹窗和开屏浮层互斥，开屏优先。                                                     |
| 清晰明确 | **每页只有一个主操作：**<br>- 商品详情是「立即购买」，结算页是「提交订单」，收银台是「立即支付」；<br>- 主按钮用品牌色实心，其余一律是描边或文字按钮；<br>- 状态要可见：加载用骨架屏，空状态有插图、说明和下一步按钮，错误有原因和「重试」。                                       |
| 便捷优雅 | **减少输入：**<br>- 用微信能力替代手填：手机号快速验证、微信地址、微信发票抬头、`chooseAvatar`、昵称键盘；<br>- 输入框使用正确的 `type`（`number`、`digit`、`idcard`、`nickname`），并设置 `confirm-type`；<br>- 结算页默认选中默认地址和最优券。                                  |
| 统一稳定 | **导航与反馈统一：**<br>- 除首页和「我的」，其他页面使用原生导航栏；<br>- 自定义导航栏必须避开右上角胶囊（`getMenuButtonBoundingClientRect`）；<br>- 反馈统一走 `ui/` 的 `Toast`、`Dialog`、`ActionSheet`，不直接调用 `wx.showModal`，保证样式和文案一致；<br>- 返回行为不被劫持。 |

### 1.2 WeUI 风格

WeUI 是微信官方的基础样式库：https://github.com/Tencent/weui 。我们**不直接使用 WeUI 的组件**（组件来自 NutUI-React-Taro，经 `ui/` 封装），但以下几点向 WeUI 看齐，让用户觉得「像微信」：

- **灰底白卡：** 页面底色为浅灰，内容放在白色卡片或 cell 组里；cell 高度 ≥ 96px，分割线用 1 物理像素（hairline）。
- **中性色克制：** 大面积只用黑、白、灰；品牌色只出现在主按钮、选中态、价格、角标上。
- **弹层从底部出：** 选择类操作（规格、地址、券、分享方式）都用底部弹层（`Sheet`），不用居中弹窗；居中 `Dialog` 只用于确认和阻断。
- **Toast 短促：** 一律 1.5 秒；成功无图标，只用文字；只有「加载中」带 loading 图标。

### 1.3 电商惯例

- **价格：** 最醒目的数字。主价格用 `--color-price`，「¥」符号小一号，小数部分也小一号（例如 ¥**199**.00）。划线价用 `--color-text-tertiary` 并加删除线。划线价的依据说明放在商品详情的「价格说明」里（C02）。
- **商品卡：** 图片 1:1，标题最多 2 行，价格单独一行，销量、标签放在最后一行；整卡可点，另有独立的加购按钮（热区 ≥ 88px）。
- **底部操作栏：** 商品详情、结算、订单详情、售后都用固定底栏：左侧是图标入口（客服、购物车、收藏），右侧是 1 到 2 个主次按钮；底栏高 100px，再加安全区。
- **倒计时和库存紧张：** 只对真实的截止时间（拼团到期、预售结束、待支付倒计时）显示倒计时，不做虚假紧迫（C02、C16）。
- **状态颜色表示含义：** 订单和售后状态用文字表达，颜色只是辅助。「待付款」和「退款中」用 `--color-warning-text`，「已完成」用 `--color-text-secondary`，「已关闭」用 `--color-text-tertiary`。

---

## 2. 设计 token

token 分两类：

| 类别     | 包含                         | 实现                                                                                | 运营能否修改          |
| -------- | ---------------------------- | ----------------------------------------------------------------------------------- | --------------------- |
| **颜色** | 第 2.1 节全部                | CSS 变量。`app.scss` 的 `page { … }` 写默认值，运行时由 `ThemeRoot` 覆盖（第 3 节） | 仅第 3.2 节列出的几个 |
| **尺寸** | 字号、间距、圆角、阴影、层级 | Sass 变量（`apps/mini/src/ui/tokens/_size.scss`），编译时确定                       | 否                    |

尺寸不使用 CSS 变量，有两个原因：

1. 尺寸不需要运行时修改；
2. Taro 的 px→rpx 转换对 `var()` 里的数值是否生效没有保证，而 Sass 变量在编译期展开成字面量，一定会被转换。

### 2.1 颜色

所有对比度按 WCAG 2.x 计算，背景是白色 `#FFFFFF`；标「灰底」的是在 `--color-bg` `#F5F5F5` 上的值。目标：正文 ≥ 4.5，大字（≥ 36px 加粗或 ≥ 48px）和图标 ≥ 3。

| token                    | 默认值                            | 用途                                                                                             | 对比度（白 / 灰底）                                     |
| ------------------------ | --------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| `--color-primary`        | `#E1251B`                         | 品牌主色：主按钮底色、选中态（tab、筛选、复选框）、角标                                          | 4.69 / 4.30                                             |
| `--color-on-primary`     | `#FFFFFF`（自动计算）             | 主色上的文字和图标                                                                               | 4.69                                                    |
| `--color-primary-soft`   | 主色 10% 透明度叠在白色上         | 标签底、选中卡片底、次按钮底                                                                     | —                                                       |
| `--color-primary-text`   | 等于主色，或自动加深（第 3.3 节） | 白底上用主色写的文字（「查看全部」、选中的 tab 文字）                                            | ≥ 4.5                                                   |
| `--color-accent`         | `#FF7E00`                         | 辅助色：渐变按钮的起点、「加入购物车」次按钮的底色（与主色并排）；**不用于小字**                 | 2.55（只作底色，上面的文字用 `--color-on-accent`）      |
| `--color-on-accent`      | `#FFFFFF`（自动计算）             | 辅助色上的文字；实心按钮上的文字 ≥ 30px 加粗，按大字标准 ≥ 3 判定，不足时自动改为 `#1A1A1A`      | —                                                       |
| `--color-price`          | `#E1251B`                         | 所有价格、优惠金额、「¥」                                                                        | 4.69 / 4.30（价格只出现在白色卡片上，不放灰底）         |
| `--color-text`           | `#1A1A1A`                         | 标题、正文、商品名                                                                               | 17.4 / 16.0                                             |
| `--color-text-secondary` | `#666666`                         | 次要说明、规格、表单标签                                                                         | 5.74 / 5.27                                             |
| `--color-text-tertiary`  | `#707070`                         | 辅助信息（时间、销量、划线价、占位符）                                                           | 4.95 / 4.54（旧版常用的 `#999999` 只有 2.85，不再使用） |
| `--color-text-disabled`  | `#B2B2B2`                         | 禁用文字；**只用于禁用态**，WCAG 对禁用控件不做要求                                              | 2.12                                                    |
| `--color-bg`             | `#F5F5F5`                         | 页面底色                                                                                         | —                                                       |
| `--color-surface`        | `#FFFFFF`                         | 卡片、cell、弹层、底栏                                                                           | —                                                       |
| `--color-surface-sunken` | `#F7F7F7`                         | 卡片内的次级块（地址块底、订单商品行底、搜索框底）                                               | —                                                       |
| `--color-mask`           | `rgba(0, 0, 0, 0.6)`              | 弹层遮罩                                                                                         | —                                                       |
| `--color-border`         | `#E5E5E5`                         | 分割线、描边按钮的边框（hairline）                                                               | 装饰线，不要求对比度                                    |
| `--color-border-strong`  | `#8C8C8C`                         | 输入框、复选框、单选框的边框（控件边界需要 ≥ 3）                                                 | 3.36                                                    |
| `--color-success-text`   | `#0F7B3F`                         | 成功文字（「已支付」「领取成功」）                                                               | 5.35                                                    |
| `--color-success`        | `#07C160`                         | 只用于「微信支付」品牌标识和结果页的成功插图（装饰，旁边一定有文字说明）；不作按钮底色，不写文字 | 2.38（达不到图标的 3，所以只作装饰）                    |
| `--color-warning-text`   | `#A85A00`                         | 警告文字（「待付款」「库存紧张」）                                                               | 5.09                                                    |
| `--color-warning-soft`   | `#FFF4E5`                         | 警告提示条的底色                                                                                 | —                                                       |
| `--color-danger`         | `#D32F2F`                         | 错误文字、表单校验、危险按钮（注销账号、删除）                                                   | 4.98                                                    |
| `--color-danger-soft`    | `#FDECEC`                         | 错误提示条的底色                                                                                 | —                                                       |
| `--color-link`           | `#576B95`                         | 链接、协议名（微信风格的蓝灰色）                                                                 | 5.33                                                    |
| `--color-skeleton`       | `#EEEEEE`                         | 骨架屏块                                                                                         | —                                                       |

**关于默认主色 `#E1251B`：**

- 旧版的默认红色是 `#E93323`（`apps/uni-app/api/mappers/diy.js:49`），在白底上只有 4.23，白字放在它上面也是 4.23，都达不到 4.5。
- `#E1251B` 与它肉眼几乎看不出区别，但两个对比度都过线。
- 运营在旧版选过的主题色原样保留，过线靠第 3.3 节的自动派生，不改运营的数据。

**旧版一键换色的另外四种颜色：**

| 颜色         | 白底对比度 | 情况                   |
| ------------ | ---------- | ---------------------- |
| 蓝 `#1DB0FC` | 2.42       | 做按钮底色，白字不过线 |
| 绿 `#42CA4D` | 2.15       | 同上                   |
| 粉 `#FF448F` | 3.24       | 同上                   |
| 橙 `#FE5C2D` | 3.10       | 同上                   |

这正是需要第 3.3 节自动派生的原因。

### 2.2 字号

字体一律用系统字体栈，不打包字体文件（体积预算，C13）。

- 字体栈：`-apple-system, BlinkMacSystemFont, 'PingFang SC', 'Helvetica Neue', 'Microsoft YaHei', sans-serif`。
- 数字（价格、倒计时）加 `font-variant-numeric: tabular-nums`，避免跳动时宽度变化。

| Sass token       | 字号 / 行高 | 字重 | 用途                                                       |
| ---------------- | ----------- | ---- | ---------------------------------------------------------- |
| `$font-display`  | 48 / 64     | 600  | 支付结果金额、商品详情主价格的整数部分                     |
| `$font-title-lg` | 36 / 50     | 600  | 页面内大标题（支付结果状态、空状态标题）、自定义导航栏标题 |
| `$font-title`    | 32 / 44     | 600  | 卡片标题、商品详情标题、弹层标题                           |
| `$font-body-lg`  | 30 / 42     | 400  | cell 主文字、按钮文字（大按钮）、输入框                    |
| `$font-body`     | 28 / 40     | 400  | **正文默认**：商品卡标题、订单商品名、说明文字             |
| `$font-caption`  | 24 / 34     | 400  | 辅助信息：规格、时间、销量、表单提示                       |
| `$font-small`    | 22 / 30     | 400  | 标签文字、tab 栏文字、价格的「¥」符号和小数部分            |
| `$font-micro`    | 20 / 28     | 500  | **仅限**角标数字（购物车数量、未读数）；不用于句子         |

规则：

- 正文不小于 28，任何可读的句子不小于 22。
- 页面根节点**跟随微信的字体大小设置**：`<PageMeta pageFontSize="system" rootFontSize="system" />`（基础库 2.11.0；`page-meta` 必须是页面的第一个节点）。参考：https://developers.weixin.qq.com/miniprogram/dev/component/page-meta.html
- 版式要能承受 1.3 倍放大：
  - 文字容器不写死高度；
  - 按钮用 `min-height`；
  - 单行文字加省略号，多行用 `-webkit-line-clamp`。

### 2.3 间距

基准是 4 的倍数；页面里只允许使用下表中的 token，由 stylelint 规则检查。

| token      | 值  | 典型用途                               |
| ---------- | --- | -------------------------------------- |
| `$space-1` | 8   | 图标与文字、标签之间                   |
| `$space-2` | 12  | 卡片内的行间距                         |
| `$space-3` | 16  | 商品网格的列间距、cell 内元素间距      |
| `$space-4` | 20  | 卡片之间的垂直间距                     |
| `$space-5` | 24  | **页面左右边距（gutter）**、卡片内边距 |
| `$space-6` | 32  | 分组之间                               |
| `$space-7` | 40  | 空状态、结果页的大块留白               |
| `$space-8` | 48  | 结果页图标与标题                       |
| `$space-9` | 64  | 结果页顶部留白                         |

- 商品网格双列：`(750 − 24 × 2 − 16) / 2 = 343`，每列宽 343。
- 单个可点元素的热区 ≥ 88 × 88。视觉上小于这个尺寸的（复选框 40、关闭图标 32），用 padding 或伪元素扩大热区。

### 2.4 圆角

| token          | 值  | 用途                        |
| -------------- | --- | --------------------------- |
| `$radius-xs`   | 4   | 标签、角标                  |
| `$radius-sm`   | 8   | 输入框、小图（订单商品图）  |
| `$radius-md`   | 16  | 卡片、商品图、弹窗 `Dialog` |
| `$radius-lg`   | 24  | 底部弹层的顶部两个角        |
| `$radius-pill` | 999 | 按钮、搜索框、胶囊标签      |

DIY 块的圆角由装修属性控制（例如商品块的「圆角 / 直角」），取值只能从上表选择。

### 2.5 阴影

原则上**不用阴影区分层级**，靠「灰底 + 白卡」区分，这也符合 WeUI 的扁平风格。只保留两档：

| token           | 值                                | 用途                                         |
| --------------- | --------------------------------- | -------------------------------------------- |
| `$shadow-bar`   | `0 -2px 12px rgba(0, 0, 0, 0.06)` | 固定底栏（与滚动内容分开）                   |
| `$shadow-float` | `0 8px 24px rgba(0, 0, 0, 0.12)`  | 浮动按钮（回到顶部、悬浮购物车）、吸顶筛选条 |

### 2.6 层级（z-index）

| token        | 值   | 用途                                         |
| ------------ | ---- | -------------------------------------------- |
| `$z-sticky`  | 100  | 吸顶的 tab、筛选条、自定义导航栏             |
| `$z-bar`     | 200  | 固定底栏、悬浮按钮                           |
| `$z-overlay` | 900  | 遮罩                                         |
| `$z-sheet`   | 1000 | 底部弹层、`Dialog`、`PrivacySheet`、开屏浮层 |
| `$z-toast`   | 2000 | `Toast`                                      |

- 原生 tabBar、导航栏、`web-view` 与原生组件（`video`、`map`、`canvas` 2D 之外的 `canvas`）不受 z-index 约束，不在此表中。
- 弹层打开时，页面上的 `video` 必须暂停并隐藏，否则会盖住弹层。

### 2.7 安全区与系统区域

| 区域                | 处理方式                                                                                                                                                                                                                                   |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 底部（Home 指示条） | 固定底栏和底部弹层的内容区加 `padding-bottom: env(safe-area-inset-bottom)`（Sass mixin `@include safe-bottom($extra)`）。tab 页使用原生 tabBar，微信已经处理安全区，**不要重复添加**。                                                     |
| 顶部（状态栏）      | 只有自定义导航栏的页面（首页、我的）需要处理。`useNavBarMetrics()` 从 `getWindowInfo().statusBarHeight` 和 `getMenuButtonBoundingClientRect()` 算出导航栏高度 = `状态栏高度 + (胶囊.top − 状态栏高度) × 2 + 胶囊.height`，结果缓存到内存。 |
| 右上角胶囊          | 自定义导航栏上的内容（搜索框、门店名）右边界 ≤ `胶囊.left − 16px`（设备像素，不是 rpx）。胶囊区域不放任何可点元素（C16：遮挡胶囊会被驳回）。                                                                                               |
| 横屏 / iPad         | `app.json` 不开启 `pageOrientation: auto`。在 iPad 等宽屏上，750 设计宽度会等比放大；商品网格在屏宽 ≥ 768pt 时改为 3 列（`useWindowWidth`），避免图片过大。                                                                                |

---

## 3. 主题

### 3.1 注入方式

- **默认值：** `apps/mini/src/app.scss` 在 `page { … }` 中写第 2.1 节的全部颜色变量。在小程序里，`page` 选择器就是根节点。
- **运行时覆盖：** 每个页面最外层是 `ui/PageShell`，第一个节点是 `<PageMeta pageStyle={themeStyle} pageFontSize="system" rootFontSize="system" />`。`themeStyle` 是 `ThemeRoot` 从 `app/config.theme` 算出的一串 `--color-*: …;`。
- 用 `page-meta` 而不是在某个 `View` 上写 style，是因为 `page-style` 作用于页面根节点，**底部弹层、Toast 等挂在根下的节点也能继承**，不会出现「弹层还是默认红色」的问题。
- `app/config` 带 ETag，本地缓存。冷启动先用缓存的主题渲染，再在后台刷新，避免主色从默认值闪变为店铺的颜色。
- **NutUI：** `ThemeRoot` 同时输出 NutUI 的 CSS 变量，指向我们的 token，例如 `--nutui-color-primary: var(--color-primary)`，`--nutui-color-primary-stop-1` 和 `-stop-2` 都指向主色，不使用它的渐变。完整的对应表放在 `ui/tokens/nutui-bridge.scss`。页面代码**不直接使用** `--nutui-*` 变量。NutUI 变量的具体名称以 S1 锁定的 NutUI-React-Taro 版本为准。
- **原生区域**不受 CSS 影响，由 `platform/theme.ts` 在启动时和主题变化后调用 API：
  - `setTabBarStyle({ color, selectedColor, backgroundColor, borderStyle })`：选中色等于 `--color-primary-text`，未选中色 `#666666`，背景 `#FFFFFF`，`borderStyle: 'white'`；
  - `setTabBarItem`：只改文字；图标用打包在主包里的 4 组 PNG（81 × 81，每个 ≤ 40 KB），不从网络加载；
  - `setNavigationBarColor`：原生导航栏统一为白底黑字，不跟随主题（微信限制 `frontColor` 只能是 `#000000` 或 `#ffffff`，白底黑字最稳妥）；
  - 购物车数量用 `setTabBarBadge`。

### 3.2 运营可以修改的 token

对应后台「装修 → 主题」和 `app/config.theme`。`diyThemeTokens` 目前是 `Record<string, unknown>`，F1 流要把它改为有类型的 schema：

| 字段（`diyThemeTokens` v2） | 对应 token                                 | 说明                                                         |
| --------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| `primary`                   | `--color-primary`（再派生 on、soft、text） | 品牌主色；旧数据的 `theme` 键在迁移时改名                    |
| `accent`                    | `--color-accent`（再派生 on）              | 辅助色；为空时等于主色（单色方案）                           |
| `price`                     | `--color-price`                            | 价格色；为空时等于 `primary` 派生出的 `--color-primary-text` |
| `tabBar.selectedColor`      | tabBar 选中色                              | 为空时等于 `--color-primary-text`                            |

其余 token（文字灰阶、背景、边框、语义色、全部尺寸）**不开放**。理由：

1. 这些颜色决定可读性和状态含义，改坏的代价高；
2. 旧版开放了十几个颜色变量（`apps/uni-app/utils/theme.js:39-53`），但实际上被运营修改的只有主色和辅助色；
3. 单个 DIY 块内部的颜色（背景、标题色）已由装修属性控制，不需要全局 token。

### 3.3 可读性的自动派生（写入 `packages/contracts`，前后端共用）

`deriveTheme(tokens)` 是一个纯函数，放在 `packages/contracts/src/system/theme.ts`（本身不依赖 zod，小程序可以直接在运行时 import；输入用 `themeInputOf(app/config.appearance.theme)` 得到）。后台预览和小程序调用同一份实现（不变量 SYS-021）：

1. `on-primary`：白色和 `#1A1A1A` 中与主色对比度较高的那个。
2. `primary-text`：如果主色在白底上 ≥ 4.5，就等于主色；否则在 HSL 中逐步降低亮度，直到 ≥ 4.5。例如蓝 `#1DB0FC` 会被加深为约 `#0077B3`。
3. `price`：同第 2 条的规则。
4. `on-accent`：同第 1 条。

**后台的提示：** 主题编辑器在颜色旁显示派生结果和对比度。当主色本身不足 4.5 时提示「按钮文字将自动改为深色」「文字将使用加深后的颜色」，**不阻止保存**。运营选择的品牌色是经营决定，我们只保证文字可读。

---

## 4. 组件清单（`apps/mini/src/ui/`）

规则：

- 页面只从 `@/ui` 导入组件，不直接导入 `@nutui/nutui-react-taro`；ESLint 的 `no-restricted-imports` 检查。NutUI 只在 `ui/` 内部使用，以后替换或升级只影响这一层。
- 每个组件都要有 Storybook 式的演示页 `ui/__demo__/`（仅开发构建），列出下表中的**全部状态**。评审时逐个状态截图对比。

**通用状态：**

| 状态       | 含义                                |
| ---------- | ----------------------------------- |
| `default`  | 默认                                |
| `pressed`  | 按下：`hover-class`，背景加 5% 黑色 |
| `disabled` | 禁用                                |
| `loading`  | 加载中                              |
| `empty`    | 空                                  |
| `error`    | 错误                                |
| `selected` | 选中                                |

表中「状态」列只写组件**特有**的状态。

### 4.1 基础

| 组件        | 用途与要点                                                                                                                                                                                                    | 状态                                                                        |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `PageShell` | 每页的最外层：输出 `PageMeta`（主题、字体）、挂载 `PrivacySheet` 和 `Toast` 容器，提供 `safeBottom`、`bg` 两个属性                                                                                            | —                                                                           |
| `Button`    | `variant: primary \| secondary \| outline \| text \| danger`，`size: lg(88) \| md(72) \| sm(56)`，`block`。透传 `openType`（`getPhoneNumber\|agreePrivacyAuthorization`、`contact`、`share`、`chooseAvatar`） | default、pressed、disabled、loading（显示转圈，保持宽度不变，不可重复点击） |
| `Icon`      | 内置 SVG 图标集，转为 base64 的 `Image` 或 iconfont（S1 定，≤ 30 KB），`size`、`color` 默认继承 `currentColor`；带 `label` 时输出 `aria-label`，否则为纯装饰（`aria-hidden`）                                 | —                                                                           |
| `Image`     | 包装 `Image`：`lazyLoad`；按容器宽度请求合适尺寸（S2 定图片参数）；1:1 占位底色；加载失败显示占位图标                                                                                                         | loading（占位底色）、error（占位图标）、loaded                              |
| `Price`     | `value: string`（契约的 money 字符串，不转 float），`size: lg \| md \| sm`，`strike`，`prefix`（「到手价」）。「¥」和小数部分小一号                                                                           | 常规、划线、`free`（「免费」/「¥0」由调用方决定）                           |
| `Tag`       | `tone: primary \| warning \| success \| neutral`，`variant: soft \| outline`                                                                                                                                  | —                                                                           |
| `Badge`     | 数字角标（`max=99` 显示「99+」）和圆点                                                                                                                                                                        | 数字、圆点、隐藏（0）                                                       |
| `Divider`   | hairline，可带文字                                                                                                                                                                                            | —                                                                           |
| `Skeleton`  | 预制：`ProductCardSkeleton`、`CellSkeleton`、`DetailSkeleton`                                                                                                                                                 | 动画、静态（减弱动效时）                                                    |

### 4.2 反馈

| 组件          | 用途与要点                                                                                                                                                                      | 状态                                          |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `Toast`       | `toast.text()`、`toast.success()`、`toast.loading()`、`toast.hide()`；显示 1.5 秒；同一时间只显示一条，新的替换旧的                                                             | text、success、loading（带遮罩，阻止点击）    |
| `Dialog`      | `confirm({ title, content, confirmText, danger })` 返回 Promise；按钮顺序：左「取消」右「确认」（与 `wx.showModal` 一致）；危险操作的确认按钮用 `--color-danger`                | 普通、危险、仅确认（alert）                   |
| `Sheet`       | 底部弹层：标题、关闭按钮、内容滚动、底部固定按钮区（含安全区）；`aria-role="dialog" aria-modal="true"`；打开时锁定页面滚动（`page-meta` 的 `page-style` 加 `overflow: hidden`） | 关闭、打开中、打开、内容加载中                |
| `ActionSheet` | 选项列表 + 取消                                                                                                                                                                 | 普通、含危险选项                              |
| `Empty`       | 插图（4 种：通用、搜索、订单、网络）+ 标题 + 说明 + 可选按钮                                                                                                                    | 各插图各一种                                  |
| `ErrorBlock`  | 按错误类型显示：网络（「网络不太好」+ 重试）、服务端（显示契约返回的中文 `message` + 重试）、404（「内容不存在或已下架」+ 回首页）、未登录（登录引导卡片）                      | network、server、notFound、unauthenticated    |
| `NoticeBar`   | 页面顶部提示条                                                                                                                                                                  | info、warning、danger；可关闭或常驻           |
| `ResultPage`  | 支付结果、提交售后、注销等结果页：图标 + 标题 + 说明 + 主次按钮                                                                                                                 | success、pending（「确认中」）、fail、waiting |

### 4.3 表单

| 组件                | 用途与要点                                                                                                                                                                             | 状态                                                            |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `Cell`、`CellGroup` | 标签 + 值 + 箭头；最小高度 96；可点时 `hover-class`；支持 `required` 星号                                                                                                              | default、pressed、disabled、带错误（下方红字）                  |
| `Field`             | 输入框：`type`（`text`、`number`、`digit`、`idcard`、`nickname`）、`maxlength`、清除按钮、计数；与 `react-hook-form` 集成（S1 定），校验规则来自 api-client 附带的轻量规则，不使用 zod | empty（占位符）、filled、focused、error、disabled、readonly     |
| `Textarea`          | 评价、售后说明；字数计数；`auto-height`                                                                                                                                                | 同 `Field`                                                      |
| `SmsCodeField`      | 手机号 + 验证码 + 「获取验证码」倒计时 60 秒；发送前检查手机号格式                                                                                                                     | idle、sending、countdown、error                                 |
| `Checkbox`、`Radio` | 圆形 40（热区 88）；选中用主色                                                                                                                                                         | unchecked、checked、indeterminate（全选）、disabled             |
| `AgreementCheck`    | 登录和注册用的「我已阅读并同意《用户协议》《隐私政策》」；**默认不勾选**；未勾选就点登录时，轻微抖动并显示 toast（C05）                                                                | unchecked、checked、shake                                       |
| `Stepper`           | 数量：减、输入、加；最小值和最大值（库存、限购）；到达上下限时对应按钮禁用                                                                                                             | default、min、max、disabled                                     |
| `Switch`            | 设置项                                                                                                                                                                                 | on、off、disabled、loading                                      |
| `RegionPicker`      | 省市区级联（`shipping.cityTree`，缓存）；位于 `account` 分包的 `components/`，不在 `ui/`，因为只有地址编辑页使用                                                                       | loading、选择中、已选                                           |
| `ImageUploader`     | 最多 N 张（评价 9、售后 6）；`chooseMedia` → `storage.userUpload`；显示每张的进度；失败的图片可点击重试；点击预览                                                                      | empty、uploading（进度）、done、error、达到上限（隐藏添加按钮） |
| `SearchBar`         | 搜索框：首页、分类页上是只读入口（点击跳转到搜索页），搜索页上可输入                                                                                                                   | entry、focused、filled                                          |
| `AvatarPicker`      | `Button openType="chooseAvatar"` 包装：拿到临时路径 → 上传 → 回填；显示当前头像                                                                                                        | default、uploading、error                                       |

### 4.4 商品与交易

| 组件                | 用途与要点                                                                                                                                                         | 状态                                                                                                            |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| `ProductCard`       | `layout: grid \| list \| mini`（横滑）；图片、标题（2 行）、价格、划线价、标签、销量；可选加购按钮                                                                 | default、售罄（图片蒙层「已售罄」，加购按钮隐藏）、下架（整卡置灰，不可点）、活动（左上角「拼团」「预售」标签） |
| `ProductGrid`       | 双列网格 + 分页加载（TanStack Query `useInfiniteQuery`）+ 骨架 + 空状态                                                                                            | loading、loaded、loadingMore、end（「没有更多了」）、empty、error                                               |
| `SkuSheet`          | 规格选择：规格图、价格、库存、规格值（不可选的置灰）、数量、确认按钮（「加入购物车」「立即购买」「确定」由调用方决定）                                             | 未选全（按钮禁用并提示「请选择 颜色」）、已选、库存不足、限购                                                   |
| `CouponCard`        | 金额或折扣、门槛、适用范围、有效期、操作                                                                                                                           | 可领、已领（「去使用」）、已抢光、可用（结算页的选择模式）、不可用（附原因）、已使用、已过期                    |
| `CouponSheet`       | 结算页和商品详情的领券 / 选券弹层                                                                                                                                  | loading、列表、empty                                                                                            |
| `AddressCard`       | 结算页顶部：姓名、电话（中间 4 位打码）、地址；无地址时显示「添加收货地址」和「导入微信地址」                                                                      | 有地址、无地址、地址不可配送（红字）                                                                            |
| `OrderCard`         | 订单列表项：状态、商品行（最多 3 行，超出显示「共 N 件」）、实付、操作按钮组（按状态由 `orderActions(order)` 纯函数决定，页面不自己写判断）                        | 按 `orderListTab` 的每个状态各一种                                                                              |
| `OrderItemRow`      | 商品图、名称、规格、单价、数量；可附带售后状态标签                                                                                                                 | —                                                                                                               |
| `PriceSummary`      | 结算页和订单详情的金额明细：商品金额、运费、优惠、实付                                                                                                             | —                                                                                                               |
| `ActionBar`         | 固定底栏：左侧图标入口（带角标）+ 右侧按钮；安全区                                                                                                                 | default、disabled（例如「已售罄」「活动未开始」替换主按钮）                                                     |
| `Countdown`         | 基于服务端时间：`app/config` 响应带 `serverTime`，客户端算出与本机时钟的偏移后再倒计时，不信任本机时间；`format: 'hms' \| 'dhms'`；到点回调                        | running、ended                                                                                                  |
| `Steps`、`Timeline` | 物流轨迹、售后进度                                                                                                                                                 | 当前节点高亮                                                                                                    |
| `ContactButton`     | 客服（C15）：`support.kind` 决定渲染 `open-type="contact"`，或跳转客服小程序，或不渲染                                                                             | contact、mini-program、hidden                                                                                   |
| `ShareSheet`        | 「分享给好友」（`open-type="share"`）+「生成海报」（打开 `PosterSheet`）；位于 `promo` 分包和主包各自的 `components/` 中                                           | —                                                                                                               |
| `PosterSheet`       | canvas 2D 海报：绘制 → 预览 → 保存到相册（需要隐私授权，失败时引导用户去设置页）；只在 `promo` 分包，商品详情通过分包异步化加载（`componentPlaceholder`，S1 验证） | drawing、ready、saving、saved、authDenied                                                                       |

### 4.5 导航与布局

| 组件            | 用途与要点                                                                                                                                                    | 状态             |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `NavBar`        | 自定义导航栏（仅首页、我的）：高度来自 `useNavBarMetrics`；滚动后背景由透明渐变为白色                                                                         | 透明、实色       |
| `Tabs`          | 横向 tab，可滚动，下划线用主色；支持吸顶                                                                                                                      | selected、带角标 |
| `Sticky`        | 吸顶容器（`position: sticky`，自定义导航栏页面要加上导航栏高度的偏移）                                                                                        | —                |
| `List`          | 分页列表的通用包装：下拉刷新（页面级 `enablePullDownRefresh`）+ 触底加载 + 空状态、错误、结束                                                                 | 同 `ProductGrid` |
| `LoginGate`     | 未登录时替代页面内容：插图 + 「登录后查看订单」+ 登录按钮（跳转到 `login { redirect: 当前路由 }`）                                                            | —                |
| `PrivacySheet`  | 全局隐私弹层（C04）：`onNeedPrivacyAuthorization` 触发；「拒绝」「同意」（`open-type="agreePrivacyAuthorization"`）；《隐私保护指引》用 `openPrivacyContract` | 关闭、打开       |
| `SplashOverlay` | 开屏浮层（首页）：图片 + 跳过倒计时 + 点击跳转（`LinkTarget`）                                                                                                | —                |

DIY 块（`apps/mini/src/blocks/`）不在 `ui/` 里。块由 S3 共享的 `packages/diy-blocks` 定义属性，小程序端各自实现渲染，但**只能组合 `ui/` 的组件**，不自己写按钮和价格样式。

---

## 5. 页面版式

| 版式               | 适用页面                                                             | 结构                                                                                                                                                                                                   |
| ------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **A. 装修流**      | 首页、我的、微页面                                                   | 自定义导航栏（首页、我的）或原生导航栏（微页面）→ DIY 块纵向排列，块间距由块自己的属性决定 → 原生 tabBar；每块独立加载骨架，一块出错只隐藏该块，不影响整页                                             |
| **B. 列表**        | 商品列表、订单列表、售后、优惠券、收藏、足迹、消息、文章、拼团、预售 | 原生导航栏 → 吸顶 `Tabs` 或筛选条 → `List`（灰底；卡片宽 702，左右边距 24，卡片间距 20）→ 可选的底部批量操作栏（收藏、足迹的「管理」模式）                                                             |
| **C. 详情 + 底栏** | 商品详情、拼团商品、预售商品、订单详情、售后详情                     | 顶部大图 / 状态头（订单详情是主色渐变背景 + 白字状态标题，渐变下半部分过渡为 `--color-bg`）→ 白卡分组 → `ActionBar`。商品详情滚动后显示吸顶锚点 tab（「商品」「评价」「详情」）                        |
| **D. 表单**        | 结算、地址编辑、申请售后、发票抬头、资料、手机号、密码、申请开票     | 原生导航栏 → `CellGroup` 分组（组间距 20）→ 底部固定提交按钮（`Button lg block`，安全区）；键盘弹起时 `adjust-position`，提交按钮不跟随键盘上移；提交时按钮 loading，失败时 toast 并聚焦第一个出错字段 |
| **E. 结果**        | 支付结果、售后提交后、注销申请后                                     | `ResultPage`：顶部留白 64 → 图标 → 标题 → 说明 → 主按钮 + 次按钮 → 可选的推荐商品 `ProductGrid`                                                                                                        |
| **F. 分栏**        | 分类                                                                 | 左侧一级类目栏（宽 180，`--color-bg` 底，选中项白底，左侧 6px 主色竖条）+ 右侧 `scroll-view`（顶部 banner、二级类目宫格、商品列表）                                                                    |
| **G. 登录**        | 登录                                                                 | 顶部店铺 logo 和名称（`app/config.site`）→ 主按钮「手机号快速登录」→ 次按钮「短信验证码登录」→ 文字链接「其他方式」→ 底部 `AgreementCheck`；右上角「暂不登录」                                         |

**通用规则：**

- **下拉刷新：** 列表、首页、我的开启。
- **回到顶部：** 滚动超过 2 屏后，右下角出现回到顶部按钮（`$z-bar`）。
- **加载状态：**
  - 首次加载用骨架屏；
  - 翻页用底部的「加载中…」；
  - 刷新用微信原生的下拉动画；
  - **不使用全屏 loading 遮罩**，只有支付和提交这类不可重复的操作才用 `toast.loading` 遮罩。
- **标题：** `navigationBarTitleText` 写在各页的 `*.config.ts` 中；动态标题（商品列表的分类名、文章标题）在数据到达后调用 `setNavigationBarTitle`。

---

## 6. 动效

- **时长：** 进入和展开 200ms，退出 150ms；缓动用 `cubic-bezier(0.2, 0, 0, 1)`。
- **弹层：** 底部弹层从下方滑入，遮罩淡入。
- **加购反馈：**
  - 按钮先轻微缩放（0.96 → 1）；
  - 然后 toast「已加入购物车」，同时 tab 角标 +1；
  - 不做抛物线飞入动画：实现成本高，在低端机上容易掉帧。
- **减弱动效：** 小程序没有 `prefers-reduced-motion`，所以骨架屏的闪光和轮播的自动播放都做成可关的开关（`app/config`），默认开启。自动轮播的间隔 ≥ 4 秒，用户手动滑动后暂停。

---

## 7. 无障碍

微信小程序从基础库 2.7.1 起支持部分 ARIA 属性（`aria-role`、`aria-label` 等），iOS VoiceOver 和 Android TalkBack 都会朗读。参考：

- https://developers.weixin.qq.com/community/business/doc/000804096442c82ec7fdb28765b40d
- https://developers.weixin.qq.com/community/develop/doc/000406895201687f01e17f65166000（`aria-label` 需要配合 `aria-role` 才生效）

| 要求                 | 做法                                                                                                                                                                     | 验证                                                                                              |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| 对比度               | 第 2.1 节的 token 全部达标；主题色通过第 3.3 节自动派生                                                                                                                  | [测试] `deriveTheme` 的单元测试：对 5 种旧调色板和随机颜色断言派生后 ≥ 4.5 / ≥ 3                  |
| 可点元素有角色和名称 | `View` 做成的可点元素统一走 `ui/Pressable`，它要求传入 `label`，并输出 `aria-role="button" aria-label={label}`；只有图标的按钮（购物车、客服、关闭、收藏）必须传 `label` | [守卫] ESLint 规则：`onClick` 只能出现在 `ui/` 组件上，页面里的原生 `View` 不能直接绑定 `onClick` |
| 价格读法             | `Price` 输出 `aria-label="价格 199 元"`；划线价读作「原价 299 元」                                                                                                       | [测试] 组件测试                                                                                   |
| 图片                 | 商品图的 `aria-label` 是商品名；纯装饰图 `aria-hidden="true"`                                                                                                            | 评审                                                                                              |
| 弹层焦点             | `Sheet`、`Dialog`、`PrivacySheet` 带 `aria-role="dialog" aria-modal="true"`，关闭按钮有 `label`                                                                          | [人工] 开发者工具「无障碍访问模式」插件，加真机 VoiceOver 抽查登录、下单、支付                    |
| 字体放大             | `page-font-size="system"`；版式不写死高度（第 2.2 节）                                                                                                                   | [人工] 微信「设置 → 通用 → 字体大小」调到最大，检查 4 个 tab、商品详情、结算不截断不重叠          |
| 热区                 | ≥ 88 × 88（第 2.3 节）                                                                                                                                                   | 评审                                                                                              |
| 不只靠颜色           | 状态用文字表达；表单错误有红字说明，而不只是红框；选中态除了颜色还有勾选图标或加粗                                                                                       | 评审                                                                                              |
| 时间限制             | 倒计时只用于真实截止时间；支付倒计时结束前 5 分钟不额外弹窗催促                                                                                                          | 评审                                                                                              |

---

## 8. 深色模式：本期不开启

**决定：** v1 的 `app.json` 不设置 `darkmode: true`，不提供 `theme.json`。微信在深色模式下会继续按浅色渲染本小程序（深色模式是可选能力：https://developers.weixin.qq.com/miniprogram/dev/framework/ability/darkmode.html ）。

**理由：**

1. **运营内容无法自动适配。** 首页、我的、微页面的 DIY 块自带背景色、文字色和图片（很多 banner 是带白底的设计图）。开启深色后，我们的外框会变深，但运营的内容仍是浅色，会出现「深色页面里夹着一块块白底 banner」的效果，比不支持更差。要做好，必须让每个 DIY 块再配一套深色属性，运营的工作量翻倍。
2. **商品图多为白底。** 电商主图普遍是白底，在深色卡片上会变成一个个白方块；主流电商小程序也不跟随深色模式。
3. **品牌色需要重新校验。** 第 3.3 节的派生规则针对白底；深色底需要另一套派生和校验，这是第二份设计工作。
4. **不影响上线和审核。** 深色模式不是审核要求。

**为以后开启保留的条件：**

- 所有颜色都经过语义 token（第 2.1 节），页面中不出现十六进制颜色（stylelint 的 `color-no-hex` 只在 `ui/tokens/` 中允许），所以以后只需在 `@media (prefers-color-scheme: dark)` 下再定义一份 `page { … }` 变量；
- 原生区域（导航栏、tabBar、背景）的颜色都已集中在 `platform/theme.ts`，以后可以改为 `theme.json` 的 `@变量`；
- 真正的前置工作是 DIY 块的深色属性。这是产品决定，列入待确认事项。

---

## 9. 如何检查（给 I1 集成流）

| 检查                                                                                            | 形式                                                                              |
| ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 页面和 `blocks/` 不直接导入 `@nutui/nutui-react-taro`                                           | [守卫] ESLint `no-restricted-imports`                                             |
| `ui/tokens/` 以外不出现十六进制或 `rgb()` 颜色字面量；间距只用 `$space-*`                       | [守卫] stylelint（`color-no-hex`、`scale-unlimited/declaration-strict-value`）    |
| 行内 `style` 中的尺寸经过 `Taro.pxTransform`                                                    | [守卫] ESLint 自定义规则（行内 `style` 中出现数字加 px 的字符串即报错）           |
| 每个 `ui/` 组件在 `__demo__` 中覆盖第 4 节列出的全部状态                                        | [测试] 演示页快照，由 I1 在开发者工具自动化中截图                                 |
| `deriveTheme` 对比度                                                                            | [测试] 单元测试（第 7 节）                                                        |
| 页面第一个节点是 `PageShell`（即 `page-meta`）                                                  | [守卫] 遍历 `pages/**/index.tsx` 和 `packages/**/index.tsx`，检查默认导出的根组件 |
| 真机：iPhone（有 Home 指示条）+ 一台安卓中端机，最大字体下的 4 个 tab、商品详情、结算、支付结果 | [人工] 发布前清单                                                                 |
