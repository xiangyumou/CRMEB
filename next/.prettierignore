# Generated or machine-owned: formatting them is either pointless or harmful.
pnpm-lock.yaml
node_modules/
dist/
.next/
.turbo/
coverage/
*.gen.ts
openapi.json

# Committed unformatted before P0-A started, and not ours to touch:
# `_conventions/**` is frozen and `turbo.json` is orchestrator-owned.
# See docs/rewrite/cr/CR-2-p0a.md — delete these two lines once the
# orchestrator has run `prettier --write` on them.
packages/contracts/src/_conventions/route.ts
turbo.json
